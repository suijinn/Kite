# Kite ロードマップ

当初の要件に対する達成状況と、未実装項目の優先順位。
設計の全体像は [architecture.md](architecture.md)、機能ごとの判断理由と踏んだ罠は
リポジトリ直下の [CLAUDE.md](../CLAUDE.md) を参照。

凡例: ✅ 完了 / 🟡 部分的 / ⬜ 未着手
規模感: **S** = 1 日以内 / **M** = 数日 / **L** = 1〜2 週 / **XL** = それ以上

---

## 1. 要件別の達成状況

| # | 要件 | 状態 | 補足 |
| --- | --- | --- | --- |
| 1 | エクスプローラーを代替できる | 🟡 | 日常操作・仮想フォルダ・書庫は可能。`Win+E` とフォルダを開く操作も Kite に向けられる（P2-11）。検索・サムネイル・表示形式の切替が未実装（→ フェーズ 2） |
| 2 | C++ で実装 | ✅ | C++20 / MSVC。サードパーティ依存ゼロ |
| 3 | 高速に起動できる | ✅ | 145〜320 ms。起動パスに COM・シェル呼び出しを一切置かない設計 |
| 4 | 軽快に動作する | ✅ | 全描画が 1 フレーム 1 ms 未満。列挙は常にワーカースレッド |
| 5 | メモリ使用量が控えめ | 🟡 | 約 66 MB。大半は D3D11/DXGI スタック（→ P4-1） |
| 6 | Win11 エクスプローラーの主要機能 | 🟡 | 下の「機能パリティ差分」に一覧 |
| 7 | タブ機能 | ✅ | 追加・複製・復元・番号ジャンプ・ドラッグ並べ替え・ペイン間移動・窓の外へ引き抜いて新しいウィンドウ。枚数が増えればタブバーが折り返す（P3-9）。バーは縦置きにもできる（P3-10）。新しいタブの位置は設定可 |
| 8 | 拡張コンテキストメニューが一発 | ✅ | `Shift+F10`／`Shift`+右クリックで拡張動詞まで直接表示（既定では出さない ─ CLAUDE.md「シェル拡張の隔離」） |
| 9 | 縦・横のペイン分割 | ✅ | 入れ子可。比率ドラッグ、方向キーでフォーカス移動 |
| 10 | セッションの保存と即時切替 | ✅ | 全セッション常駐。切替はインデックス移動のみ。分割レイアウトごと永続化。チップのドラッグと `Ctrl+Alt+Shift+PageUp/PageDown` で並べ替え可。数が増えればセッションバーが折り返す（P3-9） |
| 11 | ブックマーク | ✅ | サイドバー表示、`Alt+Shift+1..8` で直行。9 件目以降は `Ctrl+P` の «行き先» 一覧から絞り込んで選ぶ（開いているタブ・クイックアクセス・ドライブ・履歴も同じ一覧に並ぶ ─ P3-12） |
| 12 | 全操作にキー割当・変更可能 | ✅ | 全コマンドを `Ctrl+Shift+,` の設定画面と `keys.ini` の両方で変更可。`F1` で一覧。割り当てを覚えていなくても `Ctrl+Shift+P` のコマンドパレットから実行できる（P3-1） |
| 13 | クラウドストレージでクラッシュしない | ✅ | 実フォルダの列挙はシェル非依存。コンテキストメニュー・アイコン（オーバーレイ）・仮想フォルダの列挙（名前空間拡張）は `kite_shellhost.exe` で別プロセス実行（P1-1 / P2-10 / P2-1 完了） |
| 14 | 外部ストレージ・ネットワークドライブ | ✅ | 非同期列挙で固まらない。切断時はエラー表示で復帰。空き容量取得も遅い媒体では省略 |
| 15 | ダークテーマ | ✅ | 既定は無彩色。タイトルバーとシェルのコンテキストメニューも追従。配色は ini で全項目上書き可 |
| 16 | OS 依存部分の分離 | ✅ | 3 層構成。移植時に書くのは 5 つのインターフェースのみ。テストが分離を検査 |
| 17 | 多言語対応が可能な設計 | ✅ | 内部 UTF-8。日英内蔵、`lang.<code>.ini` で再ビルド無しに追加可 |
| 18 | file pilot の不満（日本語・キー割当） | ✅ | UI 全体が日本語。IME 変換窓もキャレット追従。キーは全て再割当可 |
| 19 | filedini の不満（起動速度・ペイン分割） | ✅ | 起動は上記のとおり。分割は入れ子・比率調整まで対応 |

---

## 2. 機能パリティ差分（要件 6 の内訳）

Windows 11 エクスプローラーにあって Kite にまだ無いもの。

| 機能 | 状態 | 対応フェーズ |
| --- | --- | --- |
| ファイル一覧・並べ替え・隠しファイル | ✅ | — |
| コピー／切り取り／貼り付け／削除／リネーム／新規作成 | ✅ | 同一フォルダへの貼り付けは `_copy` の複製になる。実行はワーカースレッド（`FileOpQueue`、同時 4 件まで）なので UI は止まらない |
| ごみ箱への削除・完全削除 | ✅ | — |
| ドラッグ＆ドロップ（アプリ内・Explorer 相互） | ✅ | — |
| コンテキストメニュー（拡張含む）・プロパティ | ✅ | — |
| 戻る／進む／親フォルダ・パンくず | ✅ | — |
| ファイル変更の自動反映 | ✅ | — |
| 「PC」「ごみ箱」「ネットワーク」等の仮想フォルダ | ✅ | P2-1。開いて辿れる。ごみ箱からの復元も可（`Alt+R`） |
| 書庫をフォルダとして開く | ✅ | P2-2。`.zip` / `.cab` は常に、`.tar.gz` などは Windows 11 で。`[ui] open_archives` で切り替え |
| **検索** | ⬜ | P2-3 |
| ファイル種別ごとの実アイコン・アイコンオーバーレイ | ✅ | P2-10 |
| **サムネイル表示** | ⬜ | P2-4 |
| **表示形式の切替（アイコン／一覧／並べて表示）** | ⬜ | P2-5 |
| **列のカスタマイズ・幅調整・グループ化** | ⬜ | P2-6 |
| **プレビューウィンドウ／詳細ウィンドウ** | ⬜ | P2-7 |
| ネットワークの参照（`\\server` の共有列挙） | ✅ | P2-8 |
| **ネットワーク全体の参照（サーバーの探索）** | ⬜ | P2-8b |
| **ドライブの安全な取り外し** | ⬜ | P2-9 |
| 既定のファイルマネージャーとして登録（`Win+E`・フォルダを開く） | ✅ | P2-11。`HKCU` のみ。設定画面「システム」から、確認を挟んで登録・解除。エクスプローラーの窓の中（デスクトップを含む）だけは取れない |
| 複数ウィンドウ（`Ctrl+N`） | ✅ | — |
| 元に戻す（Ctrl+Z） | ✅ | P1-2。名前変更・新規作成・移動・コピー・ごみ箱への削除。完全削除だけは戻らない |
| 単一インスタンス（既存ウィンドウで開く） | ✅ | P1-3 |
| クイックアクセスのピン留め編集 | ⬜ | P3-5 |
| アドレスバーの入力補完 | ✅ | P3-6 |
| 型入力ジャンプ（文字キーで項目へ飛ぶ） | ✅ | P3-4 |
| 対象の行の上でリネーム・新規作成 | ✅ | P3-3 |
| **表示設定（ソート・隠しファイル）の記憶** | ⬜ | P3-13。エクスプローラーはフォルダごと、Kite が覚えるべきはタブごと |
| 設定画面（GUI） | ✅ | P3-8 |
| コマンドパレット（エクスプローラーには無い） | ✅ | P3-1 |
| 行き先の検索（ブックマーク・タブ・クイックアクセス・ドライブ・履歴を絞り込んで飛ぶ） | ✅ | P3-12 |

---

## 3. フェーズ 1 — 安定性（最優先）

要件 13 の残課題と、常用に入る前に潰しておくべきもの。

### P1-1. シェル拡張の別プロセス化 `kite_shellhost.exe` — ✅ 完了

`IContextMenu` のハンドラ（Box、Google Drive、7-Zip、TortoiseGit …）は
`kite_shellhost.exe` の中だけで動く。拡張が確保したメモリを壊しても、裏でスレッドを
立てて落ちても、失われるのはメニュー 1 回分とホストプロセスだけ。次の要求で黙って
起動し直す。これで要件 13 が埋まった。

構成は `ShellHostProtocol.h`（交換する書式。Windows 非依存で `tests/test_hostproto.cpp`
が直接検証）、`ShellPipe.cpp`（名前付きパイプ）、`ShellHostClient.cpp`（`kite.exe` 側）、
`ShellMenu.cpp`（`IContextMenu` 本体。**ホストだけがリンクする**）、
`tools/shellhost/main.cpp`（ホストの入口）。

core / ui 側の変更は `IShellIntegration` の戻り値が `void` から `bool` になった
（メニューを出せなかったことを status 行に出す）だけ。設計上の判断は CLAUDE.md の
「シェル拡張の隔離」、残っている制限は同「現時点の弱点」。

### P1-2. 元に戻す（Ctrl+Z）— ✅ 完了

`Ctrl+Z`（`Cmd::Undo`）が、**名前の変更・新規作成・移動・コピー・ごみ箱への削除**を
元に戻す。履歴は `core/app/UndoStack`（OS にも描画にも触れない）が持ち、逆操作は
`App::DoUndo` が `IFileSystem`（削除だけは `IShellIntegration`）を通して行う。
`tests/test_undo.cpp` が端から端まで検証する。

**シェル任せにはできなかった。** `FOF_ALLOWUNDO` はシェルの Undo スタックに積むが、
そこを呼ぶ公開 API が無い。加えて名前の変更は `MoveFileExW` なので、そもそもそのスタックに
載らない ─ シェル任せにすると「F2 で付けた名前を戻す」という一番よく要る操作が
最初から対象外になる。

**ごみ箱への削除は戻せる。** 履歴が覚えるのは**消される前のパス**で、ごみ箱の中で
名乗る `$R` の名前ではない ─ 削除の時点で分かっているのがそちらしかなく、控えるには
削除のたびにごみ箱を引き当て直すことになって、一度も `Ctrl+Z` を押さない人まで
その代金を払うことになる。突き合わせは戻すときに 1 回だけ行い、「元の場所」
（`PID_DISPLACED_FROM`）と項目名から元のパスを組み立てて比べる。同じパスの項目が
複数あれば**最後に消したもの**を採る（`PID_DISPLACED_DATE`）─ `Ctrl+Z` が指している
のは常に直前の削除なので、それが正しい答えになる。

**完全削除だけは戻せない。** 履歴に「戻せない印」（`UndoKind::Erase`）を積み、`Ctrl+Z`
はそこで止まって理由を言う ─ 飛び越えると、消えたファイルはそのままにその前の
名前変更だけが巻き戻る。**ごみ箱への削除ではこの印を積まない**（戻せる以上、その下の
操作が到達不能になる理由が無い）。設計上の判断は CLAUDE.md の「元に戻す」と
「`Ctrl+Z` で削除を戻す」。

### P1-3. 単一インスタンス化 — ✅ 完了

2 つ目の起動は、既存のウィンドウへパスを渡して自分は黙って終わる。パスは
そのウィンドウの新しいタブになり、ウィンドウが前面に出る。目印（ミューテックス名と
ウィンドウクラス名）は **exe のフルパスから導く**ので、別フォルダに展開した 2 つの
コピーは別のインスタンスとして動く ─ 固定名にすると、一方の `config` だけが使われる
（CLAUDE.md「設定の置き場所」）。

`--new-window` が付いた起動だけがこの検出を通らない。`Ctrl+N`（`Cmd::NewWindow`）が
使うのがこの引数で、今や「本当に新しい窓が要る」と言える唯一の手段。

| ファイル | 役割 |
| --- | --- |
| `platform/win/WinSingleInstance.cpp` | 目印の生成、既存ウィンドウの探索、`WM_COPYDATA` の送受形式 |
| `src/main_win.cpp` | 起動時の振り分け |
| `App::OpenForwardedPaths` | 受け取ったパスをタブとして開く |

設計上の判断は CLAUDE.md の「単一インスタンス」。

### P1-4. エラー処理の網羅 — ✅ 完了

**失敗は必ず言う。** 名前の変更・作成・削除・コピー・移動・元に戻すは、失敗したら
何が失敗したかをステータス行に出す（`App::ReportFailure`）。以前は OS の文言を
そのまま流していたので、`ErrorText()` が文言を持たないコードでは**何も出ないまま
操作だけが失敗した** ─ 画面が何も変わらないので「キーが効かない」と読める。
設計上の判断は CLAUDE.md の「失敗の伝え方」。

| 潰したもの | どうしたか |
| --- | --- |
| アクセス拒否フォルダ | 一覧はエラー行を出し、`..` も出さない。UNC なら `Ctrl+Shift+L` を案内 |
| ロック中ファイルのリネーム | 「名前を変更できませんでした」＋ OS の文言。履歴には積まない |
| 260 文字超のパス | 規則を `path::ToExtended()`（core）へ移して検証。**拡張パスには OS の正規化が掛からない**ので、付ける前に `Normalize()` を通す |
| 列挙中のドライブ切断 | `FindNextFileW` が落ちたら**半分の一覧を捨てる**。デバイス系のコードを `Unavailable` に振る |
| オフラインのクラウドフォルダ | `ERROR_CLOUD_FILE_*` を `Unavailable` / `AccessDenied` に振る。空のフォルダとして出さない |
| シェル操作の失敗 | `SHFileOperation` の `DE_*` を Win32 コードへ写して OS の言葉で出す。利用者の取り消しは失敗にしない |

検証は `tests/test_app.cpp`（失敗の伝わり方）、`tests/test_pathutil.cpp`（拡張パス）、
`tests/test_utf8.cpp`（UTF-16 換算の長さ）。`FakeFileSystem::locked` が、
OS が何も言わない失敗を含めて再現する。

### P1-5. CI — ✅ 完了

`.github/workflows/ci.yml` が main への push と PR で回る。ビルド＋全テスト
（`-DKITE_WERROR=ON`）、2 つの exe が並ぶか、`core/` `ui/` `tests/` に Windows ヘッダが
無いか、シェル拡張が `kite.exe` に入っていないか、Doxygen 警告ゼロ、`.ps1` の BOM と
構文。詳細は CLAUDE.md の「CI」。

### P1-6. 終了時の保存失敗を伝える — **S**

**現状、終了時の `SaveAll()` が失敗しても誰にも見えない。** ステータス行はもう描かれず、
`IHost` にダイアログを出す手段も無い（CLAUDE.md「設定の置き場所」）。書けない場所に展開
すれば初回起動の `keys.ini` で必ず露見するので実用上は気付けるが、**「一度書けた場所が後から
書けなくなった」場合だけが漏れる** ─ 設定を変えたつもりで消えている、という一番たちの悪い形。

手当ての候補は 2 つ。(1) 失敗した事実を次の起動で言う（書けなかった旨をどこかに残せない
以上、これは「前回の終了時に書けなかった」ではなく「今この設定フォルダに書けない」の検査に
なる ─ 起動時に書き込み可否を 1 回試す）。(2) 閉じる前に保存して、失敗したらステータス行に
出して閉じるのをやめる。**(2) のほうが正直**だが、× を押したのに閉じないのは説明が要る。

---

## 4. フェーズ 2 — エクスプローラー機能パリティ

要件 6 を埋める。ここまで来ると「常用してエクスプローラーを無効化できる」水準。

### P2-1. 仮想フォルダ（「PC」「ごみ箱」「ネットワーク」）— ✅ 完了

3 つともサイドバーの先頭に並び、開いて辿れる。「PC」からドライブへ入れば普通のフォルダ
（`C:\`）になり、`Alt+↑` で戻れば「PC」に出る ─ `\\server` の上が「ネットワーク」に
なるのも同じ規則（`vfs::ParentOf`）。名前空間拡張が作る入れ子のフォルダ（iCloud フォト
など）もそのまま開ける。

`WinShellFileSystem` を別に立てるのではなく、`WinFileSystem::List` が `virtual:` の付いた
パスだけを振り分ける形にした。**実 FS の高速経路には一切触れていない。** `fs::Entry` に
足したのは PIDL ではなく `address` という文字列 1 本で、実フォルダの項目では空のまま ─
10 万件のフォルダで 1 件ずつ std::string を増やす代償を払わない。

**列挙は `kite_shellhost.exe` の中だけ。** `IShellFolder::EnumObjects` は名前空間拡張の
DLL を呼び出し元プロセスに読み込ませる（「PC」を開くと iCloud の拡張が起き上がるのを
実測した）。P1-1 / P2-10 で追い出したものと性質が同じなので、3 つ目のホストとして同じ
仕組みに乗せてある。読み取りには 30 秒の制限がある ─ 名前空間拡張の列挙は中断できない
ので、返ってこない相手にできるのはホストごと捨てることだけ。

| ファイル | 役割 |
| --- | --- |
| `core/fs/VirtualPath.h` | `virtual:` の規則と 3 つの根、`..` の行き先（`tests/test_vfolder.cpp`） |
| `platform/win/VirtualNames.cpp` | Kite の綴りとシェルの解析名の相互変換。**ホストは前置を知らない** |
| `platform/win/ShellFolder.cpp` | `IShellFolder` の列挙本体。**`kite_shellhost.exe` だけがリンクする** |
| `platform/win/FolderHostClient.cpp` | ホストへの要求（時間制限あり、ワーカー間で直列化） |
| `platform/win/ShellFolder.cpp` の `ResolveItemsInFolder` | 「フォルダ + 解析名」から項目の PIDL を引き当てる |

**ごみ箱の項目は「どのフォルダの中の項目か」ごと渡して指す。** 消した項目の解析名は
隠された `$R` の写しのパスなので、そのまま解析するとごみ箱の項目ではなく**ただの
ファイル**が返り、その親はごみ箱ではないので「元に戻す」がどこにも現れない。要求に
フォルダを添えると（`shellhost::Request::container`）、ホストはそのフォルダを列挙して
解析名の一致する子の PIDL を組み立てる ─ 得られる絶対 PIDL の親はごみ箱そのものなので、
メニューもアイコンも今までどおりの道を通る。**PIDL をホスト側の表に置いて id を配る形は
採らなかった**（世代がずれた id は「別のファイルを消す」になる）。

**復元は `Alt+R`（`Cmd::Restore`）。** 実体はシェルの `undelete` 動詞で、右クリックの
メニューにも「元に戻す」が正しく並ぶ。設計上の判断は CLAUDE.md の「仮想フォルダ」と
「ごみ箱の項目を正しく指す」。

### P2-2. 書庫をフォルダとして開く — ✅ 完了

一覧で `.zip` や `.tar.gz` を開くと、中身がそのまま一覧に出る。書庫はシェル名前空間
拡張として見えるので、**列挙もアイコンもメニューも P2-1 の経路がそのまま通った** ─
足したのは入口の判断（`App::ActivateEntry` が `shell_.Open()` ではなく
`vfs::ArchivePath()` を通して `OpenPath()` を呼ぶ）と、実 FS との境目をどこに置くか
だけ。判断は `core/fs/VirtualPath`（OS にも描画にも触れない）が持ち、
`tests/test_vfolder.cpp` と `tests/test_app.cpp` が端から端まで検証する。

**下地でまかなえなかったのは 4 つ。** (1) `..` は書庫そのものから**実フォルダへ抜ける**
必要があった ─ 書庫の縁がシェル名前空間と実 FS の境目で、そこで止めると `Alt+↑` も
パンくずも中から出られない。(2) 書庫の中の項目は、シェルが `SFGAO_FILESYSTEM` を
立ててきても実 FS ではない ─ 解析名がパスの綴りそのものなので、信じると開いた瞬間に
「利用できません」になる。(3) `path::Normalize` が `virtual:` の前置を 1 つの構成要素
として畳んでいて、**UNC 上の書庫で先頭の `\\` が 1 本になっていた。**
(4) 中のファイルを開くのに `ShellExecute` もシェルの「開く」動詞も使えなかった ─
前者はその綴りのファイルがディスクに無いので開けず、後者は explorer.exe が呼び出し元
プロセスからデータを吸い出す作りなので、**空のファイルが開いた。** `IFileOperation` で
`%TEMP%` へ同期的に写しを作り、出来上がった実ファイルを開いている。

**どの拡張子が開けるかは起動時にレジストリへ訊く**（`win::ShellFolderExtensions()` →
`vfs::SetArchiveExtensions()`）。決め打ちにできないのは、同じ Kite が動く先で答えが違う
から ─ Windows 11 は 23H2 から libarchive 版の `ArchiveFolder` で tar・gz・7z・rar まで
名前空間に載せるが、Windows 10 には無く zip と cab だけで、7-Zip を入れて関連付ければ
その環境の `.7z` は名前空間から外れる。**開けない拡張子を書庫だと言うと「開いたのに空」に
なる**ので、判定は「関連付けられた ProgID がシェル名前空間の CLSID を名乗るか」に置いた。
開けないものは今までどおりシェルに渡り、利用者が入れたソフトが開く ─ それがフォールバック
そのもの。`.tar.gz` に専用の規則は要らない（`path::Extension()` は `gz` を返し、`.gz` を
開けるシェルは中の tar もそのまま展開して見せる）。

**既定は「フォルダとして開く」**（`[ui] open_archives`、設定画面「書庫をフォルダとして
開く」）。展開ソフトを入れている人の答えは違うので設定にしてあるが、既定を off にすると
機能が在ること自体が伝わらないうえ、on でも失う操作は無い ─ 右クリックのメニューには
シェルのものがそのまま出る。設計上の判断は CLAUDE.md の「書庫をフォルダとして開く」。

### P2-3. 検索 — **M**

現フォルダ以下の再帰インクリメンタル検索。ワーカースレッドで列挙し、
結果を仮想的な一覧としてタブに表示する。Windows Search インデックスは使わず
自前で歩く（インデックス未作成のフォルダでも効くほうが実用的）。

主な変更: `core/fs/` に `SearchJob`、`Tab` に「検索結果モード」、`Cmd::Search`。

### P2-4. サムネイル — **M**（P2-10 で下地ができた）

`IThumbnailCache` / `IShellItemImageFactory` を**必ず別スレッド**で呼ぶ。
`ui::Renderer::DrawIcon` と `IIconProvider`、ホストとの画素のやりとりは P2-10 で
できているので、要求の種別とサイズを足して同じ経路に乗せる。
クラウドのプレースホルダに対しては要求しないこと（ダウンロードが走る）。

### P2-5. 表示形式の切替 — **M**

詳細（現行）／一覧／中アイコン／大アイコン。`ui/views/` に描画モードを足す。
P2-4 のサムネイルとセットで意味を持つ。

### P2-6. 列のカスタマイズ・幅調整・グループ化 — **M**

現在は列幅固定・列固定。`ViewState` に列定義を持たせ、ヘッダのドラッグで幅変更、
`settings.ini` に永続化。

### P2-7. プレビューウィンドウ — **M**

`IPreviewHandler` を使う場合はサードパーティコードが再び自プロセスに入るため、
**P1-1 で作った `kite_shellhost.exe` に相乗りさせる**のが筋。`ShellHostProtocol.h` に
要求の種別を足し、プレビューはホスト側のウィンドウを Kite の子として貼るか、
ビットマップを共有メモリで受け取る形になる。

### P2-8. ネットワークの参照 — ✅ 完了

`\\192.168.1.5` のようにサーバーだけを指したパスが、その共有の一覧になる。
判定は `path::IsUncServer()`、列挙は `WinFileSystem::List` が `WNetOpenEnum` /
`WNetEnumResource` へ振り分ける（他の列挙と同じくワーカースレッド）。共有の親は
サーバー、というパスの読み方も揃えてあるので `..` と `Alt+↑` とパンくずがそのまま
効く。資格情報が要る相手には `Ctrl+Shift+L`（`Cmd::ConnectNetwork`）。
詳細は CLAUDE.md「ネットワークの参照」。

### P2-8b. ネットワーク全体の参照 — **M**

**混同しないこと。** P2-1 で開けるようになったのはエクスプローラーの「ネットワーク」
フォルダ（シェル名前空間）で、そこに並ぶのは主に SSDP で見つかった機器 ─ 実測で
15 秒待ってメディアレンダラーが 2 つ、という環境がある。ここで言っているのは
`\\` そのものを開いて SMB サーバーを列挙する話で、そちらはまだ入っていない。

`\\` そのものを開いて、ドメイン／ワークグループとその中のサーバーを列挙する話。
`WNetOpenEnum` に `lpRemoteName = nullptr` を渡し、返ってきたコンテナをもう 1 段
展開すればよい ─ **が、これはコンピューターブラウザーサービスに依存する**。
SMB1 とともに既定で無効化されて久しく、今の Windows では数十秒待たされた末に
空が返ることのほうが多い。**待たせて空を返す機能は、無い機能より悪い。**
入れるならタイムアウトと「探索中」の表示が先。

### P2-9. ドライブの安全な取り外し — **S**

サイドバーのコンテキストメニューから。`CM_Request_Device_Eject`。

### P2-10. 実アイコンとオーバーレイ — ✅ 完了

一覧とサイドバーがシェルの実アイコンを表示する。TortoiseGit・OneDrive・Google Drive
などのアイコンオーバーレイもそのまま乗る（`SHGFI_ADDOVERLAYS`）。

取得は `kite_shellhost.exe` の中だけ。オーバーレイハンドラは P1-1 で追い出したものと
性質が同じ in-proc DLL であり、加えて `SHGetFileInfo` は中断できないので、別プロセスで
あることが「返ってこないハンドラ」への唯一の対処になっている（CLAUDE.md
「シェル拡張の隔離」）。

| ファイル | 役割 |
| --- | --- |
| `core/app/IconProvider.h` | `IIconProvider` と `IconCache`。要求の重複排除と上限管理（`tests/test_iconcache.cpp`） |
| `platform/win/WinIconProvider.cpp` | ワーカースレッド。要求をまとめ、結果をレンダラへ渡す |
| `platform/win/IconHostClient.cpp` | ホストへの要求（時間制限あり） |
| `platform/win/ShellIcons.cpp` | `SHGetFileInfo` 本体。**`kite_shellhost.exe` だけがリンクする** |
| `platform/win/ShellHostProcess.cpp` | ホスト 1 つ分の起動・接続・後始末。3 つのホストが共有 |

`[ui] shell_icons = false` で従来のベクタ描画に戻せる。

### P2-11. 既定のファイルマネージャーとして登録 — ✅ 完了

**「エクスプローラーを置き換える」という目標そのものが、利用者の手作業に残っていた。**
`Win+E`、フォルダのショートカットのダブルクリック、他アプリの「フォルダーを開く」は
全部エクスプローラーへ行っていた。単一インスタンス（P1-3）とコマンドライン引数の受け口は
すでにあったので、足したのは**登録と、その解除**だけ。

入口は設定画面（`Ctrl+,`）の**「システム」→「既定のファイルマネージャー」**。
レジストリの綴りを知っているのは `platform/win/WinDefaultManager.cpp` だけで、
`core` が受け取るのは `DefaultManager`（未対応・未登録・この exe・別の場所の Kite）という
4 つの答えだけ。

- **コマンドではなく設定の行にした。** これは動作ではなく**状態**で、「Kite は既定か」には
  打鍵より長生きする答えがある ─ 行はそれを値として出せるが、コマンドは出せない。
  トグル 1 つならラベルが半分の確率で嘘になり、2 つに割れば «今どちらなのか» を押す前に
  知っている人しか正しく選べない。**キー割り当ても持たない**（一度きりの操作に和音を
  割く理由が無い）。
- **設定画面で唯一、確認を挟む行。** 他の行は「切り替えたことをその場で目で確かめられる」
  が、この行の効果は **Kite の外**に出る ─ 一覧を降りる途中で矢印キーがかすっただけで
  `Win+E` の行き先が変わってはならない。確認は打鍵も描画もマウスも最上位に置く。
- **行の値は覚えず、開くたび OS に訊く。** 実体はレジストリにあるので、Kite の外で
  変えられていても行が嘘をつかない。**この行だけ `settings.ini` に何も書かない。**
- **書くのは 4 つのキーで、全部 `HKCU\Software\Classes` の下** ─
  `Folder\shell\open\command`、`Directory\shell\open\command`、
  `CLSID\{52205fd8-…}\shell\open\command` と同 `\opennewwindow\command`。
- **フォルダを開いているのは `Folder` であって `Directory` ではなかった。** `Directory` だけを
  書いた版を実機で試したところ、`Win+E` は Kite になったのに**デスクトップのフォルダは
  エクスプローラーで開いたまま**だった ─ レジストリを見ると `HKCR\Directory\shell` に
  `open` が無く、既定の動詞は `none`。フォルダを開く動詞は上位クラスの `Folder` から
  継承されていて、その中身は `Explorer.exe` + `DelegateExecute`。
- **`Folder` は上位クラスなのでシェル自身の場所も通る**（コントロール パネル、PC、
  ネットワークの場所）。そこには渡せるパスが無く `%1` には解析名が届くので、
  `vfs::FromCommandLine` が `::{CLSID}` に `virtual:` を付けて、すでに在る仮想フォルダの
  列挙に乗せる。
- **CLSID は別の仕掛け。** 書かないと `Win+E` だけエクスプローラーのまま残る。
- **`DelegateExecute` は空文字で潰す。** `HKCR` は `HKCU` と `HKLM` を**値ごとに**
  重ねるので、既定値だけを上書きしたキーからは Windows 同梱の `DelegateExecute` が
  そのまま見え続ける ─ 実際に走るのは書いたコマンドラインではなくそちらのハンドラになる。
- **`HKCU` にだけ書く。昇格は求めない** ─ ファイラーを 1 つ入れるために昇格を求めるのは、
  そのファイラーが何をするか分からない人にとって最悪の第一印象になる。
- **付けたものは全部外せる。** 書く前の状態を `HKCU\Software\Kite\DefaultManagerBackup`
  に控え、解除はその控えを読んで戻す。**控えは «Kite が触る前» の状態を指す**ので既に
  登録済みの状態では取り直さず、控えが 1 つも無ければ **Kite を名指しているキーだけ**を
  消す。途中で失敗したら、そこまでを戻してから false を返す。
- **ポータブル運用と噛み合わせてある。** exe を移すと登録は死ぬので、起動時に
  「登録されているパスが自分か」を見て、**同じ名前の exe が別の場所から**登録されている
  ときだけステータス行で言う。**他のアプリが登録されている場合は言わない** ─ それは
  事故ではなく利用者の選択。
- **書き換えたら `SHChangeNotify(SHCNE_ASSOCCHANGED)` を投げる。**

**未検証: デスクトップとエクスプローラーの窓の中でフォルダを開いたとき。**
`Directory` だけを書いていた版では取れず、`Folder` に直した版はまだ実機で確かめていない。
取れなかった場合、シェルのビューはフォルダを**自分のプロセスの中で**開いている ─
レジストリの動詞をそもそも通っていない ─ ということなので、残る道は explorer.exe に
読み込まれる DLL しかない。**その道は採らない**（シェル拡張を別プロセスへ追い出した
P1-1 と正面からぶつかる）ので、そこがこの機能の限界になる。`Win+E`・ショートカット・
他アプリの「フォルダーを開く」はこの経路の外なので、いずれにせよ Kite に来る。

---

## 5. フェーズ 3 — 操作性（file pilot / filedini を超える部分）

### P3-1. コマンドパレット — ✅ 完了

`Ctrl+Shift+P` で全コマンドを 1 枚のオーバーレイに並べ、絞り込んで `Enter` で実行する。
**キー割り当てを覚えていなくても、あるいは和音を常駐ソフトに奪われていても、そこから
全操作に届く** ─ `F1` の一覧は読むためのもので、実行する経路を持っていなかった。
メニューバーもリボンも足さない方針のもとで、要件 12 の実用性がここで一段上がる。

`Ctrl+P` の行き先の一覧と同じ画面にしてある ─ 同じパネル、同じ行の刻み、表示中は
全打鍵を飲み込み、閉じるのは `Esc`。**絞り込み・カーソル・スクロールは
`core/app/PickerList` に 1 つだけ置いて両方が使う**（タブバーの縦横で折り返しの計算を
共通にしてあるのと同じ判断 ─ 2 つに分かれた時点でどちらかが必ず古くなる）。違うのは
行が持つものだけで、パスではなくコマンドのラベル・分類・割り当てられている和音。

| ファイル | 役割 |
| --- | --- |
| `core/app/PickerList.h` | 絞り込み付き一覧の、行の中身に依らない部分。行き先の一覧と共有 |
| `core/input/TextField.h` | 絞り込み欄そのもの。Kite の全入力欄と共有（`tests/test_textfield.cpp`） |
| `ui/AppUiOverlays.cpp` | `AppUi::PaintPickerFrame`。パネル・表題・入力欄も行き先の一覧と共有（入力欄そのものを描く `PaintTextField` は `ui/AppUi.cpp`） |
| `core/app/CommandPalette.h` | 行の組み立てと «何をすべきか» の判断（`tests/test_commandpalette.cpp`） |
| `core/app/App.cpp` | `App::RunPaletteCommand()`。閉じてから `Execute` を呼ぶ |
| `ui/AppUiOverlays.cpp` | `AppUi::PaintCommandPalette`。ブックマーク一覧の兄弟 |

決めたこと:

- **絞り込みはラベル・分類・`keys.ini` 上の名前の 3 つに当てる。** 設定ファイルで見た
  `tab.new` を打つ人がいて、しかもそれは表示言語で動かない唯一の綴り。**当たる 3 つは
  3 つとも行に出す** ─ 出ていない条件で絞り込めることは、誰にも伝わらない。
- **並びはコマンド表の定義順。** 使用頻度で並べ替えると、探している行が打鍵ごとに動く。
- **`Enter` だけが実行。** `Ctrl+Enter` に «別の実行のしかた» は無いので、意味を作らない。
- **自分自身の行は出さない。** 選ぶとパレットが閉じて開き直るだけの行き止まりになる。
- **割り当ての無いコマンドには「未割り当て」と出す。** 何も書かない行は「窓が狭くて
  切れた列」と見分けが付かないうえ、«どれにキーが無いのか» はこの画面が答えるべき問い。
- **絞り込みは小さな添え書きではなく入力欄。** 打ち込む相手なので、文字の大きさで器を
  作り、キャレットとアクセント色の縁を持たせる。ブックマーク一覧も同じ器を使う。
- **並べるのはコマンドの «表» で、実行中に増減する «データ» は並べない**（行き先の窓口は
  P3-12）。ブックマークを行として足すと、次はタブ、セッション、ドライブを断る理由が
  無くなる。ただし番号で指す
  8 個（`Alt+Shift+1..8`）には**行き先の名前を添える** ─ 「ブックマーク 1 へ」は画面に
  出ていない一覧の «何番目» を指すだけで、それ単体では読めない行だった。行は増えないので
  «データを並べる» ことにはならず、9 件目以降は `Ctrl+P` の仕事のまま。
- **パネルの寸法は窓だけで決める。件数では決めない。** 一致件数で決めると打鍵のたびに
  パネルが縮み、絞り込み前の件数で決めると画面ごとに高さが変わる ─ パレットから
  `nav.places` を選んだ瞬間に、打ち込んでいた入力欄が別の場所へ飛ぶ。行き先の一覧と
  パレットは同じ大きさで出る。
- **実行するのは `App`。** `HandleKey()` は「何をすべきか」を返すだけ ─ この画面が
  コマンドを実行し始めた時点で «選ばせるもの» ではなくなる（ブックマーク一覧と同じ）。

### P3-2. 2 打鍵キーシーケンス — **S**

`Chord` は拡張できる形にしてある。`KeyMap` を「1 打鍵目で待機状態に入る」構造に変え、
`keys.ini` の記法を `Ctrl+K Ctrl+\` 形式に拡張する。

### P3-3. 対象の上で編集する（インラインリネーム）— ✅ 完了

**1 つの名前を尋ねる入力欄は、その名前が書かれている場所に出す**（`Prompt::isInline()`）。
リネームは対象の行、新規作成はカーソルの 1 行下に借りた行、セッション名はそのチップの中。
下端の帯に残るのは 2 つだけ ─ 一覧全体を相手にする絞り込みと、文字を打つのではない削除の
確認。

下端の帯でも動きはしたが、**何を相手にしているのかを言わなかった** ─ 20 行のうちどれの
話なのかはカーソルの枠だけが答えで、「分かりにくい」と報告された形がこれ。位置がそのまま
答えになれば、説明する語が要らなくなる。見出し（フォルダを作るのかファイルを作るのか）は
幅を取り合わないステータス行へ移した。

リネームは**語幹だけを選択して開く**（`archive.tar.gz` なら `archive.tar` まで。フォルダは
名前ぜんぶ）。外を押したら畳んで打った名前は捨てる ─ エクスプローラーはここで確定するが、
その同じクリックがカーソルを動かすので、**押し間違いで違うファイルの名前が変わる。**
設計上の判断は [CLAUDE.md](../CLAUDE.md) の「対象の上で編集する」。

### P3-4. 型入力ジャンプ（type-ahead）— ✅ 完了

文字キーを押すと、その名前で始まる行へカーソルが飛ぶ。`Ctrl+F` の絞り込みとは別物で、
**行は 1 つも消えない** ─ 打った文字は 1.2 秒で消え、ステータス行に出る。同じ文字を
続けて打つと次の候補へ回り、打ち足せば絞り込まれる。

`Space` の衝突は「打っている最中かどうか」で捌いた ─ 打ちかけの文字列が無ければ
今までどおり選択のトグル、ある間は名前の一部として受け取る。`Backspace` と `Escape` も
同じ読み方で、打っている間だけ打ちかけの文字列のものになる。

**既定バインドは文字キー単独と `Shift+<文字>` を空けたまま残してある** ─ 修飾は必ず
`Ctrl` か `Alt` が付く。ここを埋めると、大文字小文字を畳んで飛ぶこの機能と
「`b` は項目へ、`B` は別の何かへ」という形で必ず食い違う。

判断はすべて `core/input/TypeAhead`（OS にも描画にも触れない）にあり、
`tests/test_typeahead.cpp` が端から端まで検証する。**時計は自分で読まない** ─
現在時刻は `App` が渡す。設計上の判断は [CLAUDE.md](../CLAUDE.md) の「型入力ジャンプ」に。

### P3-5. クイックアクセスのピン留め編集 — **S**

現在は既知フォルダの固定リスト。ブックマークと統合してもよい。

### P3-6. アドレスバーと、パス入力の補完 — ✅ 完了

アドレスバーは**タブバー直下のパンくずと同じ 1 行**。`Ctrl+L`（`Cmd::EditPath`）か、
パンくずの後ろの余白をクリックすると、その行がそのまま入力欄になる（開いた瞬間は
全選択、入力欄の外を押せばパンくずに戻る）。

打てば打った先のフォルダが候補に出る。1 文字ごとに絞られ、`Tab` / `↓` で採り、
`Shift+Tab` / `↑` で戻る。候補一覧はその行からフルパスで垂れ下がり、一覧に重ねて
描く。候補行のクリックはそのまま移動になる。`Escape` は先に候補を畳み、もう一度で
パンくずに戻る。

副産物として `Prompt` が選択範囲を持つようになったので、他の入力欄でも `Ctrl+A`・
`Shift+←→`・`Ctrl+←→`（パスの構成要素 1 つぶん）が同じように使える。

判断はすべて `core/input/PathComplete`（OS にも描画にも触れない）にあり、
`tests/test_pathcomplete.cpp` が端から端まで検証する。**列挙は自分でしない** ─
`App` が `DirectoryLoader` を通して取り、`SetListing()` で渡す。設計上の判断は
[CLAUDE.md](../CLAUDE.md) の「アドレスバー」「アドレスバーの補完」「入力欄の選択」に。

### P3-7. キー割当の GUI 設定 — ✅ 完了

`Ctrl+Shift+,`（`Cmd::ShowKeySettings`）で設定画面を開く。全コマンドを一覧し、
`Enter` で変更、`Insert` で追加、`Delete` で解除、`Ctrl+R` で既定に戻す。
文字を打てばラベル・`keys.ini` の名前・現在の和音を横断して絞り込む。和音を他の
コマンドから奪った場合は、どのコマンドから奪ったかを画面に出す（黙って奪うと、
利用者は壊れたショートカットを探す羽目になる）。

判断はすべて `core/input/KeyEditor`（OS にも描画にも触れない）にあり、
`tests/test_keyeditor.cpp` が端から端まで検証する。UI 層は行を描いて入力を
渡すだけ。変更のたびに `keys.ini` を書き直すので、保存操作は無い。

### P3-8. 設定の GUI — ✅ 完了

`Ctrl+,`（`Cmd::ShowSettings`）で設定画面を開く。テーマ・言語・文字サイズ・サイドバー・
シェルアイコン・**タブバーの位置**・**新しいタブの位置**・新しいタブの既定
（隠しファイル／フォルダ先頭）を
`←→` か `Enter`、あるいはマウスで切り替える。変更はその場で効き、`settings.ini` に
即書き出す（保存操作は無い）。

メニューバーは足していない（「あえてやらないこと」）。`Ctrl+Shift+,` のショートカット設定と
同じオーバーレイ・同じ行の作法にしてあり、判断は `core/app/SettingsEditor`（OS にも
描画にも触れない）が持つ。設計上の判断は [CLAUDE.md](../CLAUDE.md) の「設定画面」に。

### P3-9. セッションバーとタブバーの折り返し — ✅ 完了

どちらのバーも、収まらなくなった分を右端で切り捨てるのをやめ、行を増やすようにした。
切り捨てていた頃は、画面外のセッションとタブがクリックできず、省略された印も出なかった。
タブは下限の 70 px まで詰めてから折り返すので、1 行に収まっている限り見た目は変わらない。
併せて、9 個目以降のセッションチップが `Cmd::Session1 + 添字` でコマンド表の外を
指していた不具合も直した。詳細は [CLAUDE.md](../CLAUDE.md) の
「セッションバーとタブバーの折り返し」。

### P3-10. 縦置きのタブバー — ✅ 完了

`[ui] tab_bar_position = left`（設定画面「タブバーの位置」）で、タブバーをペインの左に
縦に立てられる。既定は今までどおり `top`。折り返しとスクロールの数え方は横置きと共通で、
描く向きだけが入れ替わる ─ ただし**縦置きは列を増やさずスクロールする**（2 列目を作ると
その幅がまるごと一覧から消える）。**バーの上のホイールでスクロールでき**、切り替えれば
アクティブなタブのところまで戻る。隠れているタブがあるときは列の右端に**つまみ**が出る
（掴めるようにするのは P3-11）。設計上の判断は [CLAUDE.md](../CLAUDE.md) の
「縦置きのタブバー」。

### P3-11. スクロールバーのドラッグと、長い一覧での選択の枠 — **S**

現在のスクロールバーは表示だけで掴めない ─ 一覧の脇のものも、縦置きタブバーの
つまみ（P3-10）も同じ。**掴めるようにするなら 2 本まとめて**で、隣り合った帯の片方だけが
動くのは学びにくい。掴めるようにすれば、その帯を
「選択の枠を引ける余白」から外している理由（`AppUi::BeginMarquee`）も消える。
そのうえで枠を一覧の端から引けるようにすると、全行が収まらない一覧でも枠が使える。
そこまで行くと**枠の自動スクロールが初めて意味を持つ** ─ ポインタが枠外で
止まったままではマウスイベントが来ないので、`WinWindow` 側に一定間隔のタイマーを
足して `AppUi` を叩く形になる（`kStatusTimerId` と同じ作り）。

### P3-12. 行き先の検索 — ✅ 完了

`Alt+B` だった「ブックマークの一覧」を **`Ctrl+P` の行き先の一覧**に育てた。ブックマーク・開いている
タブ・クイックアクセス・ドライブ・履歴を 1 つの入力欄から絞り込んで飛ぶ、移動のための窓口。

**なぜコマンドパレットに混ぜないのか。** 「1 つの万能な窓口があれば便利」は正しい要求だが、
混ぜると**頻度の高い側が代金を払う**。ファイラーの主動詞は移動で、行き先は実行中に増減する
データ（ブックマーク数十件、タブ数枚）。対してコマンドは 125 行の固定表で、覚えた人は和音を
使う ─ パレットは「割り当てを覚えていない・奪われた」ときの窓口として稀に使われる側である。
両者を 1 つの一覧にすると:

- **絞り込みが薄まる。** `d` と打った瞬間に `file.delete`・`tab.duplicate`・`Downloads` が
  並ぶ。探しているのはたいてい後者なのに、行数は前者のほうが桁違いに多い。
- **順位付けが必須になる。** 毎日使うブックマークが一度も使わないコマンドより下に出るのは
  許容できないので、使用頻度と最近使った順を覚える場所が要る。これは
  **「探している行が打鍵ごとに動くと、目で追うより打ち直すほうが速くなる」**（P3-1）に
  正面から反する。

そこで「窓口を 1 つに」ではなく**頻度の高い側に良い和音と行の種類を渡す** ─ VS Code が
`Ctrl+P`（場所）と `Ctrl+Shift+P`（コマンド）を分け、短い和音を前者に与えているのと同じ
判断。パレットからは `nav.places` の行 → `Enter` で開くので、道は 1 打鍵ぶん長いだけで
通じている。

段取り:

1. **行の種類を増やす** — ✅ 開いているタブ（`PlacePicker::OpenTab`）、✅ ドライブ
   （`fs::Root`）、✅ クイックアクセス、✅ 履歴（`App::CollectHistory`）。器は `PickerList` と
   `AppUi::PaintPickerFrame` にあるので、増えたのは行の組み立てと «選ばれたときに何をするか»
   だけ。5 種類目を足した段で、種類ごとの引数を並べるのをやめて `PlacePicker::Sources` 1 つに
   まとめた ─ **クイックアクセスとドライブは同じ型**（`fs::Root`）なので、隣り合った引数を
   取り違えてもコンパイラは何も言わない。
2. **和音を格上げする** — ✅ `Alt+B` → **`Ctrl+P`**。移動のほうが頻繁なのだから、
   コマンドパレット（`Ctrl+Shift+P`）より打ちやすい席をこちらに渡した。**既存環境では
   `Alt+B` が残る**（`keys.ini` にその行が書かれているため。`Ctrl+N` と同じ罠で、案内する
   ときはそれを踏まえる）。同じ段で改名も済ませた: `BookmarkPicker` →
   `PlacePicker`、`Cmd::ShowBookmarks` → `Cmd::ShowPlaces`、`Hit::BookmarkRow` →
   `Hit::PlaceRow`、`cmd.show_bookmarks` → `cmd.show_places`（表示は「行き先の一覧」）。
   3 段目で `keys.ini` の名前も `bookmark.list` → **`nav.places`** に揃え、分類も
   ブックマークから移動へ移した ─ 画面が並べるものがブックマークだけではなくなった以上、
   パレットに «bookmark.list / ブックマーク» と出るのは嘘になっていた。旧名は
   `CommandFromName()` が別名として読み続けるので、既存の `keys.ini` の行はそのまま生きる。
3. **コマンドパレットはコマンドのまま** — ✅ `Ctrl+Shift+P` は据え置き。
4. **窓口を 1 つにする** — ✅ **モード方式**（下記の代替案）を採った。`Ctrl+P` の欄で
   `>` を打てばコマンド、消せば行き先。印は入力欄の中に文字として在るので、«今どちらか»
   を言うのも戻る道もそれ 1 つで足りる（`App::SyncPickerMode`、
   `CommandPalette::kPrefix`）。順位付けは持ち込んでいない。

**採ったのはモード方式（✅ 実装済み）。** 混合ではなく**モード**にする ─ 既定は行き先の
検索、入力欄で `>` を打つとコマンドに切り替わる（VS Code の `Ctrl+P` → `>`）。和音 1 つ・
窓 1 つで、薄まりも順位付けも起きない。満たしている条件は 2 つで、
**(1) 今どちらのモードかを入力欄が明示する**（印そのものが入力欄の中の文字）、
**(2) 順位付けを持ち込まない**。混合案は退けた ─ `d` の 1 文字に `file.delete` と
`Downloads` が並べば、行数の桁が違う分だけ頻繁な側が代金を払う。

並べる規則:

- **ブックマークが先、開いているタブが後。** タブは追加であって置き換えではないので、
  ブックマークを探しに来た手が変わらない並びにしてある。
- **今いるタブは並べない**（`App::CollectOpenTabs`）。「ここへ行く」は「どこへ行く」の
  答えではないし、最初の選択がその行に乗ると `Enter` が何もしない画面になる。
  **他のペインのアクティブなタブは残す** ─ 分割中はそれも立派な行き先。
- **タブの行を選んだらそのタブへ移る**（フォーカスもそのペインへ）。同じフォルダを
  もう 1 枚開くのでは、開いてあるものを選んだ意味が無い。
- **タブの行では `Ctrl+Enter` を飲み込む** ─ すでに開いているタブに «新しいタブで開く» は
  無い（P3-1 の「無い意味を作らない」と同じ）。
- **種別を行に出す**（「ブックマーク」/「開いているタブ」/「クイックアクセス」/「ドライブ」/
  「履歴」）。同じフォルダが 2 行に出ること
  が普通にあり、しかも `Enter` の意味が行によって違うので、押す前に見えていなければ
  ならない。絞り込みにも当たるので「タブ」と打てば開いているものだけが並ぶ。
- **背面のセッションのタブは入れない。** あちらへ移るのは画面に出ていないペインごと
  入れ替わる別の操作（セッションの切り替え）で、行き先を 1 つ選ぶのとは重さが違う。
- **クイックアクセスとドライブは下。** どちらもいつも同じ顔ぶれで並んでいる «背景» なので、
  探しに来る頻度がいちばん低い ─ 上に出すと、実際に開いているタブが画面の下へ押しやられる。
  クイックアクセスがドライブより上なのは、あちらが実際に作業するフォルダで、ドライブは
  フォルダの置き場所だから。**ブックマークと同じフォルダが 2 行に並ぶのは畳まない** ─
  畳むにはどちらの種別を残すか選ぶことになるが、«利用者が留めたもの» と «Windows が
  勧めるもの» は同じ答えではない。
- **履歴はいちばん最後で、1 つの塊に固める**（`App::CollectHistory`）。他の行は前回開いた
  ときと同じ場所に在り、それが «目で覚えられる一覧» を成り立たせている。履歴は順序が
  変わることそのものが定義なので、**その約束を持てない唯一の種類** ─ 末尾に置けば、
  破るのは自分についてだけで済む。
- **履歴はフォーカス中のタブの「戻る」だけ。** 全タブぶんを混ぜるには «どちらが新しいか»
  を答えられなければならないが、履歴が持っているのはタブごとの順序で時刻ではない ─
  2 本の列をつなぐことはできても、交互に並べる根拠がどこにも無い。「進む」履歴も入れない
  （あちらは «戻るで離れた先» で、`Alt+→` がすでに指している）。**同じフォルダは畳み**、
  **今いるフォルダは落とし**、**件数には上限がある**（`kHistoryRows`）─ 溢れたぶんは
  `Alt+←` で届く。
- **絞り込み欄はキャレットの動く入力欄**（`TextField`）。`←→`・`Home`/`End`・`Shift`
  での選択・`Ctrl+A`・クリップボードの 3 つが、Kite の他の入力欄とまったく同じに動く。
  数え方は 1 つしかない（`core/input/TextField.h`）。

### P3-13. タブごとの表示設定の永続化 — **S**

**今は保存していない。** `sessions.ini` に残るのはセッション名・分割レイアウト・タブの
パスだけで、読み込み時は全タブの `ViewState` が既定値（`App::defaultView_`）で塗り直される
（`App::LoadWorkspace`）。名前順に並べ替えて隠しファイルを出したタブは、次の起動で元に戻る。

エクスプローラーは**フォルダごと**に覚えるが、Kite が覚えるべきなのは**タブごと** ─
同じフォルダを 2 つのペインで別の並びで見るのは分割ファイラーの日常で、フォルダに紐付けると
その 2 枚が取り合う。`Session::Serialize()` にソート・降順・フォルダ先頭・隠しファイルの
4 つを足すのが素直（レイアウト文字列の書式を変えるので、**古い `sessions.ini` を読めなく
しないこと**）。

### P3-14. フォルダのサイズを数える — **M**

一覧のサイズ列はフォルダに対して何も言わない（`FindFirstFile` が答えないため）。
「どれが容量を食っているのか」はファイラーに来る問いの中でも上位で、今の Kite はそれに
**答える手段を 1 つも持っていない** ─ ステータス行の使用量は「このドライブ全体」の話。

再帰の列挙はすでにワーカーで回せる（`DirectoryLoader` と同じ形）。要るのは (1) 選んだ
フォルダに対して数え始めるコマンド、(2) 途中経過を出す場所（サイズ列を数えながら書き換え、
終わったら確定）、(3) **やめる道** ─ ネットワーク共有の再帰は分単位になりうる。
P2-3 の検索と歩き方が同じなので、**再帰の列挙を 2 つ書かないこと**（先に入るほうが器を作る）。

---

## 6. フェーズ 4 — 性能とメモリ

### P4-1. メモリ削減（約 66 MB → 40 MB 台）— **M**

内訳の実測がまだ。まず何が食っているかを確定させる。想定される手当:
D3D11 デバイスの遅延生成と非アクティブ時の解放、`fs::Entry` の SoA 化
（現在は 1 件あたり `std::string` を持つ）、DirectWrite のフォントキャッシュ調整。
**先に計測すること。** 推測で削らない。

### P4-2. 巨大ディレクトリ対応 — **M**

10 万件クラスでの列挙・ソート・描画。列挙結果のストリーミング表示
（全件揃う前に描き始める）と、ソートの並列化。

### P4-3. 起動時間の実測と短縮 — **S**

現状の計測は `Start-Process` 経由でプロセス生成込みのため誤差が大きい。
アプリ内に計測点を仕込んで内訳（DLL ロード / D3D 初期化 / 設定読み込み / 初回列挙）を出す。

---

## 7. フェーズ 5 — クロスプラットフォーム（要件 16 の回収）

分離は済んでいるので、あとは実装するだけ。ただし規模は大きい。

| 項目 | 規模 | 備考 |
| --- | --- | --- |
| P5-1. パスセパレータの platform trait 化 | **S** | 現在 `path::kSep` が `'\\'` 固定。他はすでに両対応 |
| P5-2. Linux バックエンド | **XL** | 描画（Skia か Cairo）、FS（`readdir`）、監視（inotify）、シェル（xdg-open / GTK メニュー） |
| P5-3. macOS バックエンド | **XL** | CoreGraphics、FSEvents、NSWorkspace |

現実的には、まず Windows 版を常用水準まで仕上げてからで良い。
分離を維持できているかは `kite_tests` が守り続ける。

---

## 8. あえてやらないこと

判断を蒸し返さないための記録。

- **リボン UI / コマンドバー。** キーボード駆動を主軸に据えたので不要。
- **ファイル操作の自前実装。** コピー・移動・削除はシェル（`SHFileOperation`）に
  委ねる。進捗表示・競合解決・ごみ箱連携を自前で書き直すのは、ファイラーが
  データを失う最も一般的な原因。**呼ぶ場所だけは UI スレッドから外した**
  （`FileOpQueue`）─ あれは完了するまで戻らないので、呼んでいる間ウィンドウが
  メッセージを 1 つも処理できない。
- **ファイル操作の中断。** シェルに始めさせた操作を外から止める手段が無い。
  進捗ダイアログの「キャンセル」はシェル自身のものなので、そちらは今までどおり効く。
- **シェルアイコンの同期取得。** `SHGetFileInfo` は 1 行ごとにクラウドプロバイダを
  呼ぶ。要件 3・13 と正面から衝突する。アイコン自体は取るようになったが、
  UI スレッドからは決して呼ばない ─ 見えている行の分だけをまとめて
  `kite_shellhost.exe` に投げ、時間制限付きで待つ（P2-10）。
- **プラグインシステム。** 当面。サードパーティコードを自プロセスに入れるのは、
  P1-1 / P2-10 で追い出した方針と正面から逆行する。
- **サードパーティライブラリの導入。** 起動時間・配布サイズ・ビルドの単純さを
  優先する。テストハーネスを自作しているのもこの方針の一貫。

---

## 9. 次にやるべきこと

「エクスプローラーを無効化して常用できる」までの最短経路。フェーズ 1 と、フェーズ 2 の
仮想フォルダ・書庫・アイコン・ネットワーク・既定のファイルマネージャーとしての登録は入った。

1. **P2-3 検索** — 残っている最大の穴。今の Kite には「名前は覚えているが場所を忘れた」への
   答えが 1 つも無く、そのたびにエクスプローラーへ戻ることになる。
2. **P3-13 タブごとの表示設定の永続化** — **S** で、毎回の起動で確実に気付く不便。
3. **P2-4 サムネイル → P2-5 表示形式の切替** — 画像フォルダはこの 2 つが無いと使えない。
   経路（`IIconProvider` とホスト）は P2-10 で通っているので、順に足す。
