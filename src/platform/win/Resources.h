/// @file
/// @brief Win32 リソースの識別子。
///
/// リソーススクリプト（`Version.rc.in` から生成される `kite.rc`）と、それを
/// `LoadIcon` で引く C++ 側の両方が読む。番号を 2 か所に書くと、片方だけ直した日に
/// 「アイコンが出ない」という形で静かに壊れる。
///
/// @note ここに置けるのはリソースコンパイラが解釈できる記述だけ。C++ の宣言を
///       足してはならない。

#pragma once

/// @brief アプリケーションアイコン。`assets/kite.ico` が入る。
///
/// 持っているのは `kite.exe` のみ。`kite_shellhost.exe` は隠しウィンドウしか出さず、
/// 画面に出るのはメニューだけなので、アイコンの分だけ大きくする理由が無い。
#define IDI_APPICON 101
