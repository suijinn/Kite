# CLAUDE.md

Kite — Windows エクスプローラーを置き換えることを目的とした、C++20 製の軽量ファイラー。
このファイルは、このリポジトリを編集する人（人間・エージェントを問わず）向けの作業指針。

設計の全体像（層・プロセス構成・データモデル・移植の境界）は
[docs/architecture.md](docs/architecture.md)、ロードマップと未実装項目は
[docs/ROADMAP.md](docs/ROADMAP.md) を参照。**このファイルが書くのは「なぜそう決めたか」と
「何をしてはならないか」**で、構造そのものの説明は architecture.md にある。

## ビルドとテスト

**x64 Native Tools Command Prompt for VS 2022**（または `-Arch amd64 -HostArch amd64`
付きで起動した Developer PowerShell）から実行する。CMake・Ninja・ctest はすべて
Visual Studio に同梱されているので、追加インストールは不要。

**素の「Developer PowerShell for VS 2022」は x86 を向く。** ショートカットが
`Launch-VsDevShell.ps1` を引数なしで呼ぶためで、そのシェルからビルドすると `LIB` だけが
32 ビットを指した状態になり、コンパイルは通って**リンクだけが
`error LNK2001: 外部シンボル _purecall は未解決です` で落ちる**（実際に踏んだ ─ 下記）。
`cmake --preset` は毎回 configure を通るので、CMakeLists がこのシェルを検出して
その場で止める。

```bash
cmake --preset release
cmake --build --preset release
ctest --preset release
```

生成物は `build/release/` に `kite.exe`、`kite_shellhost.exe`、`kite_tests.exe`。
同じ 3 コマンドで使える `debug` プリセットもある。

`kite.exe` は `kite_shellhost.exe` を**自分と同じフォルダから**起動する。片方だけを
別の場所へコピーするとコンテキストメニューが出なくなる（「シェルメニューを表示
できませんでした」と出る）。

開発者プロンプトでない普通の PowerShell からは、MSVC 環境を自前で読み込む
`build.ps1` を使う（こちらは常に `vcvars64.bat` を読むので、上の x86 の罠が無い）。
開発者プロンプト以外で `cmake --build build/release` を直接叩くと
「`<mutex>` を開けません」という紛らわしいエラーになるが、これはコードの問題ではなく
`INCLUDE` が未設定なだけ。

1 スイートだけ回す高速ループ（`--list` で全テスト名を列挙できる）:

```bash
build/release/kite_tests.exe --filter app.
```

### 版とアイコン

ビルドのたびに `cmake/GitVersion.cmake` が 3 つを書き出す:

| 生成物 | 雛形 | 読む側 |
| --- | --- | --- |
| `generated/core/base/Version.h` | `src/core/base/Version.h.in` | 画面（タイトルバー・セッションバー右端・`F1` の見出し） |
| `generated/win/kite.rc` | `src/platform/win/Version.rc.in` | エクスプローラーのプロパティ、タスクマネージャー |
| `generated/win/kite_shellhost.rc` | 同上 | 同上 |

中身は `project(Kite VERSION …)` の版番号と `git rev-parse --short` の結果で、
未コミットの変更があれば SHA の末尾に `+` が付く。内容が同じなら書き直さないので、
毎回の全体再コンパイルにはならない。git が無い環境ではコミットが `unknown` になる
だけでビルドは通る。

**3 つを 1 回の実行でまとめて書いているのは git を何度も起動しないため**（ビルドの
たびに走るので、数十ミリ秒が毎回の待ち時間になる）。**版の出どころを増やさないこと**
─ 別々に組み立てると、プロパティ画面と画面上の表示が食い違う日が必ず来る。

`.rc` が違うのは名前と説明、そしてアイコンを持つかどうかだけ。説明を
`Kite File Manager` と `Kite shell extension host` で分けているのは、応答しなくなった
ホストをタスクマネージャーで見つける唯一の手掛かりがこの行だから。

アイコンの正は `assets/make_icon.ps1`。`assets/kite.ico` はその生成物なので、
**デザインを変えるときは `.ico` を直接編集せず、スクリプトを直して再実行する。**

```powershell
pwsh -File assets\make_icon.ps1 -PreviewPath preview.png
```

青紫グラデーションの角丸正方形に、白い一筆の「欠けた菱形」。**姉妹プロジェクト
Blinker（画像ビューア）の「欠けたリング」と同じ語彙**で、余白・角丸・線の作り
（白一色、丸い端、閉じない一筆）は同じ値、違うのは形と色 ─ 円か菱形か、水色か青紫か。
同じ作者のものだと分かり、かつ取り違えない距離を狙っている。

- **色まで Blinker に寄せない。** 形だけ変えて水色のままにした版では、16px で輪郭が
  潰れて色しか残らず、タスクバーで見分けが付かなかった。色相を離し、明度も一段
  落としてある。
- **絵を描かない。** 凧の骨や尾を入れていたことがあるが、16px では細部が必ず潰れる。
  潰れて「何かの塊」になるくらいなら輪郭だけのほうが強い。線幅も同じ理由で上げ
  すぎない ─ 太らせると 16px で中の穴が埋まり、ただの菱形になる。
- **確かめるのは大きいほうではなく 16〜24px。**（`-PreviewPath` は 256px の確認用で、
  `.ico` の中身は 16〜256 の 8 枚。）

`.ps1` は UTF-8 BOM 付きで保存する（`ci.yml` が全 `.ps1` を検査している）。
なお **PowerShell のカンマは算術より強く結合する** ─ `@($a - $b, $c)` は
`$a - ($b, $c)` と解釈されて「配列は引けない」で落ちる。座標は 1 つずつ変数に持つこと。

## アーキテクチャ

3 層に厳密分離。これがこのプロジェクト最大の設計制約であり、将来他 OS へ移せる根拠。

```
src/core/       OS 呼び出しを一切行わない。例外は core/base/Platform.h が宣言する
                5 つの自由関数のみ。ここに <windows.h> を include してはならない。
src/ui/         レイアウト・描画・ヒットテスト・ドラッグ処理。描画は抽象 ui::Renderer
                経由のみ。
src/platform/   Windows ヘッダが現れる唯一の場所。
tools/shellhost/ kite_shellhost.exe。サードパーティのシェル拡張が動く唯一のプロセス。
tests/          kite_core だけをリンクする。OS 非依存であるべき core/ ui/ に Windows
                ヘッダが紛れ込めばテストのビルドが壊れる。これが分離の防波堤。
```

CMake ターゲットは 3 つ。`kite_core` = `core/` + `ui/`、`kite` = `kite_core` +
`platform/`（`ShellMenu.cpp` と `ShellIcons.cpp` と `ShellFolder.cpp` を除く）、
`kite_shellhost` = `tools/shellhost/` + その 3 ファイル + `ShellPipe.cpp` + `WinUtf.cpp`。

**その 3 ファイルを `kite` ターゲットに足してはならない。** `ShellMenu.cpp` は
`IContextMenu`、`ShellIcons.cpp` は `SHGFI_ADDOVERLAYS`（＝アイコンオーバーレイ
ハンドラ）、`ShellFolder.cpp` は `IShellFolder` の列挙（＝名前空間拡張）を触る唯一の
ファイル。`kite.exe` にリンクした時点で隔離の意味が消える。

他 OS へ移植する際に実装するのは以下だけで、それ以外は書き換えない:

| 境界 | ヘッダ |
| --- | --- |
| 描画 | `ui/Renderer.h` |
| ファイルシステム | `core/fs/FileSystem.h` |
| 変更通知 | `core/fs/DirectoryWatcher.h` |
| シェル／クリップボード | `core/app/Host.h` の `IShellIntegration` |
| アイコン | `core/app/IconProvider.h` の `IIconProvider` |
| ウィンドウ機能 | `core/app/Host.h` の `IHost` |
| ファイル入出力・時刻・ロケール | `core/base/Platform.h` |

### データモデル

```
Workspace -> Session[]（1 つがアクティブ）
Session   -> SplitNode ツリー。葉が 1 つの Pane を持つ
Pane      -> Tab[]（1 つがアクティブ）
Tab       -> パス + 一覧 + 表示状態 + 履歴
```

全セッションが常駐しているので、切り替えはインデックスの移動だけで済む。
背面に回ったセッションの非アクティブタブは一覧データを解放し、常駐メモリを
画面に映っている量に比例させる。

**`Tab::visible` は `listing.entries` への添字だけではない。** 先頭に `..` を表す
`Tab::kParentRow`（= -1）が入りうる ─ 親フォルダは列挙結果に含まれないので、実体を
持たない行として紛れ込ませてある。おかげで「表示行 = `visible` への添字」が保たれ、
カーソルもスクロールも行番号のままで書ける代わりに、**`listing.entries[visible[i]]`
と直接書くと `..` の行で範囲外に落ちる。** `Tab::EntryAt()` か `IsParentRow()` を
通すこと。件数を数えるときは `visible.size()` ではなく `Tab::ItemCount()`。

**選択はカーソル移動で消えない。** `Space` が印を付けて 1 行進む方式なので、移動で
消していては隣り合った項目しか選べない。範囲選択（`Shift+↑↓`）は `Tab::ExtendTo()` が
伸縮を始める前の印を土台に毎回引き直すため、縮めた分は解放されつつ、`Space` で付けた
離れた印は巻き添えにならない。選択を捨てるのは `Escape`・`SelectNone`・行のクリック、
そして一覧の余白を押したとき。

マウスからは 3 通り ─ `Ctrl+クリック`で 1 件ずつ、`Shift+クリック`で範囲
（どちらも `AppUi::HandleListClick`）、そして余白から引く**選択の枠**（下の「制御
フロー」）。枠も毎フレーム「引き始めたときの印」から引き直す ─ `Tab::ExtendTo()` が
`Shift+↑↓` に対してしているのと同じことを、行ではなく座標でやっている。

**すでに印の付いている行を押したときは、印を捨てるのを離すまで待つ**
（`AppUi::pendingUnmark_`）。押した瞬間に捨てていたので、**複数選択したまま
ドラッグすると 1 件しか運ばれなかった** ─ 掴んだ時点で選択が 1 行に落ちており、
`Tab::SelectionPaths()` はその 1 行を答えていた（利用者から「1 ファイルしか
移動できない」と報告された形がこれ）。とはいえ**押したのがただのクリックなら
「その行だけ」の意味は残る**ので、離した時点でまだ `Drag::PendingFile` なら
そこで捨てる。エクスプローラーも含めどの一覧も同じ順序で、**押しただけでは
選択を壊さない**のが要点。

- **印の無い行では今までどおり押した瞬間に捨てる。** 待つ理由が無く、待てば
  枠やドラッグの間ずっと古い選択が残る。
- **右ボタンは待たない** ─ 押した時点でメニューが開き、その相手は選択そのもの。
  印の無い行なら 1 件だけを選び直すのも今までどおり。
- **本当にドラッグが始まったら忘れる**（`AppUi::CancelDrag` が印を落とす）。
  ドラッグの後に選択が消えては、運んだ先で何を落としたのか分からなくなる。

### 制御フロー

`App`（`core/app/App.cpp`）が唯一のディスパッチ点。テキスト入力欄を除き、UI は生の
キー入力に一切反応しない。キーマップが和音を `Cmd` に変換し、`App::Execute`
（`core/app/AppCommands.cpp`）が実行する。「どの操作にもキーを割り当てられる」が機能では
なく構造として保証されるのはこのため。**`App::Execute` はコマンド 1 つに `case` 1 つで、
表が伸び続けるので別ファイルに置いてある** ─ 設定とセッションの読み書きも同じ理由で
`core/app/AppConfig.cpp` に分けてあり、`App.cpp` に残っているのはそれ以外全部。

例外は 3 つだけで、いずれも「入力そのものを対象にする」画面。プロンプト行
（`App::HandlePromptKey`）と、`Ctrl+Shift+,` のショートカット設定画面
（`core/input/KeyEditor`）、`Ctrl+,` の設定画面（`core/app/SettingsEditor`）。後ろ 2 つは
表示中のキーをすべて飲み込む ─ 割り当てたい和音がその場で別のコマンドを実行しては
設定にならないし、値を選んでいる最中に `Ctrl+T` でタブが増えるのも同じこと。判断は
`KeyEditor` / `SettingsEditor` が持ち、UI は行を描いて入力を渡すだけ。どちらも変更の
たびにファイルを書き直すので保存操作は無い。

**キーマップを通らずに文字そのものを受けるものがもう 1 つある** ─ 一覧の型入力ジャンプ
（`App::TypeAheadChar`、下記）。これはキーマップの外に置かざるを得ない ─
文字キー 1 つ 1 つを `Cmd` にすると、割り当てられる和音がアルファベットのぶんだけ
消える。代わりに**キーマップが答えなかった打鍵だけ**を受け取る形にしてあるので、
どの文字キーにコマンドを割り当てても、割り当てたほうが勝つ。

ショートカット設定画面で **1 つのコマンドに和音を足すのは `Ctrl+Enter`**（`Enter` が
置き換えなので、`Ctrl` を足すと「今のを残して増やす」─ 一覧の `Ctrl+Enter` が
「新しいタブで開く」なのと同じ読み方）。**マウスからは行末の `+`** で、行そのものと
ダブルクリックは置き換えのほう。`Insert` も残してあるが、無いキーボードがあるので
画面の案内には出さない。

**1 つの和音だけを外す道が要る。** `Delete` は行のコマンドの割り当てを全部解除するが、
2 つ割り当ててあるうちの片方が邪魔なだけ、という場合の答えになっていない。`←→` が
**行の中の和音を 1 つ選び**（`KeyEditor::chordCursor()`）、選んである間の `Delete` は
それだけを外す。マウスは同じ読み方で、**1 回目のクリックで指し、2 回目で外す** ─ 行が
「選んでから実行」なのと揃えてある（通りすがりの 1 クリックで割り当てが消えては困る）。
**印は文字の下に敷く**（入力欄の選択と同じ理由）。和音ごとの幅は 1 つずつ測って出すが、
等幅フォントなので断片の合計は全体と一致する。**行を移れば選択は外れる** ─ 指す先が
無くなった以上、`Delete` は再びコマンド全体の話に戻る。

**一覧（`F1`）にも設定画面にも、割り当ては全部並べる**（`KeyMap::ChordText`）。代表の
1 つだけを出していた頃は、2 つ目を足しても画面が何も言わないので、足せたかどうかを
確かめる方法が無かった。`F1` の桁数は**実際に必要な幅を測ってから決める** ─
固定幅では `Ctrl+Shift+Tab, Ctrl+PageUp` が入らず、答えを半分に切った一覧になる。

**1 行に 2 つ以上の文字を置くときは、後のものに「前が使い残した幅」しか渡さない。**
`F1` の見出し行は表題・版・案内の 3 つが並ぶが、それぞれに行の右端までの矩形を渡して
いたので、窓を狭めると 3 つとも重なって全部読めなくなった（`AppUi::PaintKeyHelp` と
`PaintKeySettings`）。**入らないものは重ねずに落とす** ─ 落とす順は「来た目的から遠い
ほう」で、`F1` なら版の表示が最初に消える。`D2D1_DRAW_TEXT_OPTIONS_CLIP` が効くのは
自分の矩形に対してだけなので、**矩形が重なっていれば文字も重なる。**

**行間は詰めてよいが、文字の高さより下げない**（`AppUi::PaintKeyHelp`）。全部を 1 画面に
入れるために行間を縮めていたが、下限が無かったので小さい窓では数ピクセルまで潰れ、
一覧が読めない塊になった。詰める順は **行間 → 分類の間の空行 → それでも余ればホイールで
送る**（つまみを出す）。空行を先に捨てるのは、そこだけが読むためのものではないから。

**`keys.ini` は書かれたとおりに読む。** 名前の挙がったコマンドについては、ファイルの
内容がそのまま答えになる（`KeyMap::ApplyIni`）─ 既定はまとめて外してから、書かれた順で
入れ直す。触れていないコマンドは既定のまま。**「既定の上に足す」に戻さないこと。**
差分だけ書けるほうが親切に見えるが、Kite が書き出す `keys.ini` には全コマンドが並んで
いるので、利用者が触るのはたいていその行のほうで、足すだけにすると既定が下に残る。
当時の `F1` の一覧は **先に割り当てられたほう＝既定** しか出さなかったので、**編集が
画面のどこにも反映されなかった**（実際にそう報告された）。全部の和音を並べるように
なった今でも、外したはずの既定が隣に残るだけで、直したことにはならない。

同じ理由で、**割り当ての無いコマンドは `# tab.next = none` ではなく `tab.next = none` と
書く**（`KeyMap::ToIni`）。コメントにすると読み戻したとき「書かれていないコマンド」に
なり、既定が復活する ─ 画面で外した割り当てが次の起動で生き返る。

ディレクトリ列挙は UI スレッドで絶対に動かさない。冷えたネットワーク共有は 1 回の
`FindFirstFile` で数秒ブロックする。リクエストは `DirectoryLoader` を通り、結果は
`IHost::Wake()` の後に `App::PumpLoader` が回収する。

**ホバー表示は「どの行に乗っているか」を覚えない。** `AppUi` が持つのは最後に受け取った
ポインタ座標だけで、光らせるかどうかは描くたびに矩形との判定で決める（`AppUi::Hovered`）。
覚える作りにすると、ホイールで一覧が動いたときに壊れる ─ ポインタが止まったまま行の
ほうが入れ替わるのに、それを知らせるマウスイベントは来ない。マウス移動が再描画を
要求するのは**別の項目に入ったときだけ**（`hoverRect_` の比較）。1 ピクセルごとに
`Invalidate` すると、動かしている間ずっと全面再描画になる。

光らせないのは 4 つ:

- オーバーレイ（`F1`・`Ctrl+Shift+,`・`Ctrl+,`・`Ctrl+P`・`Ctrl+Shift+P`）が出ている間 ─ 後ろは
  押せないので押せそうに見せない。
- 分割線とタブのドラッグ中 ─ 通りすがった行が光ると掴んでいるものの行き先が読めない。
- 外からのファイルドラッグ中 ─ 答えはドロップ先の枠のほうで、隣にもう 1 つ光ると
  どちらが行き先か分からなくなる。
- 枠を引いている間（`Drag::Marquee`）─ 枠が通った行はすでに選択色で塗られている。

**押した瞬間は消さない**（`Drag::PendingFile` を除外しないこと）─ クリックのたびに
1 フレーム暗転する。

**選択の枠（`AppUi::BeginMarquee` / `UpdateMarquee`）は一覧の余白からしか引けない。**
行を押して引くのは「ファイルを外へドラッグする」動作で、こちらのほうが日常的に使う。
エクスプローラーを含めどのファイラも同じ割り切りで、その帰結として**枠が引けるのは
全行が収まっている一覧だけ**になる ─ 余白があることと、スクロールできることは両立
しない。だから枠に自動スクロールは無い。入れても到達できないコードにしかならない。
長い一覧を範囲選択する手段は `Shift+クリック`。同じ理由で、**スクロールバーが出て
いるときはその帯からも引かない** ─ 行の矩形は帯の手前で終わるので当たり判定の上では
「余白」になるが、つまみを掴もうとして枠が出るのは誰も望んでいない。

引き始めた点は画面座標ではなく**一覧の中の位置**（行 0 の上端からの画素数）で覚える。
ホバーが行番号を覚えないのと同じ理由で、ガラスの上の一点が何を指すかは一覧が動けば
変わる。行の判定も同じで、覚えた行番号ではなく毎回「枠と行の矩形が重なるか」で決める。
押したまま数ピクセル動いただけで最終行が選ばれる、といった形で差が出る。

### セッションバーとタブバーの折り返し

**どちらのバーも、収まらなくなったら行を増やす。**（`AppUi::LayoutSessionBar` /
`AppUi::LayoutTabBar`）以前は右端で描くのをやめていたが、それは**画面に出ていない
セッションやタブがクリックできない**ということで、しかも「まだ先がある」と言う印すら
出ない。バーは何が開いているかを言うためにあるので、6 個目から黙って列挙をやめるなら
無いほうがましだった。

- **タブは詰めてから折り返す。** 幅は今までどおり `avail / 枚数` を 70〜190 px に
  クランプするので、**1 行に収まっている限り見た目は一切変わらない。** 下限の 70 px に
  達したところで初めて 2 行目が生まれる。
- **`+` ボタンのぶんを引いた幅（`avail`）で 1 行の枚数を決める。** そうすると最終行が
  満杯でも必ず 28 px 余るので、`+` が行からあふれない。全幅で割ると「タブ 5 枚が
  ぴったり収まったので `+` だけ 2 行目」という間抜けな形になる。
- **上限を超えたら行をスクロールする** ─ タブバーはペインの半分、セッションバーは
  ウィンドウの 1/4 まで。バーが一覧を押し潰したら、一覧を読むためのアプリではなくなる。
  この状態になるのは数十枚を開いたときだけ。**セッションバーはどの行を出すかを
  アクティブなセッションから毎回引く**（補完候補の一覧と同じ理由 ─ 別に覚えた
  スクロール位置は、選択と食い違うことしかできない）。**タブバーだけは位置を覚える**
  ─ ホイールで動かせるようにした以上、覚えないという選択肢が無い（下記）。
- **版の表示（`Kite 0.1.x`）は右上に固定**し、その幅ぶんは**どの行も**手前で折り返す。
  最後のチップに続けて出すと、セッションを 1 つ足すたびに版表示が別の行へ移る ─
  「今動いているのはどの版か」を読み上げてもらうための場所なので、動いては困る。
  スクロールで 1 行目が画面外に出ることがあるため、1 行目だけ狭めるのでは足りない。
- **セッションチップは `Cmd::Session1 + 添字` で飛ばない**（`App::GotoSession`）。
  `Cmd::Session1..8` は 8 個しか無く、9 個目のチップが押せるようになった今、
  足し算で作ったコマンドはコマンド表の外（`Cmd::ToggleHidden` あたり）を指す。
- **セッションチップのダブルクリックで名前を変える**（`Cmd::RenameSession`）。名前の
  書かれたチップを 2 度押せば名前を編集できる、はどの UI でも同じ読み方で、しかも
  **和音だけを入口にしていると常駐ソフトに奪われた環境で道が完全に消える** ─ 既定の
  `Ctrl+Alt+R` は AutoHotkey のスクリプト再読み込みや画面録画ソフトの定番で、
  奪われた側からは「Kite のバグ」と見分けが付かない。名前を変える先は
  **今アクティブなセッション**なので（`App::SubmitPrompt` が `activeSession()` を引く）、
  1 度目のクリックがそのチップを選んでいる順序に頼っている ─ ここを
  「クリックされた添字」で書き直すなら両方を直すこと。
- **1 クリックで名前を編集しない。** セッションを選ぶだけのつもりで押した人の
  ポインタの下に入力欄が出る。逆に**切り替えを離すまで待つ理由も無い** ─ タブと同じで、
  押した瞬間に切り替えてから並べ替えの構えだけを取っておけばよい（サイドバーの項目が
  離すまで待つのは、あちらの「押す」がフォルダを開いてしまうから）。
- **チップはドラッグで並べ替えられる**（`AppUi::ResolveSessionDrop` →
  `App::MoveSession` → `Workspace::ReorderSession`）。タブの並べ替えと同じ文法で、
  落とし先はチップの左半分／右半分。**判定に入れるのはチップだけ**で、行の末尾の
  余白では何も提案しない ─ バーは折り返すので、そこは「この行の後ろ」であると同時に
  「次の行の前」でもあり、どちらとも決められない。挿入位置は端から端まで届く
  （先頭＝最初のチップの左半分、末尾＝最後のチップの右半分）ので、失うものは無い。
- **並べ替えても切り替わらない**（`Workspace::ReorderSession` が `active` を追随させる）。
  `ActivateSession()` は通さない ─ 離れるセッションが無いのだから、他のセッションが
  抱えている一覧を捨てる理由も無い。
- **キーボードは `Ctrl+Alt+Shift+PageUp/PageDown`**（`Cmd::MoveSessionLeft` /
  `MoveSessionRight`）。セッションの移動が `Ctrl+Alt+PageUp/PageDown` なので、
  タブが `Ctrl+PageUp` に `Shift` を足して並べ替えになるのと同じ読み方。
  **端で止まり、回り込まない** ─ `NextSession` の回り込みは「別のを見せてくれ」への
  答えだが、こちらはバーに沿った方向の指示で、先頭のチップが末尾へ飛ぶのは事故に
  しかならない（`Cmd::MoveTabLeft/Right` と同じ）。

### 最後のタブと `Ctrl+W`

**ペインに 1 枚しか残っていないときの `Cmd::CloseTab` は、分割していればそのペインを、
分割していなければウィンドウを閉じる。** `Pane::CloseTab` は最後の 1 枚を閉じないので
（ペインは常にタブを 1 枚以上保つ）、ここで答えなければキーが黙る ─ どのブラウザでも
`Ctrl+W` は最後のタブで窓を閉じるので、「効かないキー」に見える。

- **分割中にウィンドウを閉じない。** タブが尽きたのは複数あるペインのうち 1 つでしか
  ないのに、他のペインが見せていたものまで一緒に消える ─ 左右に並べて作業している
  最中の `Ctrl+W` がその形になるのは、押した人が頼んでいない。
- **セッションへは降りない。** セッションは `Ctrl+Alt+W`（`Cmd::CloseSession`）を持って
  おり、しかも**画面に出ていないペインを抱えている** ─ 見えている 1 ペインが空になった
  ことを合図にそれを閉じるのは、予測できない。ペインだけは `Alt+W`（`Cmd::ClosePane`）と
  重なるが、こちらは**タブが尽きたときだけ**の話で、しかも**閉じたタブは `Ctrl+Shift+T`
  で戻る**（`workspace_.closedTabs` に積む）。
- **閉じても失われない。** 終了時にワークスペースを書き出すので、次の起動で戻る
  （単独ウィンドウは書かない ─ 「2 枚目のウィンドウ」の規則そのまま）。
- **マウスの × と中クリックは変えていない**（`AppUi` から `Pane::CloseTab` を直接呼ぶ）。
  通りすがりの 1 クリックで窓やペインが消えるのは、キーを押したときとは別の話。

### タブを引き抜いて新しいウィンドウにする

**タブを掴んでウィンドウの外で離すと、そのフォルダが新しいウィンドウで開く**
（`AppUi::FinishTabDrag` → `App::DetachTabToNewWindow` → `IHost::OpenNewWindow`）。
どのブラウザも同じ読み方で、ペイン間の移動がすでに同じ文法を持っている。

- **渡るのはフォルダだけ。** 新しいウィンドウは別プロセスなので（「2 枚目のウィンドウ」）、
  `Tab` そのものは境界を越えられない ─ 履歴も表示状態も残らない。`Cmd::NewWindow` と
  同じ道を通すのはそのためで、**新しい窓の作り方を 2 通りにしない。**
- **判定は当たり判定ではなく描画面の外側**（`AppUi::OutsideWindow`）。「ここには何も無い」
  はセッションバーやステータス行の上でも起きるが、そこは外ではない ─ **バーの上で
  離したときは何もしない**（以前は直前の落とし先に入っていたのを、セッションチップと
  同じく「提案しない」に揃えた）。ボタンを押している間は OS がポインタを捕らえている
  ので、窓の外の座標も届き続ける。
- **ウィンドウにその 1 枚しか無いときは断る**（`ui.cannot_detach_last`）。開いた先が今の
  ウィンドウと同じものになるだけで、しかも元の窓は空にできない。**ペインが他にあれば
  引き抜ける** ─ 空になったペインは、タブをペイン間で移したときと同じく畳む。
- **開けなかったらタブは動かさない。** `OpenNewWindow` が失敗した後に閉じれば、その
  タブはどこにも無い。
- **`closedTabs` には積まない。** 閉じたのではなく移したので、`Ctrl+Shift+T` が同じ
  フォルダを戻すと 2 つのウィンドウに同じものが並ぶ。
- **他の Kite の窓へ落とす道は無い。** 窓ごとに別プロセスで、タブを受け取る経路を
  持っていない（単一インスタンスの転送はパスを新しいタブにするだけ）。外へ落とせば
  必ず新しい窓になる。

### 縦置きのタブバー

`[ui] tab_bar_position`（設定画面「タブバーの位置」）が `left` なら、タブバーは
ペインの**左に縦**に立つ。既定は今までどおり `top`。幅の広い画面では、横に寝かせた
バーは 1 行ぶんの高さのために横幅を丸ごと使うのに対し、縦なら余っている左右を使って
**タブ名を最後まで読める**（横置きの 70〜190 px は、深いパスのフォルダ名を必ず切る）。

**軸を入れ替えただけで、数え方は共通にしてある**（`AppUi::LayoutTabBar` /
`PaintTabBar`）。「行」は折り返しの単位で、横置きなら画面上の 1 行、縦置きなら 1 枚ぶんの
段 ─ `perRow`・`rows`・`firstRow`・`shownRows` の意味も、スクロール位置をアクティブな
タブから引く規則もそのまま。描くときに進む向き（`rowStep` / `slotStep`）だけが入れ替わる。
**片方だけの計算を書き足さないこと** ─ 折り返しとスクロールの規則が 2 つに分かれた
時点で、どちらかが必ず古くなる。

- **縦置きは列を増やさない。** 収まらなくなったらスクロールする。横置きが行を増やすのは
  高さを 1 行ぶん足すだけで済むからで、縦に 2 列目を作れば**その幅がまるごと一覧から
  消える** ─ 一覧を読むためのアプリではなくなる、という上限の話がそのまま当てはまる。
  タブは 1 行の文字ぶんの高さなので、横置きの「詰めてから折り返す」に当たる余地も無い。
- **幅はペインの半分まで**（`Theme::tabBarWidth`、既定 170 px）。横置きが高さをペインの
  半分で止めるのと同じ線。文字を入れる器なので**文字サイズの倍率が掛かる**
  （`Theme::Scale`）。
- **末尾の 1 行は `+` のために空ける。** 横置きが幅から 28 px 引いているのと同じ ─
  引かないと、最後のタブが下端に届いた瞬間に `+` の置き場所が無くなる。
- **バーはペインの高さいっぱいを取り、パスバーはその右**から始まる。パスバーの下に
  潜り込ませると、タブの列だけが一覧より低い位置から始まってペインの左上に用途の無い
  角ができる。
- **アクティブの印は「タブが並ぶ側の縁」に置く** ─ 横置きなら上辺、縦置きなら左辺。
  縦置きで上辺のままにすると、印が帯の外（一覧のほう）を指しているように見える。
- **前後を測る軸も入れ替わる。** ドラッグの落とし先（`AppUi::ResolveTabDrop`）と挿入の
  印は、縦置きでは中点の上下で決まる。x のまま測ると、列の右半分に入った瞬間に
  「次のタブの後ろ」になる。

#### タブバーのスクロール位置

**バーの上でホイールを回すとバーが動く**（横置き・縦置きとも）。**ただし隠れている行が
あるときだけ**で、全部が出ているバーは答えを持たないので、そのまま今までどおり一覧が
スクロールする ─ 1 行のバーの上でホイールが利かなくなるほうが、明らかに損。1 ノッチは
どちらの向きでも**タブ 3 枚ぶん**（縦置きの 1 行はタブ 1 枚、横置きの 1 行は 1 画面ぶん
なので、横置きは 1 行きざみ）。

位置は `Pane::tabScroll` が覚える。**「アクティブなタブから毎回引く」をやめたのは、
引いたままではホイールが何もしないから** ─ 動かした次のフレームに必ず元へ戻る。
代わりに **`Pane::tabScrollFor`（位置を合わせたときの `active`）と食い違ったときだけ
引き戻す**（`AppUi::LayoutTabBar`）。タブを切り替えれば新しいタブが見えるところまで
動き、切り替えていない間はホイールで見に行った場所に留まる。

- **ホイールは `tabScrollFor` も更新する。** ここを忘れると、選択が動くのを待っている
  引き戻しが次のフレームで発火して、回した瞬間に戻る。
- **`tabScroll` のクランプはレイアウト側の仕事。** タブを閉じれば行数は減るのに、
  位置は長かった頃の値のまま残る（サイドバーのスクロールを前フレームの内容高さで
  丸めているのと同じ話）。
- **ホイールの行き先は当たり判定で決める**（`AppUi::IsTabBarHit`）。バーの上にいるかを
  訊いているので、タブ・`+`・帯の余白のどれでも同じ答えになる。

**隠れているタブがあるときは、列の右端につまみを出す**（縦置きのみ）。無いと、5 枚の
列と 50 枚の列が同じ見た目になる ─ ホイールで動くのに、動かせることも、今どのあたりを
見ているのかも画面が言わない。

- **細いほう**（幅 3 px、キー設定画面の一覧と同じ作り ─ 描くのは
  `AppUi::PaintThinScrollbar` 1 つで、`F1` の一覧もチューザもここを通る）。一覧の脇の
  10 px のものと違い、これはタブと同じ帯を分け合う。**入る場所があるのは閉じるボタンが
  右端から 6 px 内側で終わっているから**で、印を太らせるならタブの中身のほうを先に
  動かすこと。
- **横置きには出さない。** 帯の右端はタブが最後まで使う場所なので、そこに 3 px を置けば
  最終列の閉じるボタンに重なる。避けるには 1 行の枚数を決める `avail` から引くことに
  なり、**引いた結果で行数が変わって「つまみが要るかどうか」の答えが変わる** ─ 折り返しの
  計算に自分自身への参照が入る。横置きが隠れた行を持つのは数十枚を開いたときだけなので、
  その循環を持ち込むほどの見返りが無い。
- **掴めない。** 一覧のスクロールバーと同じ扱い（ROADMAP P3-11 が両方まとめて掴める
  ようにする話）。ここだけ掴めると、隣り合った 2 本のうち片方だけが動く。

### 行き先の一覧

`Ctrl+P`（`Cmd::ShowPlaces`）で**ブックマーク・開いているタブ・クイックアクセス・
ドライブ・履歴**を 1 枚の
オーバーレイに並べる。**番号ショートカット（`Alt+Shift+1..8` / `Ctrl+1..8`）は 8 個で
打ち止めなので、その先へキーボードだけで届く道はこれしかない。** 判断は
`core/app/PlacePicker`（OS にも描画にも触れない）が持ち、`tests/test_placepicker.cpp` が
端から端まで検証する。

**ここが «行き先» の窓口で、コマンドパレットは «コマンド» の窓口**（ROADMAP P3-12）。
移動はファイラーの主動詞なので、行の種類が増えるのは
常にこちら ─ **パレットへ行き先を混ぜない**理由は下の「コマンドパレット」に。
**並べるものは `PlacePicker::Sources` 1 つで渡す。種類ごとの引数を並べない** ─
クイックアクセスとドライブは同じ型（`fs::Root`）なので、隣り合った引数を取り違えても
コンパイラは何も言わない。
名前も分類も画面に揃えてある ─ `keys.ini` 上は **`nav.places`**、分類は「移動」、
クラス名は `PlacePicker`、列挙子は `Cmd::ShowPlaces`。**旧名 `bookmark.list` は
`CommandFromName()` が別名として読み続ける**（`Commands.cpp` の `kAliases`）ので、
既存の設定ファイルの行はそのまま生きる ─ **書き出すのは常に現在の名前**で、両方を並べると
読み戻したとき 1 つのコマンドが 2 行を持つ。並べるものがブックマークだけではなくなった
以上、パレットに «bookmark.list / ブックマーク» と出るのは嘘だった。

`Ctrl+Shift+,` / `Ctrl+,` と同じ作法にしてある ─ 同じパネル、同じ行の刻み、表示中は全打鍵を
飲み込み、閉じるのは `Esc`。違うのは行が持つものだけで、和音でも値でもなく
**名前・パス・番号ショートカット**の 3 つ。**絞り込みとカーソルとスクロールは
`core/app/PickerList`、絞り込み欄そのものは `core/input/TextField`、パネルと描画は
`AppUi::PaintPickerFrame` を、どれもコマンドパレットと共有する**（下の「コマンドパレット」）。
器の側の規則 ─ 入力欄の大きさも、寸法を窓だけで決めることも ─ は 2 画面で 1 つしかない。

- **開いているタブも並べる。ただしブックマークが先、タブが後**（`PlacePicker::Open`）。
  タブは追加であって置き換えではないので、ブックマークを探しに来た手が変わらない並びに
  してある。**今いるタブは並べない**（`App::CollectOpenTabs`）─ 「ここへ行く」は「どこへ
  行く」の答えではないし、最初の選択がその行に乗ると `Enter` が何もしない画面になる。
  **他のペインのアクティブなタブは残す**（分割中はそれも立派な行き先）。**背面のセッションの
  タブは入れない** ─ あちらへ移るのは画面に出ていないペインごと入れ替わる別の操作。
- **タブの行を選んだらそのタブへ移る**（`App::ChoosePlace`。フォーカスもそのペインへ動く）。
  同じフォルダをもう 1 枚開くのでは、開いてあるものを選んだ意味が無い。だから
  **タブの行では `Ctrl+Enter` を飲み込む** ─ すでに開いているタブに «新しいタブで開く» は
  無い（パレットが `Ctrl+Enter` を飲み込むのと同じ話）。
- **クイックアクセスとドライブも並べる。ただしタブより下**（`App::OpenPlacePicker` が
  `quickAccess_` と `roots_` を渡す）。どちらもサイドバーと同じ
  並びで、**キーボードだけで «どのディスクがあるのか» を尋ねられる場所が他に無い** ─
  番号ショートカットはブックマークのものだし、ドライブ文字を打つのは「どれがあるか」を
  すでに知っている人の操作。**下なのは、いつも同じ顔ぶれで並んでいる «背景» だから** ─
  上に出すと、実際に開いているタブがネットワークドライブの列に押し下げられる。行の
  振る舞いはブックマークと同じ（`Enter` で現ペイン、`Ctrl+Enter` で新しいタブ）。
  **クイックアクセスがドライブより上**なのは、あちらが実際に作業するフォルダで、
  ドライブはフォルダの置き場所だから。
- **ブックマークと同じフォルダが 2 行に並ぶのは畳まない。** 畳むにはどちらの種別を残すか
  選ぶことになるが、«利用者が留めたもの» と «Windows が勧めるもの» は同じ答えではない ─
  そのための種別の列。
- **履歴はいちばん最後で、1 つの塊**（`App::CollectHistory`）。他の行は前回開いたときと
  同じ場所に在り、それが «目で覚えられる一覧» を成り立たせている。履歴は順序が変わること
  そのものが定義なので、**その約束を持てない唯一の種類** ─ 末尾に置けば、破るのは自分に
  ついてだけで済む。
- **履歴はフォーカス中のタブの `back` だけ。** 全タブぶんを混ぜるには «どちらが新しいか»
  を答えられなければならないが、履歴が持っているのはタブごとの順序で時刻ではない ─
  2 本の列をつなぐことはできても、交互に並べる根拠がどこにも無い。**`forward` も入れない**
  （あちらは «戻るで離れた先» で、`Alt+→` がすでに指している ─ 最近見た順の列に混ぜると
  その順序が嘘になる）。**同じフォルダは畳む**（新しいほうが残る）、**今いるフォルダは
  落とす**（タブの行と同じ理由。`A → B → A` で `back` に自分が残る）、**件数には上限がある**
  （`kHistoryRows`）─ «最近» は画面 1 枚ぶんで尽きるし、それ以上並べるとブックマークと
  ドライブを画面外へ押し出す。溢れたぶんは `Alt+←` で届く。
- **履歴の表示名は `App::DisplayNameOf`。`Tab::title()` は使えない** ─ あちらの最初の
  答えは列挙が持ち帰った表示名で、履歴の 1 行はパスしか持っていない。
- **種別を行に出す**（「ブックマーク」/「開いているタブ」/「クイックアクセス」/「ドライブ」/
  「履歴」）。同じフォルダが
  2 行に並ぶことは普通にあり、しかも **`Enter` の意味が行によって違う**ので、押す前に
  見えていなければならない。絞り込みにも当たるので「タブ」と打てば開いているものだけが
  並ぶ。
- **タブの行の和音はフォーカス中のペインにだけ出す**（`kNumberedTabs`）。`Ctrl+<数字>` が
  届くのはそのペインだけなので、届かない和音を隣に書くのは無いより悪い。
- **`Alt+Shift+1..8` は残す。** よく使う 8 個への 1 打鍵は依然として最速で、一覧を開くのは
  その外側の話。**同じ理由で、割り当てのある行にはその和音を出す**（`F1` の「割り当ては
  全部並べる」と同じ ─ 答えを知っている画面が黙っていると、番号で行けること自体が
  伝わらない）。
- **既定は `Ctrl+P`。** **移動はファイラーの主動詞**なので、コマンドパレット
  （`Ctrl+Shift+P`）より打ちやすい席をこちらに渡す ─ VS Code が `Ctrl+P` を «場所»、
  `Ctrl+Shift+P` を «コマンド» に当てて短いほうを場所に与えているのと同じ判断。
  **`Alt+B` から移した**（0.1.x の既定）。**既存の `keys.ini` にはその行が書かれているので、
  和音が移るのは新規の環境だけ** ─ `Ctrl+N` と同じ罠で、案内するときはそれを踏まえること
  （名前が `nav.places` に変わっても同じ ─ 旧名の行はそのまま読まれる）。`Ctrl+B` にはしない（**あちらは既にサイドバーの表示切り替え**で、
  既定を移すと `view.toggle_sidebar = Ctrl+B` を持つ既存の `keys.ini` と 1 つの和音を
  取り合う）。**`Shift+B` にもしない** ─ 文字キー単独と `Shift+<文字>` は型入力ジャンプ
  （ROADMAP P3-4）のために空けてある。
- **番号の和音は表を引く**（`PlacePicker.cpp` の `kNumbered`）。`Cmd::Bookmark1 + 添字`
  で作ってはならない ─ 9 件目でコマンド表の外を指す。**この画面が足された理由そのものが
  「9 件目がある」なので**、セッションチップ（`App::GotoSession`）と同じ罠がここで必ず出る。
- **絞り込みは名前とパスの両方に部分一致。** 前方一致にすると、深いパスの末尾のフォルダ名で
  探せない ─ ブックマークは「置き場所」で覚えていることのほうが多い。
- **絞り込み欄はキャレットの動く入力欄**（下の「チューザの絞り込み欄」）。**先頭に `>` を
  打つとコマンドパレットに切り替わる** ─ VS Code と同じ読み方（同じ節の「`>` でコマンドに
  切り替える」）。
- **選択は行番号ではなく «行そのもの» で覚える**（`PickerList` の id ＝ 並べたときの位置）。
  1 文字打つたびに行番号は振り直されるので、絞り込み後の位置で覚えると打鍵ごとに別の
  行へ滑る。**ブックマークの添字にはできない** ─ 行の出どころが 2 つになった以上、
  両方を指せる値でなければならない。
- **絞り込みで選択が消えたら先頭に落とす。** 行が並んでいるのに `Enter` が何もしない、
  という状態を作らない。逆に**候補が 0 件のときの `Enter` は何もしない** ─ そこで先頭を
  開くと、打ち間違えたまま `Enter` した人が知らないフォルダへ飛ぶ。
- **`Enter` が現ペイン、`Ctrl+Enter` が新しいタブ**（一覧の `Ctrl+Enter` と同じ読み方）。
  **マウスは 1 クリックで移動** ─ 補完候補と同じで、押した人はもう決めている。
- **`Escape` は先に絞り込みを捨てる。** 打ち間違えた絞り込みを消すために画面ごと開き直す
  ことにならないように。
- **行き先が 1 つも無いときは開かない。** 空のパネルは「押しても何も起きない」と同じうえ、
  閉じる手間だけ増える。代わりにステータス行で、無いことと `Ctrl+D` で足せることを言う。
  **ドライブが並ぶようになったので実機では起こらない**が、道は残してある ─ 消すと、
  «0 件でも開く» が既定に戻ってしまう。
- **追加・削除・並べ替えはこの画面に持ち込まない。** サイドバーにある操作を 2 か所に置くと、
  片方だけが直る日が来る。
- **`HandleKey()` は「何をすべきか」を返すだけで、移動そのものは `App::ChoosePlace`。**
  この画面が `Workspace` を書き換え始めた時点で、«選ばせるもの» ではなくなる。
- **設定を読み直すときは閉じる**（`App::LoadSettings`）─ 行が持っているのは、これから
  差し替わるブックマーク列とペイン構成への添字。

### コマンドパレット

`Ctrl+Shift+P`（`Cmd::ShowCommandPalette`）で全コマンドを 1 枚のオーバーレイに並べ、
絞り込んで `Enter` で実行する。**キー割り当てを覚えていない人、和音を常駐ソフトに
奪われた人にとって、ここが全操作への唯一の入口**になる ─ `F1` の一覧は読むためのもので、
実行する経路を持っていない。判断は `core/app/CommandPalette`（OS にも描画にも触れない）が
持ち、`tests/test_commandpalette.cpp` が端から端まで検証する。

行き先の一覧（`Ctrl+P`）と同じ作法にしてある ─ 同じパネル、同じ行の刻み、表示中は
全打鍵を飲み込み、閉じるのは `Esc`。**絞り込み・カーソル・スクロールは
`core/app/PickerList` が、入力欄そのものは `core/input/TextField` が 1 つだけ持ち、
両方の画面が使う。** タブバーの縦横で折り返しと
スクロールの計算を共通にしてあるのと同じ理由で、**片方の画面だけに「選択を画面内に
引き戻す」ような規則を書き足さないこと** ─ 2 つに分かれた時点でどちらかが必ず古くなる。
`PickerList` が持つのは «行を指すための値»（id）と絞り込みの対象になる文字列だけで、
行の実体は画面の側にある。

- **絞り込みはラベル・分類・`keys.ini` 上の名前の 3 つに当てる。** 設定ファイルで見た
  `tab.new` で探す人がいて、しかも**それは表示言語で動かない唯一の綴り**。分類にも当てる
  ので「セッション」と打てばセッション関係が並ぶ。部分一致なのはブックマーク一覧と同じ
  理由で、前方一致では語尾で探せない。
- **当たる 3 つは 3 つとも行に出す。** 名前を出していなかった頃は、`tab.new` で絞り込め
  ることが画面のどこからも読み取れなかった（「把握できない」と報告された形がこれ）─
  `F1` が「割り当ては全部並べる」のと同じ話で、**答えを知っている画面が黙っていては
  その機能が無いのと変わらない。** 名前は和音と同じ等幅フォントで出す ─ ファイルへ
  写す綴りであって、読ませる文ではない。
- **ラベルは `Strings::Label()` で引く。`Get()` ではない** ─ 番号違いの 24 個
  （`cmd.goto_session_n`）は表 3 行で賄われているので、`Get()` では画面に
  `cmd.goto_session_1` がそのまま出る（実際に出ていた）。
- **入る場所が無い列は落とす。落とす順は「その行を読んでいる理由から遠いほう」** ─
  分類（一覧全体で繰り返される）→ `keys.ini` 上の名前（すでに在るラベルの別の綴り）→
  和音。和音が最後まで残るのは、**「何のキーで動くのか、そもそも在るのか」がこの画面の
  問い**だから。列の幅は毎フレーム「画面に出ている行」から測る（`F1` の桁数と同じ理由 ─
  固定幅は最長の答えを半分に切る）。
- **並びはコマンド表の定義順**（`F1` の一覧と同じ並び）。**使用頻度で並べ替えない** ─
  探している行が打鍵ごとに動くと、目で追うより打ち直すほうが速くなる。頻度を覚えるなら
  それを書き出す場所も要る。
- **選択は行ではなくコマンドで覚える**（`PickerList` の id）。1 文字打つたびに行番号は
  振り直されるので、行で覚えると打鍵ごとに別のコマンドへ滑る。
- **`Enter` だけが実行。`Ctrl+Enter` は飲み込む** ─ 行が持つのはコマンド 1 つで、
  «別の実行のしかた» が無い。黙って `Enter` と同じ扱いにすると、無い意味を作ることになる
  （ブックマーク一覧の `Ctrl+Enter` は「新しいタブで開く」という実体がある）。
- **自分自身の行は出さない。** 選ぶとパレットが閉じて同じ画面が開き直るだけで、
  `Escape` を遠回りに綴った行き止まりになる。
- **並べるのはコマンドの «表» で、実行中に増減する «データ» は並べない。** ブックマークも
  開いているタブもセッションもドライブも「探して飛ぶ」ものだが、1 つ入れたら次を断る
  理由が無くなり、パレットはコマンドの表ではなく何でも入る箱になる。**行き先を選ばせる
  画面は `Ctrl+P` の側**（上の「行き先の一覧」。そちらは件数に上限が無いから在る）。
  パレットからは `nav.places` の行 → `Enter` でそれが開くので、道は 1 打鍵ぶん長い
  だけで通じている。
- **「1 つの窓口が便利」への答えは、混ぜることではない。** 混ぜると *頻度の高い側* が
  代金を払う ─ 行き先は毎日探すもの、コマンド表は覚えたら和音で叩くもので、`d` の 1 文字に
  `file.delete` と `Downloads` が並ぶ一覧では前者の行数が桁違いに多い。しかも毎日使う
  ブックマークを上に出すには**使用頻度の記憶**が要り、それは「探している行が打鍵ごとに
  動かない」を捨てることになる。**窓口を 1 つにする答えはモードのほう** ─ 入力欄で `>` を
  打つとコマンドに切り替わる（上の「`>` でコマンドに切り替える」）。«今どちらか» は
  入力欄の印が言い、順位付けは持ち込まないので、どちらの一覧も薄まらない。
- **例外は番号で指す 8 個で、そこには行き先の名前を添える**（`ui.command_palette_target`）。
  「ブックマーク 1 へ」は**どのフォルダなのかを何も言っていない** ─ 画面に出ていない一覧の
  «何番目» を指しているだけで、それ単体では読めない行だった。**行は増えない**（8 個は
  コマンド表の側の数）ので «データを並べ始める» ことにはならず、添えた名前は絞り込みにも
  当たるので 8 個までは名前で引ける。**9 件目以降は `Ctrl+P` の仕事のまま。**
  `Cmd::Bookmark1 + 添字` で作らないのはここでも同じ（`CommandPalette.cpp` の
  `kNumberedBookmarks`）。
- **割り当ての無いコマンドには「未割り当て」と出す**（`ui.command_palette_unbound`）。
  何も書かない行は「窓が狭くて切れた列」と見分けが付かないうえ、**«どれにキーが無いのか»
  はこの画面が答えるべき問い**。
- **絞り込みは入力欄として描く**（`AppUi::PaintPickerFrame`）。表題の行に小さく添えた
  文字ではなく、**打ち込む相手**として、行の高さではなく文字の大きさで器を作り
  （`rowHeight * 1.6`）、キャレットと縁を持たせる。縁がアクセント色なのは「キーボードは
  ここにいる」─ パレットが出ている間、他に行き場が無い。**空でもキャレットは描く**
  （そこが打ち込む場所だと言うのはこれだけ）。
- **パネルの寸法は窓だけで決める。件数では決めない。** 一致した件数で決めていたら
  **1 文字打つたびにパネルごと ─ 入力欄まで ─ 窓の中心へ縮み**（「目で追いづらい」と
  報告された形がこれ）、絞り込み前の件数で決めていたら**画面ごとに高さが変わった** ─
  パレットから `nav.places` を選ぶと、いま打ち込んでいた入力欄が指の下から別の場所へ
  飛ぶ（これも報告された）。**表題・入力欄・区切り線はパネル上端からの固定の位置**なので、
  これで «打っても、画面を移っても動かない» が揃う。件数の表示だけは変わるが、位置は
  動かない。**入力欄は Kite の中で 1 か所にある**、が守るべき形。
- **だから 2 つの画面は同じ大きさで出る**（`AppUi::PaintPickerFrame`、幅の上限も
  `kPickerMaxWidth` 1 つ）。`PickerList` が数え方を 1 つにしてあるのと同じ理由で、
  **片方の画面にだけ器の規則を書き足さないこと** ─ 寸法の決め方が 2 通りになった時点で、
  行き来したときに必ずどちらかが飛ぶ。行の中身と、行を何行描くかだけが画面ごとの仕事。
- **既定は `Ctrl+Shift+P`。** 揃える先が Kite の中に無い機能なので外の作法をそのまま借りる
  ─ 覚えていない人のための入口が、その人が唯一知っている和音でないのでは順序が逆になる。
- **実行するのは `App::RunPaletteCommand`。** `HandleKey()` は「何をすべきか」を返すだけで、
  この画面が `Execute` を呼び始めた時点で «選ばせるもの» ではなくなる。**閉じてから実行する**
  ─ 実行したコマンドが入力欄を出したり別のオーバーレイを開いたりするので、パレットが
  上に残っていてはならない。
- **設定を読み直すときは閉じる**（`App::LoadSettings`）─ 行が持っているラベルと和音は、
  これから差し替わる言語とキーマップのもの。

### 型入力ジャンプ

一覧の上で文字キーを押すと、その名前で始まる行へカーソルが飛ぶ。判断は
`core/input/TypeAhead`（OS にも描画にも触れない）が持ち、`tests/test_typeahead.cpp` が
端から端まで検証する。**カーソルを動かすのはここではない** ─ `Type()` は飛び先の行を
返すだけで、動かすのは `App::TypeAheadChar`。

**`Ctrl+F` の絞り込みとは別の機能で、行は 1 つも消えない。** 探すだけのために絞り込むと、
見つけた後に絞り込みを解く手間が要るうえ、着いた先の前後に何があるかが見えない。

- **打った文字は 1.2 秒で消える**（`TypeAhead::kTimeoutMs`）。短すぎると名前を打ち終わる
  前に切れて途中から別の行へ飛び、長すぎると「もう忘れているはず」の文字列が次の
  ジャンプに混ざる。**時計は自分で読まない** ─ 現在時刻は呼ぶ側が渡す（テストが
  `FakeClockMs` を進めるだけで時間切れを検査できる）。
- **打ちかけの文字列はステータス行に出す**（`ui.type_ahead`）。**絞り込みと違って
  画面が何も変わらない**ので、これが無いと打ち間違いと «キーが効いていない» の区別が
  付かない。当たらなかったときは**打とうとした文字まで含めて**出す
  （`ui.type_ahead_no_match`）─ 見せるべきなのは、まさにその落ちた 1 文字。
- **`Space` の衝突は「打っている最中かどうか」で捌く。** 打ちかけの文字列が無ければ
  今までどおり選択のトグル、ある間は名前の一部として受け取る（空白を含む名前は
  珍しくない）。**`OnKey` で飲み込むのはコマンドのほうだけ** ─ `TranslateMessage` は
  `DispatchMessage` の前に走るので、続く `WM_CHAR` はもうキューにあり、それがバッファに
  空白を入れる。`Backspace`（1 文字消す）と `Escape`（打ちかけを捨てる）も同じ読み方で、
  打っている間だけ打ちかけの文字列のものになる。`Escape` が先に文字列を捨てるのは
  補完やブックマーク一覧と揃えてある。
- **同じ文字を続けて打つと次の候補へ回る。** ただし**先に「その文字が並んだ名前」を
  探す** ─ `aa` を打った人の前に `aardvark` があるなら、そちらが答え。当たらなかった
  ときだけ「`a` の 2 回目」と読み替える。どちらの読み方も失わないための順序。
- **新しい 1 文字目はカーソル行を飛ばして探し、打ち足したときは飛ばさない。** 前者を
  飛ばさないと、すでに選ばれている行が答えになって一覧が固まったように見える。後者は、
  今いる行こそ絞り込もうとしている相手であることのほうが多い。
- **当たらなかった 1 文字はバッファに入れない。** 入れると、打ち間違いの 1 文字のせいで
  以後の打鍵が全部空振りする文字列だけが残る。
- **`Backspace` でカーソルは動かさない。** 短くした文字列にも今の行は必ず前方一致して
  いる ─ 「ab」で着いた行は「a」でも当たる。
- **コマンドを 1 つでも実行したら打ちかけの文字列は捨てる**（`App::OnKey`）。別の操作を
  した以上、それはもう «今打っているもの» ではない。
- **`swallowChar_` は文字を伴う和音でしか立てない**（`ProducesChar`）。`WM_KEYDOWN` を
  消費しても `WM_CHAR` は止まらないので、キーを割り当てた 1 文字がコマンドの実行後に
  ジャンプへ落ちないよう印を立てるが、**矢印キーやファンクションキーで立てると次の
  打鍵まで印が残る** ─ IME の変換確定は `VK_PROCESSKEY` なので `OnKey` を通らず、
  残った印がその 1 文字目を食う。

### 設定画面

`Ctrl+,`（`Cmd::ShowSettings`）で開く。**メニューバーやリボンは足さない**
（ROADMAP「あえてやらないこと」）─ キーボード駆動を主軸に据えた以上、設定も
`Ctrl+Shift+,` のショートカット設定画面と同じ**オーバーレイ 1 枚**で、同じ行の作法
（`↑↓` で移動、表示中は全打鍵を飲み込む、閉じるのは `Esc`）にしてある。違うのは
行が持つものだけ ─ 和音ではなく、両側に矢印の付いた値。

判断は `core/app/SettingsEditor`（OS にも描画にも触れない）にあり、
`tests/test_settings.cpp` が端から端まで検証する。

- **`SettingsEditor` は設定の値を持たない。** 持つのは「選択肢の何番目か」だけで、
  実体は `App` の各メンバ。**反映するのは `changed()` が指す 1 項目だけ**
  （`App::ApplySetting`）─ 全項目を書き戻すと、この画面が選択肢を持たない値、たとえば
  `lang.fr.ini` を置いた人の `language = fr` が「自動」で上書きされる。
- **開くたびに現在値から作り直す。** 設定は画面の外（`Ctrl+Shift+M`、`Ctrl++`）からも
  変わる。
- **変更のたびに `settings.ini` へ書く。** `keys.ini` と同じ理由で、目の前で効いた
  変更が次に落ちたとき消えているほうが実害が大きい。**単独ウィンドウでは書かない**
  （「2 枚目のウィンドウ」の規則そのまま）ので、開いたときにその旨をステータス行に出す。
- **`←→` は端で止まり、`Enter` と `Space` だけが回り込む。** 前者は方向の指示なので、
  文字サイズ 14 段の端から端へ飛ぶのは事故にしかならない。後者は「切り替え」なので、
  押すたびに何かが起きないと押した意味が無い。マウスも同じ割り当て ─ 矢印は 1 つ動かし、
  行そのものを押すと次へ回る。
- **選択肢の刻みはキー操作と揃える。** 文字サイズは `Ctrl++` と同じ 0.1 きざみ・
  同じ上下限（`kFontScaleMin` / `kFontScaleMax`）で、片方だけ広げると
  「キーでは出せるのに設定画面では選べない大きさ」ができる。

**新しいタブの位置**（`[ui] new_tab_position`）は挙動そのものを変える設定なので、
入口が 2 つある ─ 上の画面と ini。既定は現行動作の `end`（末尾）で、`after_current` に
すると今のタブの右に入る（`App::NewTabAt`）。**`Cmd::DuplicateTab` はこの設定に従わない**
─ 複製が元の隣に出るのは位置の好みではなく、その操作の意味そのもの。**起動時の
コマンドライン引数も従わない** ─ 並べて開くよう頼まれた順がそのまま並びになる。

**ZIP をフォルダとして開くか**（`[ui] open_archives`）も同じ扱いで、入口は 2 つ。
理由は「仮想フォルダ」の「ZIP をフォルダとして開く」に。

### 文字サイズ

`Ctrl++` / `Ctrl+-` / `Ctrl+0` が変えるのは `App::fontScale_` という 1 つの倍率
だけで、テーマは変えるたびに **「既定 → `settings.ini` → 倍率」の順で作り直す**
（`App::ApplyTheme`）。テーマ切り替えも設定再読み込みもこの関数を通る ─ 別々に
組み立てると、後から走ったほうが先の結果を黙って捨てる。

**倍率が掛かるのは文字サイズだけではない**（`Theme::Scale`）。行・各バー・
サイドバー幅は文字を入れる器なので同じ率で伸ばす。器を据え置いて中身だけ大きく
すれば下端から切れ、小さくすれば 1 行が無駄に高いまま残る。逆に分割線や
スクロールバーの幅は文字を載せないので変えない。一覧のアイコン枠も
`ui/AppUi.cpp` の `IconCell()` で行の高さから引く ─ 定数のままだと、行の高さが
倍になったところに 16 px のアイコンが取り残される。

**`ui/` 側は倍率を知らない。** 受け取るのは掛け算済みの `Theme` だけで、
「今 1.4 倍だから」と判断する場所を作らない。

プラットフォーム側は `WinWindow::Paint` が毎フレーム `UpdateTheme` を呼ぶ。
DirectWrite の書式はサイズ固定で作るので、作り直さないと寸法だけが伸びて文字は
元の大きさのまま残る。`UpdateTheme` は先に比較するので、変化が無ければ何もしない。

### サイドバーのアイコン

**3 つの区画すべてが、行ごとにシェルへ訊く**（`AppUi::PaintSidebar` の `item`、
`App::IconFor`）。**ブックマークだけ★を描いていた時期があるが、やめた。** 理由は
「留めた印」なら見出し（ブックマーク）がすでに言っており、★は**何を留めたのかを
何も言わない**から ─ 同期中の OneDrive、変更のある作業コピー、誰も届かない共有が、
並んだ同じ★になる。**オーバーレイを載せられるのはシェルのアイコンだけ**なので、
区別が要る場所で自前の絵を描く意味が無い。

**届くまで（および `[ui] shell_icons = false`）はベクタ描画が同じ寸法で場所を持つ**
─ 一覧の行と同じ理由で、持たないとアイコンが届いた瞬間にラベルが横へずれる
（`tests/test_appui.cpp` が、アイコンの有無でラベルの位置が変わらないことを検査している）。
代わりの絵は**その行が実際に何であるか**で選ぶ ─ ブックマークはフォルダなので
クイックアクセスと同じフォルダの絵。

### サイドバーの折り畳み

見出し（クイックアクセス／ブックマーク／ドライブ）はクリックで折り畳める。
状態は `App` が持ち `settings.ini` に残る ─ ドライブが 20 台ある環境で、開くたびに
ブックマークまでスクロールし直すことになる。

**スクロール位置は前フレームの内容高さで丸める**（`AppUi::sidebarContent_`）。
折り畳めば中身は縮むのに、スクロール量はさっきの高さを前提にした値のまま残る。
丸め忘れると、畳んだ瞬間にサイドバーが真っ白になって戻し方が分からなくなる。

### サイドバーの並べ替え

ドラッグできるものが 2 つある ─ **項目**（`App::MoveSidebarItem`）と、見出しを
掴んで動かす**区画そのもの**（`App::MoveSidebarSection`）。

項目は**区画をまたげない**。3 つの区画は見出しの付いた 1 本の一覧ではなく、
別々に順序を持つ 3 本の一覧で、ブックマークをドライブの間に置いても意味を持たない。
区画の行から外れたところでは挿入位置を出さない（`AppUi::ResolveSidebarDrop`）。

**区画の落とし先は見出しではなく「塊」で測る**（`AppUi::SectionBlock`）。見出しの行
だけで判定すると、クイックアクセスを開いているときその見出しは自分が占める範囲の
真ん中から程遠い位置にある ─ 「ブックマークの下」は、その項目群の下でなければ意味が
合わない。塊は前フレームの領域一覧から組み立てるので、折り畳んであれば見出し 1 行が
そのまま塊になる。

**開くのも畳むのも離した瞬間**（`Drag::PendingSidebar` / `Drag::PendingSection`）。
押した瞬間にはどちらなのかまだ決まっておらず、そこで実行すると項目は並べ替えるたびに
今見ているフォルダから離れ、見出しは動かすたびに畳まれる。中クリックだけは押した時点で
開く ─ 並べ替えを始められるのは左ボタンだけなので、待つ理由が無い。

**クイックアクセスとドライブは並びを別に覚える。** どちらも列挙のたびに OS が OS の順で
返してくるので、`quickAccessOrder_` / `driveOrder_` にパスの列として持ち、
`RefreshRoots()` で掛け直す（`F5` でも通る道なので、覚えていないと更新のたびに元に
戻る）。覚えている並びに無い項目 ─ 後から挿した USB ドライブなど ─ は末尾に付ける。
列挙順で間に差し込むと、一覧が変わるたびに位置が動く。**ブックマークだけは順序そのものが
Kite のデータ**なので、`bookmarks.ini` の並びがそのまま答えになる
（`Alt+Shift+1..8` もこの並びを数える）。

区画の並びは `settings.ini` の `[sidebar]` に名前で書く。**読むときは、書かれて
いない区画を組み込みの順で末尾に足す**（`App::LoadSidebarSections`）─ 手で編集した
ファイルや古い版が書いたファイルには全部が並んでいるとは限らず、落とすと画面から
消えたまま、ファイルを編集し直す以外に戻す手段が無くなる。

### アドレスバー

**パンくずと同じ 1 行**（`AppUi::PaintPathBar`）。普段はタブバー直下のパンくずで、
`Ctrl+L` かパンくずの後ろの余白をクリックすると、その行がそのまま入力欄になる。

**2 段に分けない。** パンくずはパスの読む形、入力欄は書く形で、言っている内容は同じ
─ 別の行を足せば、同じパスが常に 2 か所に出る。同じ場所を使えば、専用の帯を出し
入れする作りにありがちな「編集に入った瞬間に窓の中身全部がずれる」も起きない
（`tests/test_appui.cpp` が、編集前後で行の位置が変わらないことを検査している）。

**開いた瞬間は全選択**（`Cmd::EditPath` が `Prompt::SelectAll`）。どのアドレスバーも
そうしている ─ 次の一手はたいてい別のパスを打つか貼るかで、その前に消す作業を
挟ませない。**同時に、これが「この行はもう表示ではない」と言う一番強い合図**でもある
（背景とアクセント色の線だけでは、パンくずが消えたことに気付かないという報告があって
足した）。打ち足したい人は `End` か `→` で畳んでから、という入力欄の一般則どおりに。

**入力欄の外を押したら畳む**（`App::CancelInlineEdit`、下の「対象の上で編集する」）。
押した先がすでに別の答えなので、書きかけのパスを抱えたまま居座らせない。打った文字は
捨てる ─ 半端なパスへ勝手に移動するよりは、何も起きないほうがいい。押した先の操作自体は
普通に実行する（`AppUi::OnMouse` が振り分けの**前**に畳むのはこのため。別ペインの
パスバーを押したときは、畳んでからそちらが開く）。入力欄は 1 つしかないので、
**書き換わるのはフォーカスされたペインの行だけ** ─ 他のペインはパンくずのまま。

**見出しは付けない。** 「パス:」と書いたところで、その行に入っているのがパスである
ことは中身が言っている ─ 語を置けばその幅だけパスが削れる。**行がそのまま答えに
なっていない入力欄には見出しが要る**（下の「対象の上で編集する」）が、その置き場所は
入力欄の中ではない。

**当たり判定はパンくずが勝つ**（`Add` の順）。行の背景を先に登録し、パンくずを後から
登録する ─ `Pick` は後ろから引くので、クリックした場所にパンくずがあればそこへ移動、
無ければ編集に入る。エクスプローラーと同じ割り切り。

### アドレスバーの補完

打った先のフォルダを候補に出す。判断は `core/input/PathComplete`
だけが持ち、`App` は文字列と列挙結果を渡して、選ばれた文字列を受け取る。

**列挙は `PathComplete` の外**。UI スレッドで列挙しない理由は「制御フロー」と同じで、
要求は他と同じく `DirectoryLoader` を通り、結果は `App::PumpLoader` が `SetListing()`
で流し込む。トークンはタブのものと別に持つ
（`App::completeToken_`）─ 打ち続けている間に届いた古いフォルダの答えは、どのタブの
ものでもないので黙って捨てられる。**同じフォルダは 1 回しか列挙しない**
（`App::RequestCompletion`）。`C:\Users\ab` と打つと 1 つのフォルダに対して 3 つの
前置が生まれるが、打鍵ごとに列挙し直せば共有の待ち時間がそのまま 1 文字ごとに乗る。

- **候補一覧はその行から下へ垂らす**（`LayoutCompletion`）。垂らせるのはこの入力欄
  だけで、**候補を持つ入力欄が他に無いから**である ─ 名前の変更も新規作成も、
  «その名前で始まるもの» を勧める意味が無い（作る名前はまだ無いもので、変える名前は
  今そこに在るもの）。
- **候補はフォルダだけ。** 入力欄の `Enter` が行うのは移動なので、ファイルを勧めても
  行き先にできない。
- **候補はフルパスで描く**（`PathComplete::TextAt`）。`..` を含む入力やスラッシュ書き
  のとき、末尾の名前だけでは「どこの下の話か」が読み取れない。採ったときに入力欄へ
  入る文字列と画面に出ている文字列を同じ関数から出しているのはそのため。
- **隠しフォルダは、名前を打ち始めていれば出す。** 何も打っていない状態は「ここに何が
  あるか」を尋ねているので `$Recycle.Bin` から返しても仕方がないが、名前で探している
  人に見えている名前の続きを補完しないのはただの意地悪。
- **キャレットが末尾にないときは補完しない**（`App::SyncCompletion`）。補完が足すのは
  文字列の末尾なので、途中にキャレットがあるまま候補を採ると後ろが消える。`←` や
  `Home` で離れた時点で畳み、`Tab` を押しても直前の候補を採らない。
- **候補一覧は重ねて出す。** 描くのは全ペインを描いたあと（`AppUi::PaintCompletion`）
  だが、**位置を決めるのはパンくずの行を描くとき**（`LayoutCompletion`）─ その下の
  一覧を描く時点で矩形が分かっていないと、ポインタの下に来た行を光らせずに済ませ
  られない（押せないものを押せそうに見せない、というオーバーレイと同じ扱い）。
  上下分割の下側のペインにはみ出しても、そちらは後から描かれるので同じ判定が効く。
- **スクロール位置は選択から毎回引く。** この一覧を動かす手段は選択を動かすことしか
  無いので、別に覚えても選択と食い違うだけになる。
- **`Escape` は先に候補を畳む。** 打った文字列を巻き添えにせず候補だけ消せる道が
  無いと、間違えて出した候補一覧を消すために入力をやり直すことになる。
- `Tab` は既定で `Cmd::FocusNextPane` に割り当ててあるが、入力欄はキーマップより
  手前で全打鍵を受けるので（`App::HandlePromptKey`）衝突しない。

### 対象の上で編集する

**1 つの名前を尋ねる入力欄は、その名前が書かれている場所に出す**（`Prompt::isInline()`）。
アドレスバーがパンくずの行を借りるのと同じ作法を、**名前の変更・新規作成・セッション名**へ
広げたもの ─ 借りる先は一覧の行とセッションチップ。**下端の帯に残るのは 2 つだけ**で、
一覧全体を相手にする絞り込みと、文字を打つのではない削除の確認。

**下端でも動きはするが、何を相手にしているのかを言わない。** 「名前の変更:」の欄が窓の
下辺に出ていても、20 行のうちどれの話なのかは書いていない ─ カーソルの枠だけが答えで、
利用者から「分かりにくい」と報告された実際の形がこれ。位置がそのまま答えになれば、
説明する語が要らなくなる。

- **見出しはステータス行へ移す**（`AppUi::PaintStatusBar`）。捨てられない ─ 借りた行の
  上の空の箱は「ここに名前を打て」と言うが、**フォルダを作るのかファイルを作るのかは
  言わない。** 入力欄の中に置くとその幅だけ名前が削れるので、幅を取り合わない場所に
  出す。直前の操作の報告より優先する ─ 今まさに打っている欄が何を訊いているのかは、
  さっき起きたことより先に要る。
- **名前の変更は語幹を選択して開く**（`Cmd::Rename` が `Prompt::SelectRange`）。`Ctrl+L` が
  全選択で開くのと同じ理由 ─ 次の一手はたいてい別の名前を打つことなので、その前に消す
  作業を挟ませない。**拡張子は選択に入れない**（`archive.tar.gz` なら `archive.tar` まで）。
  キャレットは選択の終端側に置く（`→` で後ろへ畳める、という一般則）。
  **フォルダは名前ぜんぶ** ─ `path::Stem` を通してはならない。`backup.2026` に、誰も
  拡張子だと思っていない `.2026` が残る。なお `path::Stem` は先頭のドット
  （`.gitignore`）とドットの無い名前に対して名前ぜんぶを返すので、そこに分岐は要らない。
- **新規作成は行を 1 つ借りる**（`AppUi::PaintList` の `phantom`）。`Enter` までファイルは
  存在しないので自分の行が無い。**カーソルの 1 行下**に挿し、下の行を押し下げる ─ 上に
  挿すと、今読んでいた行が動く。エクスプローラーのように**先に作ってから名前を訊く形には
  しない** ─ `Escape` で消えるべきものがディスクに残るし、取り消し履歴にも積まれる。
  借りた行の**位置は仮**で、出来上がった項目がどこへ並ぶかは並べ替えの答え（打ち終わる
  前には決まらない）。
- **行数を数えるものは全部「枠」で数える**（`slots`）。借りた行はスクロール範囲・画面に
  出る行・縞の位置のどれにも効く。**entry の添字と枠の添字を混ぜないこと** ─ 借りた行より
  下では 1 つずれる。当たり判定に渡すのは entry の添字のほう（クリックは実在の行に効く）。
- **借りた行が画面外なら送る。** 挿す先はカーソルの 1 行下で、そのカーソルが最下行のことが
  ある ─ **見えない入力欄は、下端の帯より悪い。** 一覧の高さが分かるのは `PaintList` だけ
  なので、送るのもそこ。
- **セッションチップは中身に合わせて伸ばす**（`AppUi::SessionChipEditing`）。名前の幅で
  測ったままの箱は、打っている最中の文字を切る。**レイアウトと描画が同じ関数に訊く** ─
  片方のチップを測って別のチップに描くと、必ず切れる側ができる。番号は残す（`Alt+<数字>`
  の答えであり、消えると別のチップに見える）。
- **外を押したら畳む。打った名前は捨てる**（`App::CancelInlineEdit`）。**エクスプローラーは
  ここで確定するが、真似していない** ─ 確定させる側のクリックはたいてい行に落ち、その同じ
  クリックがカーソルを動かす。カーソルは「誰の名前を変えるか」を決めているもの
  （`App::ApplyPrompt` が `CursorEntry()` を引く）なので、確定にすると**押し間違いで、
  しかも違うファイルの**名前が変わる。`Enter` だけが「はい」。
- **入力欄の中を押したら、その打鍵はそこで終わる**（`Hit::PromptField`）。畳む判定より
  後ろで当たり判定を登録すること（`Pick` は後ろから引く）─ 登録が前だと、入力欄を押した
  だけでカーソルが動く。クリックでキャレットを動かす機能はまだ無く、**入れるときは
  全部の入力欄に同時に入れる**（アドレスバーも同じ扱い）。

### 入力欄のクリップボード

**入力欄が出ている間、`Ctrl+C` / `Ctrl+X` / `Ctrl+V` は入力欄のもの**
（`App::HandlePromptKey`。チューザの絞り込み欄では `PickerClipboardKey`）。キーマップに
素通しすると `Cmd::Copy` と `Cmd::Paste` になって**後ろの一覧に効く** ─ 半端に打ちかけた
パスの前でキャレットが点滅している最中にファイルがコピーされる、というのがその帰結で、
これでは Kite でただ 1 つ「パスを貼り付けられないテキスト欄」がアドレスバーになる。
`Ctrl+Insert` / `Shift+Insert` / `Shift+Delete` も同じ 3 つの別綴りとして受ける。

**判断は 1 か所**（`CopyFieldToClipboard()` / `PasteIntoField()`、どちらも `App.cpp`）。
どの入力欄でも同じに動く ─ 画面によって `Ctrl+C` の答えが違う欄は、1 つの入力欄では
ない。

- **選択が無いときの `Ctrl+C` は行全体を写す。** `Ctrl+X` は何もしない ─ コピーは
  何も失わないので拾える答えは拾うが、切り取りは失うので範囲を指すまで待つ。
- **貼り付けは 1 行だけ採り、囲みの引用符を落とす**（`ClipboardToField`）。
  エクスプローラーの「パスのコピー」は `"C:\foo"` の形で寄越すので、そのまま
  入れるとどのファイルシステムも知らないパスになる。複数行は、改行が見えないまま
  1 本につながった文字列として入るほうが害が大きい。
- **テキストが無ければクリップボードのファイルの 1 つ目を使う。** 訊く順が逆だと、
  エクスプローラーでファイルをコピーした直後の「パスのコピー」がファイル側に
  食われる。
- 確認の入力欄（削除の Yes/No）では 3 つとも黙って捨てる。あそこの文字列は件数で
  あって編集するものではない。

### 入力欄の選択

**文字列・キャレット・選択と、その上の打鍵は `core/input/TextField` が 1 つだけ持つ。**
`Prompt` はそれを継承して «何を尋ねているか» を足しただけ、チューザの絞り込み欄
（`PickerList`）はそれを 1 つ持っているだけで、**キャレットの数え方は Kite に 1 つ
しかない** ─ 2 つに分かれた時点でどちらかが必ず古くなる、というのはタブバーの縦横や
チューザの器と同じ話で、`Shift+←` の伸ばし方が画面ごとに違うのでは入力欄とは言えない。
`tests/test_textfield.cpp` が端から端まで検証する。

キャレットのほかに `anchor` を持つ。両者が等しければ選択なし。
`Ctrl+A` で全体を選び、**文字入力・`Backspace`・`Delete` はまず選択を消す**
（`TextField::DeleteSelection`）─ 選択したまま打った文字が置き換えではなく挿入に
なるなら、全選択に用は無い。

矢印キーは修飾で 3 通りに分かれる（`TextField::HandleKey`）:

| | 単独 | `Shift` |
| --- | --- | --- |
| `←` `→` | 1 文字。選択があれば**その端に畳む** | `anchor` を残して 1 文字伸縮 |
| `Ctrl+←` `Ctrl+→` | パス 1 区切り（`path::PrevSegment` / `NextSegment`） | 同じ幅で伸縮 |
| `Home` `End` | 端へ | 端まで選択 |

**`Ctrl` が動く単位が「単語」ではなく「パスの構成要素」なのは、この欄に入るのが
パスだから。** `Users` の途中で止まっても誰も嬉しくない。区切りは ASCII なので、
UTF-8 の文字の途中に着地することはない。**`Shift` 付きで `anchor` を据え置くのが
肝**で、ここを `SetCaret` にすると打鍵ごとに選択が作り直され、伸ばせなくなる。

キャレットを動かす場所では `TextField::SetCaret` を通すこと。`caret` だけを書くと
`anchor` が前の位置に残り、意図しない範囲が選択されたままになる（`Shift` の側は
これを**わざと**やっている）。

**`TextField` はクリップボードを持たない。** 読み書きできるのは `IShellIntegration` を
持つ側だけなので、`Ctrl+C` / `Ctrl+X` / `Ctrl+V` の 3 つは `App` が
`CopyFieldToClipboard()` / `PasteIntoField()` で組み立てる（下の「入力欄の
クリップボード」）。同じ理由で **`Shift+Delete` は `TextField` が受け取らない** ─ あれは
切り取りの別の綴り。

**キャレットは文字色、選択の色は文字の下に敷く**（`AppUi::PaintTextField`）。
キャレットは文字の並びの中に立つ文字大の印なので、アクセント色（「キーボードが
ここにいる」）は要らない ─ それはバーの縁がすでに言っている。選択を下に敷くのは、
`DrawText` 1 回では一部だけ色を変えられず、3 つに割ると各断片を単独で測ることになって
詰め（カーニング）が入った瞬間に全体の幅と合わなくなるため。

**描くのも 1 か所**（`AppUi::PaintTextField`）。パンくずの行・一覧の行・セッションチップ・
チューザの絞り込み欄が全部ここを通る ─ 2 つあると、選択が片方では文字の下、もう片方では
上に乗り、変換中の文字列がまた別の場所に出る。画面ごとに違うのは**文字の大きさ**（借りる
先に合わせる）と**キャレットが器の上下からどれだけ内側で止まるか**（チューザの箱は行の
1.6 倍の高さがある）だけで、引数で渡す。

### チューザの絞り込み欄

行き先の一覧（`Ctrl+P`）とコマンドパレット（`Ctrl+Shift+P`）の絞り込み欄は、上記の
`TextField` そのもの。**「打った文字が末尾に足されるだけ」ではない** ─ `←→`（`Ctrl` で
パス 1 区切り）、`Home` / `End`、`Shift` を足しての選択、`Ctrl+A`、クリップボードの 3 つが
アドレスバーとまったく同じに動く。

- **一覧のキーが先、残りが欄のもの**（`PickerList::HandleKey`）。`↑↓`・`PageUp/Down`・
  `Enter`・`Escape` を先に見て、それ以外を `TextField` へ渡す。
- **`Home` / `End` だけは絞り込みが空のときだけ一覧のもの。** 空の欄でキャレットを
  動かしても何も起きないので、そのときは «先頭の行へ» / «末尾の行へ» と読む。打ち始めた
  ら欄のものになる ─ どのテキスト欄でもそうだから。
- **文字列が変わったときだけ数え直す**（`Rebuild()`）。キャレットが動いただけで一覧を
  作り直す理由が無い。
- **クリップボードは `App` が横取りする**（`PickerClipboardKey`）。`PickerList` からは
  `IShellIntegration` に手が届かないので、`OnKey` が `HandleKey` より手前で 3 つの和音を
  拾い、`filterField()` を書き換えてから `FilterEdited()` で数え直す。**素通しすると
  `Cmd::Paste` になって後ろの一覧に効く** ─ 入力欄と同じ罠。
- **`Escape` はキャレットも戻す**（`TextField::Clear`）。文字列だけ捨ててキャレットを
  4 文字目に残すと、次に打った文字がどこへ入るか誰にも読めない。**戻す先はモードの印まで**
  （下記）─ 印ごと消すと、打った文字を捨てただけのつもりの `Escape` が画面を入れ替える。

#### `>` でコマンドに切り替える

**行き先の一覧（`Ctrl+P`）の欄で `>` を打つとコマンドパレットになり、消すと戻る。**
VS Code の `Ctrl+P` → `>` と同じ読み方で、**2 つの画面は 1 つの窓の 2 つのモード**。
切り替えは `App::SyncPickerMode`（絞り込み欄を書き換えた直後に必ず呼ぶ ─ 文字入力・
削除・貼り付け）。

**混ぜるのではなくモードにする**理由は下の「コマンドパレット」に。ここが満たしている
条件は 2 つで、**(1) «今どちらか» を入力欄が言う**、**(2) 順位付けを持ち込まない**。

- **印は入力欄の中に文字として在る**（`CommandPalette::kPrefix`）。ラベルでも枠の色でも
  ないので、**`Backspace` がそのまま戻る道**になる ─ 専用のキーが要らない。パレットは
  `>` を入れた状態で開くので、**「パレットが出ている」と「先頭が `>`」は同じ事実**。
  `Ctrl+Shift+P` で直接開いても印は在る。
- **絞り込みが掛かるのは印の後ろだけ**（`PickerList::SetPrefix`）。`filter()` が返すのは
  そちら、入力欄に見えている文字列そのものは `field()`。
- **持ち越すのは入力欄まるごと**（文字列・キャレット・選択）。捨てると、`>` を打った
  瞬間に打ちかけの文字列が消える。逆向きも同じで、`>` だけ消せば残りがそのまま
  行き先の絞り込みになる。
- **先頭の 1 文字だけがモードを決める。** 途中の `>` はただの文字 ─ `ab>` と打っても
  切り替わらない。`Home` でキャレットを印の手前に置いてから打てば切り替わるが、それは
  «先頭が `>` でなくなった» の当然の帰結。
- **相手のモードの和音は飲み込まずに渡す。** パレットの上での `Ctrl+P` は行き先へ、
  行き先の上での `Ctrl+Shift+P` はコマンドへ。1 つの窓なのだから、和音の意味が
  «今どちらを見ているか» で変わる理由が無い。
- **行き先が 1 つも無ければ戻らずに閉じる**（`App::OpenPlacePicker` が false）。空の
  パネルを出さないという規則がここでも効く ─ 理由はステータス行が言う。
- **`>` が在ることは画面が言う**（`ui.goto_hint` / `ui.command_palette_hint`）。
  `F1` が割り当てを全部並べるのと同じで、答えを知っている画面が黙っていると機能が
  無いのと変わらない。

### 日本語入力（IME）

**未確定の文字列は Kite が描く。IME には描かせない。** IME の変換窓は自前のフォントと
行送りで文字を置くので、入力欄の中の文字とは数ピクセルずれた位置に出て、確定した瞬間に
跳ぶ ─ 利用者から「確定していない状態だと文字の位置がずれる」と報告された形がこれ。
テーマにも従わないので、暗いテーマの Kite の上でそこだけ白く光る。

未確定の文字列は `App` の `Composition`（テキスト・キャレット・注目節の範囲）が預かり、
`Prompt::text` にも絞り込み文字列にも入れない ─ 変換は丸ごと取り消されうるので、
入れてしまうと `Backspace` の意味も一覧の絞り込み結果も変換の途中経過で揺れる。
プラットフォーム側は `WinWindow` の `WM_IME_*` だけで、`kite_core` は Windows を知らない
ままでいる（`tests/test_app.cpp` と `tests/test_appui.cpp` が端から端まで検査する）。

- **引き取るのは常に。画面によって IME に描かせたり描かせなかったりしない。** 変換窓を
  出すかどうかを言えるのは `WM_IME_SETCONTEXT` の `ISC_SHOWUICOMPOSITIONWINDOW` だけで、
  **そのメッセージが来るのはフォーカスが移るときだけ** ─「入力欄が開いている間は消す」
  という切り替え方ができない。半端に分けると、欄を開いてから打った文字が Kite の入力欄と
  IME の窓の**2 か所に出る。**
- **だから入力欄の無い画面にも描く先が要る。** 一覧の上（型入力ジャンプ）で変換している
  ときは、ステータス行が「変換中」として出す（`ui.composing`）。打ちかけの文字列を
  ステータス行に出しているのと同じ理由で、しかも**変換中はジャンプすら起きていない**ので、
  ここで言わなければ打った文字はどこにも出ない。
- **確定した文字は `GCS_RESULTSTR` を読んで `App::OnChar` へ 1 文字ずつ流す。**
  `WM_IME_COMPOSITION` を自分で消費している以上 `DefWindowProc` は `WM_CHAR` を作らない ─
  渡さなければ変換した文字はどこにも入らない。同じ理由で **`WM_IME_CHAR` は捨てる**
  （通すと二重に入る）。**未確定を捨ててから確定を流す** ─ 逆だと 1 フレームだけ同じ文字が
  2 つ並ぶ。
- **`GCS_RESULTSTR` と `GCS_COMPSTR` は同時に立ちうる。** 節を 1 つ確定して残りの変換を
  続ける IME があるので、どちらか一方の分岐にしない。
- **IME が数えるのは UTF-16 の符号単位、`core` はバイト。** 渡す前に「その手前までを
  UTF-8 にした長さ」へ直す（`WinWindow::ReadComposition`）。変換の途中経過だけ別の数え方に
  する理由が無い。受け取った側も文字列長に丸める ─ 描く側は毎フレームその添字で文字列を
  切る。
- **見せるのは «確定した後の形»。** 選択の上から変換を始めたら、選択は確定時に置き換え
  られるので、変換中からその範囲を消して見せる（`AppUi::PaintTextField`）。`Enter` を
  押して初めて消えるのでは、何が起きるのかが押すまで分からない。
- **幅は «画面に出ている 1 本の文字列» の接頭辞から測る。** 断片を別々に測って足すと、
  詰め（カーニング）が入った瞬間に全体の幅と合わなくなる ─ 入力欄の選択を 1 回の
  `DrawText` の下に敷いているのと同じ話。
- **注目節（今まさに変換している節）は一段強く。** 下線 1 本が「まだ確定していない」の
  共通語彙で、注目節にはさらに下敷き（`textSelection`）と太い下線（`accent`）を足す。
  節が 2 つ以上あるとき、**どれを変換しているのかは IME の窓を消した以上ここでしか
  分からない。**色を足していないのは、入力欄の中で「今この範囲の話をしている」と言う色が
  すでに `textSelection` だから。
- **印は器の下端ではなく «行» に付ける**（`CompositionLine`）。チューザの入力欄は行の
  1.6 倍の高さがあり文字は真ん中に置かれるので、器の下辺に引くと下線だけが文字から離れて
  浮く。
- **候補一覧には `CFS_EXCLUDE` でキャレットの矩形を避けさせる。** 変換窓を消した以上、
  一覧に隠されて困るのは Kite が描いている当の文字。
- **キャレットの位置を知っているのは描画だけ**（`AppUi::caretRect()`）。だから
  `WinWindow::Paint` が毎フレーム教え直す ─ 打つほど右へ伸びるので、変換を始めたときの
  位置に置いたままでは候補一覧が離れていく。入力欄が 1 つも出ていないフレームでは
  **フォーカスされた一覧のカーソル行**を指す（型入力ジャンプの変換を隅に取り残さない）。
- **入力欄の中で始まった変換だけが、その欄と運命を共にする**（`compositionInField_`）。
  外を押して畳まれたら `CPS_CANCEL` で打ち切る ─ 入力欄の「外を押したら畳む。打った名前は
  捨てる」をそのまま変換にも当てる。**一覧の上で始まった変換は巻き添えにしない**
  （そちらに畳まれる欄は無い）。
- **`LOGFONT` を送る**のは、`ISC_SHOWUICOMPOSITIONWINDOW` を落としても自前の窓を出す IME
  への保険。フォントが既定のままだと、そこだけ「ずれる」が戻る。
- **`IHost` に IME の窓口は無い。** 位置の出どころを `AppUi::caretRect()` の 1 つに保つ
  ため、使われていなかった `IHost::SetImePosition()` は外した（版の出どころを増やさない、
  というのと同じ話）。
- **キーの取り込み中と削除の確認は「入力欄」ではない**（`App::acceptsText()`）。どちらも
  打った «文字» ではなく打った «キー» が答えなので、畳まれる欄としては数えない。

### ネットワークの参照

`\\192.168.1.5` のようにサーバーだけを指したパスは、**その共有の一覧**になる。
振り分けの判定は `path::IsUncServer()`、列挙は `WinFileSystem::List` が
`WNetOpenEnum` / `WNetEnumResource` へ回す。

- **`FindFirstFile` では答えが出ない。** `\\srv\*` はファイルシステムが知っている
  パターンではないので、共有の一覧を返せるのはネットワークプロバイダだけ。
  ここを通さなかった頃、IP 指定の SMB サーバーは「この場所は利用できません」と
  だけ言って終わっていた。
- **共有の親はサーバー**（`path::Parent`）。共有はファイルシステムから見ればルート
  だが、その上に共有の一覧を持つ実在の場所がある以上、そこで止めると `..` も
  `Alt+↑` もパンくずも共有名を打ち直す以外に辿り着けない。サーバー自身が最上位。
- **サーバーは末尾の区切りを持たない**（`path::Normalize`）。`C:\` の末尾の `\` は
  「この後に来るものがある」という印だが、サーバーの後に来る共有はパスの一部では
  ない。`\\srv` と `\\srv\` が同じタブで揺れると、動いていないのに動いたように見える。
- **共有名の取り出しに `path::FileName` を使わない。** 共有はルートなので
  `\\server\share` が丸ごと返る。`path::UncServerLength()` の先を取ること。
- **`$` で終わる共有（`ADMIN$`、`C$`、`IPC$`）には隠し属性を付ける。**
  エクスプローラーが見せないものを既定で見せる理由が無い。
- **列挙が途中で切れたら結果ごと捨てる。** 半分の一覧を全部として出すと、
  「その共有は無い」と読めてしまう。

**`\\` そのもの（ネットワーク全体）は列挙しない。** サーバーの探索は
コンピューターブラウザーサービスに依存し、SMB1 とともに既定で無効化されて久しい
─ 今の Windows では数十秒待たせた末に空を返すほうが多い。**待たせて空を返す機能は、
無い機能より悪い。** 詳細と入れるときの条件は ROADMAP の P2-8b。

#### 資格情報

**`Ctrl+Shift+L`（`Cmd::ConnectNetwork`）だけが資格情報のダイアログを出す。**
入力欄を出すのは OS（`WNetAddConnection3W` の `CONNECT_INTERACTIVE | CONNECT_PROMPT`）で、
Kite は打たれた内容を受け取らないし保存もしない。

- **列挙の失敗から自動で呼んではならない。** 列挙はワーカースレッドで走り、しかも
  アドレスバーの補完も同じ道を通る ─ `\\192.168.1.5\` と打った瞬間に資格情報の
  ダイアログが飛び出すことになる。だから利用者が明示的に呼んだときだけ。
- 代わりに、**アクセス拒否で返ってきた UNC のパスにはキーの名前をステータス行に
  出す**（`App::PumpLoader`）。共有の「アクセスが拒否されました」は判定ではなく、
  まだ訊いていない質問であることのほうが多い。
- **相手はサーバーか共有で、その下のフォルダではない**（`path::UncRoot`）。
  資格情報が照合される単位がそこなので、深いパスのまま渡しても通らない。
- **サーバーだけを渡されたら `IPC$` を相手にする**（`WinShell::ConnectNetwork`）。
  共有名の分からない相手に認証を通す唯一の道で、エクスプローラーも同じことをする。
- **ドライブ文字は割り当てない。** `lpLocalName` を渡さないので接続はデバイスレスの
  まま残らず、Kite で 1 度サインインしたせいで次回ログオン時に復元される
  ネットワークドライブが増えたりしない。

### 仮想フォルダ

サイドバーの先頭に並ぶ**「PC」「ごみ箱」「ネットワーク」**は、ファイルシステムに
無い場所。パスの綴りは `virtual:computer` などで、判定は `core/fs/VirtualPath.h`
（`vfs::IsVirtual`）が持つ。**core はこの前置の規則しか知らない** ─ それが Windows の
どの場所を指すのかは `platform/win/VirtualNames.h` だけが知っている（`config::Choose()`
や `path::ToExtended()` と同じ理由で、規則を Windows 実装に埋めるとテストできない）。

**列挙は `kite_shellhost.exe` の中でしか行わない**（`ShellFolder.cpp`）。`IShellFolder`
の列挙は**名前空間拡張の DLL を呼び出し元のプロセスに読み込ませる** ─ 「PC」を開く
だけで iCloud の拡張が起き上がるのを実測している。`IContextMenu` と
`SHGFI_ADDOVERLAYS` を追い出したのと同じ話が 3 つ目として増えただけで、
**`ShellFolder.cpp` を `kite` ターゲットに足してはならない**（`ci.yml` が検査する）。

- **ホストは 3 つ目を立てる**（`FolderHostClient`）。メニューのホストは表示中ずっと
  `TrackPopupMenu` の中にいるので、共有すると**メニューを開いている間フォルダが 1 つも
  開けない**。アイコン用と分けているのは `SHGetFileInfo` が最大 8 秒待たされうるためで、
  一覧の到着がその後ろに並ぶ理由が無い。**読み取りには時間制限がある**（30 秒）─
  名前空間拡張の列挙は中断できないので、返ってこない相手にできるのはホストごと
  捨てることだけ。
- **`DirectoryLoader` のワーカーは複数あるので `FolderHostClient` が直列化する。**
  仮想フォルダを 2 枚同時に開いたときだけ待つ。実 FS の一覧は別経路なので巻き込まない。
- **項目は `fs::Entry::address` で指す。** 名前を親のパスに繋いでも指せない
  （`virtual:computer\Windows (C:)` は何でもない）ので、`path::Join(dir, name)` を
  直接書かず **`fs::EntryPath()` を通す**。空なら今までどおり連結、というだけなので
  実フォルダの経路は 1 バイトも増えない。
- **実 FS 上の項目はそのまま実パスになる。** 「PC」の下のドライブは `C:\` として
  返るので、開いた先は普通のフォルダ ─ 速い列挙も監視も効く。前置が付くのは
  名前空間拡張（`virtual:::{CLSID}\...`）だけ。
- **`..` は仮想フォルダへ抜ける**（`vfs::ParentOf`）。`C:\` の上は「PC」、`\\server`
  の上は「ネットワーク」。共有の親をサーバーにしたのと同じ判断で、その上に実在の
  置き場所がある以上そこで止める理由が無い。パンくずもこの規則で辿る。
- **名前は Kite の言語で呼ぶ**（`vfs::LabelKey` → `App::RefreshRoots` / `App::DisplayName`）。
  シェルの表示名は OS の言語なので、英語 Windows で日本語の Kite を使うと画面の中で
  そこだけ言語が変わる。3 つ以外（名前空間拡張）には Kite の言葉が無いので、
  **列挙が持ち帰った表示名**（`fs::ListResult::title`）を使う。
- **監視を張らない**（`App::SyncWatches`）。シェル名前空間には通知を掛けるハンドルが
  無い。更新は `F5`。
- **アドレスバーの補完はここへ入らない**（`App::RequestCompletion`）。補完は打鍵ごとに
  走るので、パスを打っているだけでホストが起動し、質問を受け続けることになる。
- **Kite 自身は書き込まない**（`App::ReadOnlyHere`）。作成・名前の変更・削除・切り取り・
  貼り付けは断ってステータス行で理由を言う。ドロップ先にもならない
  （`App::IsValidDropTarget`）。一覧であってフォルダではないうえ、**項目が持つパスは
  操作の相手になるパスとは限らない**（次項）。

#### ごみ箱の項目を正しく指す

消した項目の解析名は、ごみ箱が隠し持っている **`$R...` の写しのパス**（実測:
`C:\$Recycle.Bin\S-1-5-…\$R0L38Q5.ini`）。**これを `SHParseDisplayName` に渡しては
ならない** ─ 返るのはごみ箱の項目ではなく**ただのファイル**で、その親はごみ箱では
ないので、組み上がるのは「元に戻す」を持たない普通のファイルのメニューになる。
代わりに並ぶ「削除」「名前の変更」は `$I` の記録だけを残してごみ箱の対応表を壊す
（利用者が見たことのないパスに対する破壊操作）。

**だから項目は「どのフォルダの中の項目か」ごと渡す**（`shellhost::Request::container`）。
ホストはそのフォルダを列挙し、解析名の一致する子の PIDL を組み立てる
（`win::ResolveItemsInFolder`）。得られる絶対 PIDL の親はごみ箱そのものなので、
`SHBindToParent` から先はメニューもアイコンも今までどおりの道を通る。

- **実フォルダでは `container` を空にする**（`App::ShellMenuContainer`）。渡すと
  右クリック 1 回ごとにそのフォルダを列挙し直すことになる ─ 10 万件のフォルダで
  それは払えない。ファイルは自分のパスで正しく解決できるので、そもそも要らない。
- **PIDL をホスト側の表に置かない。** 「id を配って後で引く」形（アイコンの識別子と
  同じ作り）にすると、アイドル終了でホストが入れ替わった瞬間に id が別の項目を
  指す ─ アイコンなら絵が 1 枚間違うだけだが、こちらは**別のファイルを消す**。
  毎回引き当て直すのは、その代償を持たないため。列挙は数十ミリ秒で、
  右クリックの間隔に対しては無視できる。
- **見つからなかった項目は黙って飛ばす。** 一覧を出してから右クリックするまでの間に、
  別の窓がごみ箱を空にしていることがある。

**復元は `Alt+R`（`Cmd::Restore`）と `Ctrl+Z`**。どちらも実体はシェルの `undelete`
動詞で、`win::InvokeShellVerb` が引き当てを通してから `InvokeCommand` に名前を渡す。
**違うのは項目の指し方だけ** ─ `Alt+R` はごみ箱の一覧で選んだ項目なので解析名で、
`Ctrl+Z` は「さっき消したもの」なので**消される前のフルパス**で指す（下記）。

- **動詞を実行する前に `QueryContextMenu` を通す。** ハンドラが動詞の表を作るのは
  そこなので、構築を頼んでいないメニューはどの名前にも答えない。作った `HMENU` は
  そのまま捨てる（画面には何も出さない）。
- **ごみ箱以外では何もしない**（`App::DoRestore`）。動詞が存在しないので、押しても
  黙って失敗するだけになる ─ 効かないキーに見せず、その旨をステータス行に出す。
- **取り消し履歴には積まない。** 「元に戻す」を元に戻すのは「消す」であって、
  `Ctrl+Z` で救い出したファイルがごみ箱へ帰るのは誰も望んでいない。
- **名前の衝突も、戻す先がもう無い場合も、シェルが自分のダイアログで尋ねる。**
  ここを自前で書くと、ファイル操作をシェルに委ねている理由がここだけ崩れる。

#### `Ctrl+Z` で削除を戻す

履歴が覚えるのは**消される前のパス**（`UndoKind::Delete` の `targets`）。ごみ箱の中で
名乗る `$R` の名前ではないのは、**削除の時点で分かっているのがそちらしかない**から ─
控えるには削除のたびにごみ箱を引き当て直すことになり、一度も `Ctrl+Z` を押さない人まで
その代金を払う。突き合わせは戻すときに 1 回だけ行う（`win::ResolveTrashItemsByOrigin`）。

- **元のパスは「元の場所」と項目名から組み立てる。** 前者は `PID_DISPLACED_FROM`
  （フォルダまで）、後者はシェルの表示名。**項目名は拡張子を欠くことがある** ─
  エクスプローラーの「拡張子を表示しない」設定に従うため ─ なので、組み立てた文字列に
  拡張子 1 つ分を足した形も一致とみなす（`NamesSameFile`）。足すのが本当に末尾の
  1 要素であることは、続きに区切りが無いことで確かめる。
- **同じパスが複数あれば、最後に消したものを採る**（`PID_DISPLACED_DATE`）。
  `Ctrl+Z` が指しているのは常に直前の削除なので、それが正しい答えになる。実際に
  «消す → 同じ名前で作り直す → 消す → `Ctrl+Z`» で確かめてある。
- **完全削除は今までどおり戻せない**（`UndoKind::Erase`）。履歴に「戻せない印」を積み、
  そこから下は捨てる。**ごみ箱への削除では捨てない** ─ 戻せる以上、その下の操作が
  到達不能になる理由が無い。
- **ホストは日付を数として読むだけ。** `GetDetailsEx` が返すのは `PROPVARIANT` では
  なく `VARIANT` なので、「元の場所」は BSTR、「削除日時」は OLE の日付（double）で
  受け取れる ─ propsys への依存が要らない。書式に直す必要はどこにも無い。

#### ZIP をフォルダとして開く

**一覧で `.zip` を開くと、その中身が一覧に出る**（`App::ActivateEntry` →
`vfs::ArchivePath` → `OpenPath`）。ZIP はシェル名前空間拡張として見えるので、**列挙も
アイコンもメニューも上の経路がそのまま使える** ─ 足したのは入口の判断と、実 FS との
境目をどこに置くかだけ。`.cab` も同じ扱い。

- **並べる拡張子は「OS が最初からフォルダとして開けるもの」だけ**
  （`VirtualPath.cpp` の `kArchiveExtensions`）。`.7z` や `.rar` は展開ソフトを入れれば右クリックのメニューには載るが、**シェル名前空間
  には現れない** ─ 足したところで開く先が無く、「開いたのに空」になる。
- **書庫の縁が、シェル名前空間と実 FS の境目**（`vfs::ParentOf`）。`..` は書庫の中では
  1 つ戻るだけだが、**書庫そのものからは実フォルダへ抜ける** ─ `virtual:C:\a\pack.zip`
  の上は `C:\a`。ここで止めると、`Alt+↑` もパンくずも「書庫の中」から出られない。
  入れ子の書庫は**外側**が答え（`vfs::ArchiveFileOf`）─ 1 つ目の書庫より先はシェルの
  領分で、中で見つかった `.zip` は実 FS 上のファイルではない。
- **書庫の中の項目は、シェルが何と言おうと実 FS ではない**（`WinFileSystem::ListVirtual`
  の `insideArchive`）。解析名はパスの綴りそのもの（`C:\a\pack.zip\notes.txt`）で、
  `SFGAO_FILESYSTEM` を立てる実装もあるが、**`FindFirstFile` は開けないし監視も張れない。**
  ビットを信じずここで決めるのは、「実フォルダかどうか」の答えを 1 つに保つため ─
  取り違えると、書庫の中のフォルダを開いた瞬間に「この場所は利用できません」になる。
- **`virtual:` の前置を `path::Normalize` が壊さないようにした**（`SchemeLength`）。
  前置ごと 1 つの構成要素として畳んでいると、**UNC の先頭の `\\` が 1 本になる** ─
  `virtual:\\nas\pub\pack.zip` が `virtual:\nas\pub\pack.zip` になり、どこも指さない。
  併せてドライブ文字も畳まれるようになったので、`virtual:c:\…` と `virtual:C:\…` が
  2 枚のタブに割れない。**解析名（`::{CLSID}`）はそのまま素通しする** ─ あれはパスでは
  ないので、畳む構成要素が 1 つも無い。
- **アドレスバーに打った `C:\a\pack.zip` も同じ場所を開く**（`App::ArchiveTarget`）。
  パンくずが `C:\a\pack.zip\docs` と実パスの綴りで出る（`App::DisplayPath`）以上、
  打ち返されるのは当然で、**エクスプローラーも同じ綴りを受ける。** 拡張子が合ったときだけ
  実在を確かめる ─ `backup.zip` という名前の**フォルダ**は今までどおりただのフォルダで、
  その 1 回の問い合わせを払うのも拡張子が合った行だけ（`.lnk` と同じ順序）。
- **中のファイルは「取り出してから開く」**（`IShellIntegration::Open` の `folder` →
  `ShellHostClient::Extract` → `win::ExtractShellItem` → 出来上がった実ファイルを
  `ShellExecute`）。**素直な道が 2 つとも駄目だったのでこうなっている:**

  | やってみたこと | 何が起きたか |
  | --- | --- |
  | 行のパスを `ShellExecute` | 「ファイルが見つかりません」。`C:\a.zip\notes.txt` はパスの綴りだが、その綴りのファイルはディスクに無い |
  | シェルの既定動作を `InvokeCommand` | **空のファイルが開く。** explorer.exe が %TEMP% に空のフォルダを作って掴んだまま離さない |

  2 つ目の理由が肝で、シェルの「開く」は **explorer.exe が呼び出し元プロセスの中に
  在るデータ源から中身を吸い出す**作り。要求に答えてパイプの読み取りに戻ったホストは
  もう誰の相手もしないので、吸い出しが起きないまま空のファイルが開く。**待てば済む話
  ではない** ─ どれだけ待てばよいかを誰も言えないうえ、ホストは 60 秒で終了する。
- **だから `IFileOperation` で自分で写しを作る**（`ShellFolder.cpp` の
  `ExtractShellItem`）。`PerformOperations()` は同期なので、返った時点でファイルは
  出来上がっている ─ 実測で 1.4 KB が 0.3 秒、5 MB が 0.5 秒。**出来上がったかを
  必ず確かめてから開く**（`GetFileAttributesW`）─ 確かめずに開くのが、まさに空の
  ファイルが開く形だった。
- **取り出し先は `%TEMP%\Kite\<GUID>` で、1 回ごとに新しいフォルダ。消さない。**
  同じフォルダを使い回すと「同じ名前が既に在るとき」を決めることになり、答えは
  «開いているかもしれないファイルを上書き» か «別名を作る» の二択でどちらも悪い。
  消さないのは、開いたアプリがいつ閉じるか分からないから（エクスプローラーの同じ
  操作も %TEMP% に残していく）。
- **開いたものが書庫に戻らないことは画面が言う**（`ui.opened_copy`「読み取り専用で
  開きました」）。黙っていると «保存したのに反映されない» になる。**短く言う** ─
  仕組み（写し・一時フォルダ）を説明する場所ではないし、ステータス行の右は次の操作で
  消える。**開けなかったときも言う**（`ui.open_failed`）─ 一覧は動かずダイアログも
  出ないので、黙って失敗すると利用者に見えるのは「何も起きない」だけで、**この不具合は
  まさにその形で報告された。**
- **右クリックのメニューの「開く」は書庫の中では効かない。** あれはシェル自身の項目を
  ホストが `InvokeCommand` するだけで、上の «呼び出し元から吸い出す» 経路をそのまま
  通る ─ 開いたアプリには中身の入っていないファイルが渡る。**Enter とダブルクリックは
  Kite が自分で取り出すので効く。** 直すなら「既定項目が選ばれた」ことをホストに
  実行させず kite.exe へ返して、`App` の同じ経路に流すこと ─ **開き方を 2 通りに
  しない**（`Cmd::NewWindow` と同じ話）。
- **メニューの既定項目を番号で実行する道は採らなかった。** 動くところまでは確かめた
  （名前では頼めない ─ 実測で `GetCommandString` は「開く」に `E_NOTIMPL` を返し、
  隣の「コピー」「削除」だけが名乗る）が、**中身が入るかどうかが上の理由で運任せ**に
  なる。`GetCommandString` が答えることも、`InvokeCommand` が S_OK を返したことも、
  ファイルが出来上がった証拠にはならない。
- **`folder` を渡すのは仮想フォルダのときだけ**（`App::ShellMenuContainer`）。実フォルダの
  項目は自分のパスで正しく解決できるので、渡せば右クリック 1 回ごとにフォルダを列挙し直す
  ことになる ─ メニューのときと同じ話が、そのまま「開く」にも当てはまる。
- **書庫の中には書き込まない**（`App::ReadOnlyHere`）。ただし**言葉は分ける**
  （`ui.archive_read_only`）─ 仮想フォルダ共通の「ここは一覧であってフォルダーでは
  ありません」は、**見るぶんには立派にフォルダである**書庫に対しては誰も訊いていない
  ことに答えている。足りないのは書き込みのほうで、取り出す道は右クリックのメニュー。
- **アイコンは、パスとして引けなかったらシェルの名前として引き直す**
  （`ShellIcons.cpp` の `ask`）。書庫の中の項目はパスの綴りを名乗るのに、その綴りの
  ファイルはディスクに無い ─ この 1 段が無いと、書庫の中は全行がベクタ描画になる。
  **払うのは安いほうの問いが外れたときだけ。**
- **「プログラムから開く」とプロパティは書庫の中では効かない。** どちらも
  `ShellExecute` のままで、上の理由でそのまま失敗する ─ 直すなら `Open()` と同じく
  `folder` を取らせて `"openas"` / `"properties"` を `InvokeVerb` へ回すこと。
  右クリックのメニューからは今でも両方とも出せる。
- **既定は「フォルダとして開く」**（`[ui] open_archives`、設定画面「ZIP をフォルダとして
  開く」）。展開ソフトを入れている人の答えは違うので設定にしてあるが、**既定を off に
  すると、この機能が在ること自体が設定画面を開くまで誰にも伝わらない。** on でも失う
  操作は無い ─ 右クリックのメニューはシェルのものがそのまま出るので展開ソフトの項目は
  並ぶし、`..` で戻れる。

### フォルダのショートカット

**フォルダを指す `.lnk` はフォルダとして開く**（`App::ShortcutFolder` →
`App::ActivateEntry`）。シェルに渡すと `ShellExecute` がリンク先を
**エクスプローラーで**開く ─ 置き換えるつもりのものが立ち上がるうえ、Kite の
タブは元の場所に残る。

- **判定は拡張子が先。** 解決には COM とファイル読み取りと存在確認が要るので、
  ただのファイルを開くたびに払う代金ではない。
- **リンク先がファイルなら今までどおりシェルに渡す。** プログラムを名指している
  ことがあり、それを起動するのはまさにシェルが `.lnk` に対して行う仕事。
- **解決するのは `WinShell::ResolveShortcut`**（`IShellLink` + `IPersistFile`）。
  シェル拡張 DLL は動かないので自プロセスでよい ─ 隔離しているのは `IContextMenu`
  とアイコンオーバーレイのほう。**探索はさせない**（`SLR_NOSEARCH | SLR_NO_UI`）
  ─ リンク先が消えている `.lnk` を開くたびに OS の捜索が走っては、フォルダかどうかを
  訊いているだけの問い合わせが数秒かかる。
- **`\\?\\` を付けない。** シェルはこの綴りを解さないので、`ToExtendedPath` を
  通すと深い場所の `.lnk` が「読めない」ではなく「壊れている」ように見える。
  読めなければ今までどおりシェルに渡るだけ。
- **仮想フォルダの中では解決しない。** そこの項目が持つパスは操作の相手とは限らず
  （消した `.lnk` は隠された `$R` の写し）、辿ると、ごみ箱を見ているつもりの人が
  生きたフォルダへ飛ぶ。

### 2 枚目のウィンドウ

`Ctrl+N`（`Cmd::NewWindow`）は自分と同じ exe を `--new-window <表示中のフォルダ>` で
起動する。**1 プロセスに 2 つ目のウィンドウを作るのではない** ─ `App` も描画資源も
ローダーのワーカースレッドもウィンドウ 1 つぶんの寿命に紐付いていて、増やすには
その全部を多重化することになる。分けておけば片方が落ちてももう片方は残る。

`--new-window` が付いた起動だけが**単独ウィンドウ**（`App::SetStandalone`）になる。

- **保存されたセッションを読まない。** 読めば新しいウィンドウに元の窓の全セッションが
  複製され、頼んだフォルダはその何枚目かのタブになる。
- **終了時に何も書かない**（`App::SaveAll` が即戻る）。設定もセッションも起動時の写し
  でしかないので、後から閉じたほうが本体の変更を古い内容で上書きしてしまう。
  `Ctrl+S` は「保存しません」と答える。**`keys.ini` だけは例外**で、`Ctrl+Shift+,` での
  変更はその場で書く ─ 目の前で行った操作が黙って消えるほうが実害が大きい。
- **位置は OS に選ばせる**（`placement_` の x/y を -1 に戻す）。保存された座標を
  そのまま使うと、開いた瞬間に元のウィンドウとぴったり重なって増えたことが
  分からない。大きさは受け継ぐ。

**パスだけを渡す起動（`kite.exe C:\foo`）と区別が要る。** そちらは既存ウィンドウの
新しいタブになるので（下の「単一インスタンス」）、`--new-window` が
「本当に新しい窓が要る」と言える唯一の手段。子プロセスをジョブに入れていないのも
意図的 ─ 開いたウィンドウは開いた側より長生きしてよい（`kite_shellhost.exe` とは
ここが逆）。

**コマンドラインの末尾のバックスラッシュに注意。** ドライブ直下を表示している
ウィンドウから開くとパスは `C:\` で終わり、そのまま `"` で閉じると引用符自身が
エスケープされて残り全部が 1 つの引数になる。`QuoteArgument`（`WinWindow.cpp`）が
閉じ引用符の前のバックスラッシュだけを倍にする。

なお `Ctrl+N` は既定バインドを移しただけなので、**`keys.ini` を持っている環境では
効かない**（ファイルにある `file.new_file=Ctrl+N` が既定を上書きする）。既定を
変えるときは、消してよい設定ではないことを踏まえて案内すること。

### 単一インスタンス

2 つ目の起動は、既存のウィンドウへパスを渡して自分は黙って終わる
（`WinSingleInstance.cpp`、`main_win.cpp`、`App::OpenForwardedPaths`）。
エクスプローラーの代わりに使う以上、フォルダを開くたびにウィンドウが増えては困る。
**`--new-window` が付いた起動だけがこの検出を通らない**（上記）。

- **目印は exe のフルパスから導く**（`InstanceKey()`。大文字小文字を畳んだ FNV-1a）。
  ミューテックス名もウィンドウクラス名も同じ値から作るので、**別フォルダに展開した
  2 つのコピーは別のインスタンス**になる ─ 固定名にすると、`config` を隣に置いた
  ポータブル運用の 2 つが同じインスタンスとして振る舞い、一方の設定だけが使われる
  （下の「設定の置き場所」）。**出どころを増やさないこと。**
- **探索は `FindWindowW` 1 回。** クラス名が exe ごとに違うので、列挙して素性を
  確かめる必要がない。
- **単独ウィンドウには別のクラス名を使う**（`InstanceClassName(false)`）。同じ名前だと
  探索がそちらを掴みうるが、単独ウィンドウはワークスペースを保存しない ─ 頼んだ
  フォルダが「閉じれば消えるタブ」として開くことになる。
- **ミューテックスが在るのにウィンドウがまだ無い隙間がある。** 先のプロセスが起動の
  途中なら順序はそうなるので、1 回引いて諦めず 2 秒待つ（`FindExistingWindow`）。
- **渡せなかったら自分でウィンドウを開く。** 相手が応答しない（メニューを開いたまま
  固まっている、名前を残して落ちた）ときに、**ダブルクリックに何も起きないのが
  一番悪い。** 送信は `SendMessageTimeoutW` + `SMTO_ABORTIFHUNG` ─ 素の
  `SendMessage` だと、固まった Kite が以後の全起動を道連れにする。
- **前面に出すのは受け取った側。** 送る側が先に `AllowSetForegroundWindow` を呼ぶ
  （その権利を渡せるのは今それを持っているプロセスだけ）。**パスが 1 つも無くても
  前面に出す** ─ 起動しても何も起きないように見えるのが一番困る。
- **渡されたパスは `[ui] new_tab_position` に従わない。** 中身は 2 つ目の起動の
  コマンドライン引数そのもので、起動時の引数と同じ扱い ─ 並べて開くよう頼まれた
  順がそのまま並びになる（`after_current` だと 1 枚ずつ手前に入って逆順になる）。
- **ミューテックスは終了まで持つ**（`main_win.cpp`）。先に閉じると、終了処理の最中の
  起動が「誰も居ない」と判断して 2 つ目の本体を立ち上げる。

### 既定のファイルマネージャーとして登録

設定画面（`Ctrl+,`）の**「システム」→「既定のファイルマネージャー」**で、`Win+E` と
フォルダを開く操作を Kite に向ける。レジストリの綴りを知っているのは
`platform/win/WinDefaultManager.cpp` だけで、`core` が受け取るのは `DefaultManager`
（未対応・未登録・この exe・別の場所の Kite）という 4 つの答えだけ。

- **コマンドではなく設定の行。** これは動作ではなく**状態**で、「Kite は既定か」には
  打鍵より長生きする答えがある ─ 行はそれを値として出せるが、パレットの項目は出せない。
  **コマンドにすると必ず嘘をつく**: トグル 1 つならラベルが半分の確率で外れ、2 つに
  割れば «今どちらなのか» を押す前に知っている人しか正しく選べない。**キー割り当ても
  持たない** ─ 一度きりの操作に和音を割く理由が無い。
- **設定画面で唯一、確認を挟む行**（`PromptKind::ConfirmDefaultManager`）。他の行は変えた
  ことがその場で見えるが、この行の効果は **Kite の外**（`Win+E` とフォルダのダブル
  クリック）に出る ─ 一覧を降りる途中で矢印キーがかすっただけで変わってはならない。
- **確認は打鍵も画面も最上位。** `App::OnKey` は設定画面より先に確認へ渡し
  （渡さないと後ろでカーソルが動き、「はい」がどの行の話か分からなくなる）、
  `AppUi::Paint` は**オーバーレイの後**に帯を描く（オーバーレイは窓全面なので、
  その場で描くと質問が、質問した画面の下に隠れる）。マウスも同じ理由で、確認中の
  設定画面はクリックを受け取らない。
- **行の値は覚えない。開くたび OS に訊く**（`App::CollectSettings`）。実体はレジストリに
  あり、Kite の外で（別のファイラーが、別の Kite が）変えられていても行が嘘をつかない。
  `DefaultManager::Other` は「この exe ではない」ので «いいえ» 側。
- **この行だけ `settings.ini` に何も書かない**（`App::ApplyPendingSetting` が先に横取り
  する）。他の行と同じ «反映して書き出す» 道に乗せると、OS 側にしかない値の写しが
  ini に生まれる。
- **確認を取り消したら、動かした行も戻す**（`App::SyncDefaultManagerRow`）。OS が断った
  ときも同じ ─ 起きなかったことを行が主張してはならない。

**書くのは 4 つのキーで、全部 `HKCU\Software\Classes` の下。**

| キー | 何のため |
| --- | --- |
| `Folder\shell\open\command` | **フォルダを開く動詞の本体** |
| `Directory\shell\open\command` | 同上（より具体的なクラス側） |
| `CLSID\{52205fd8-…}\shell\open\command` | `Win+E` |
| `CLSID\{52205fd8-…}\shell\opennewwindow\command` | 同上（今の Windows はこちらを使う） |

- **フォルダを開いているのは `Folder` であって `Directory` ではない。** `Directory` だけを
  書いた版を実際に試して、`Win+E` は Kite になったのに**デスクトップのフォルダは
  エクスプローラーで開いたまま**だった ─ 実機のレジストリを見ると
  `HKCR\Directory\shell` に `open` が無く、しかも既定の動詞は `none`。フォルダを開く
  動詞は上位クラスの `Folder` から継承されていて、その中身は `Explorer.exe` +
  `DelegateExecute`。**`Directory` も残す**のは、より具体的なクラスを先に見る実装が
  あれば先に Kite を見つけるから（キー 1 つの代金）。
- **CLSID は `Directory` / `Folder` とは別の仕掛け。** 前者を書かないと `Win+E` だけ
  エクスプローラーのまま残る。`open` と `opennewwindow` の両方を書くのは、どちらが
  使われるかが版によって違うから ─ どちらも同じ控えで戻るので、当てにいく理由が無い。
- **`Folder` は上位クラスなので、シェル自身の場所も通る** ─ コントロール パネル、PC、
  ネットワークの場所。そこには渡せるパスが無く、`%1` には**解析名**が届く。だから
  `vfs::FromCommandLine` が要る ─ `::{CLSID}` で始まる引数に `virtual:` を付けるだけで、
  すでに在る仮想フォルダの列挙にそのまま乗る。**付けないと「場所が利用できません」**の
  窓が開く。
- **`%1` を付けるのはフォルダの動詞だけ。** `Win+E` は何も渡さないので、付ければその
  2 文字がそのまま引数として届く。
- **`DelegateExecute` は空文字で潰す。** `HKCR` は `HKCU` と `HKLM` を**値ごとに**重ねるので、
  既定値だけを上書きしたキーからは Windows 同梱の `DelegateExecute` がそのまま見え続ける ─
  実際に走るのは書いたコマンドラインではなく**そちらのハンドラ**になる。
- **`HKCU` にだけ書く。昇格は求めない。** ファイラーを 1 つ入れるために昇格を求めるのは、
  そのファイラーが何をするか分からない人にとって最悪の第一印象になる。
- **付けたものは全部外せる。** 書く前の状態（キーが在ったか、在ったなら既定値と
  `DelegateExecute` は何だったか）を `HKCU\Software\Kite\DefaultManagerBackup` に控えてから
  書き、解除はその控えを読んで戻す。**控えが指すのは «Kite が触る前» の状態**なので、
  既に登録済みの状態では取り直さない ─ 取り直すと Kite 自身が書いた値を「元の値」として
  覚え、解除が Kite を書き戻す。**控えが 1 つも無ければ**（古い版が書いた、手で書いた）
  **Kite を名指しているキーだけ**を消す ─ 他のアプリの登録を消すのは誰も頼んでいない。
- **控えは «何番目» ではなくキーのパスを持つ。** 表が変わった版が古い登録を解除しても、
  実際に書き換えられたキーがそのまま戻る。
- **途中で失敗したら、そこまでを戻してから false を返す。** 半分だけ書き換わったレジストリは
  登録済みでも未登録でもない。戻す道は解除と同じ 1 本（`RestoreAll`）。
- **すでにその状態なら何も書かず理由を言う**（`App::SetDefaultManager`）。
  **`DefaultManager::Other`（別の場所の Kite）は「すでに登録されている」ではない** ─ そこへ
  付け替えるのがこの行の仕事。解除も許す（控えが指すのは Kite が触る前の状態なので、
  どのコピーが頼んでも戻す先は同じ）。
- **起動時に 1 行だけ言う。** exe を移すと登録は死ぬ（ポータブル運用で zip を展開し直した
  後の形）のに、画面はそれを言う機会を持たない ─ **同じ名前の exe が別の場所から**登録されて
  いるときだけステータス行に出す。**他のアプリが登録されている場合は言わない** ─ それは
  事故ではなく利用者の選択で、起動のたびに「Kite は既定ではありません」と言うのはただの
  押し売りになる。
- **書き換えたら `SHChangeNotify(SHCNE_ASSOCCHANGED)` を投げる。** 投げないと、次のログオン
  まで古い関連付けのまま動き続ける。
- **単独ウィンドウでも書く。** 「2 枚目のウィンドウは何も書かない」は設定とセッション
  （起動時の写しでしかないもの）の話で、これは利用者がその場で明示的に頼んだ 1 回の操作。
- **エクスプローラーのウィンドウの中は取れない。** デスクトップも含め、シェルのビューは
  フォルダを**自分のプロセスの中で**開く ─ レジストリの動詞を通らないので、そこを取るには
  explorer.exe に読み込まれる DLL が要る。それはシェル拡張を別プロセスへ追い出した
  この設計と正面からぶつかる（「シェル拡張の隔離」）。

### 元に戻す

`Ctrl+Z`（`Cmd::Undo`）が戻すのは**名前の変更・新規作成・移動・コピー**。履歴は
`core/app/UndoStack`（OS にも描画にも触れない）が持ち、逆操作は `App::DoUndo` が
`IFileSystem` を通して行う。

**シェル任せにはできない。** `FOF_ALLOWUNDO` はシェルの Undo スタックに積むが、そこを
呼ぶ公開 API が無い。しかも名前の変更は `MoveFileExW` なのでそのスタックに載らず、
シェル任せは「`F2` で付けた名前を戻す」を最初から取りこぼす。

- **ごみ箱への削除は戻せる**（`UndoKind::Delete`）。逆操作はシェルの `undelete` 動詞で、
  覚えているのは消される前のパス（詳細は「`Ctrl+Z` で削除を戻す」）。
- **完全削除だけは戻せない。履歴に「戻せない印」を積んで、そこで止まる**
  （`UndoKind::Erase`）。**印を積まずに黙っていてはならない** ─ 次の `Ctrl+Z` がそれを
  飛び越え、消えたファイルはそのままに、その前の名前変更だけを巻き戻す。印を積むときは
  **その下の履歴を捨てる**（`UndoStack::Push`）─ どのみち届かないので、残すのは
  «届いたら害になるもの» を抱え続けることにしかならない。**ごみ箱への削除では捨てない。**
- **触ってよいのは「操作の前に無く、後に在る」ものだけ**（`App::RecordTransfer`）。
  競合したときシェルは別名を付けるか上書きするかで、どちらの場合も転送先の名前は
  元の名前とは限らない。`CopyTo` の前に転送先の有無を控えておき、後でもう一度訊く
  ─ **控えずに «元の名前» を消しに行くと、元から在ったファイルを消す。**
- **コピーと新規作成の取り消しはごみ箱へ入れる**（完全削除ではない）。1 時間前に
  作ったフォルダには中身が入っていることがある。取り消しは削除キーではない。
- **移動の取り消しは元のフォルダごとにまとめて呼ぶ。** 1 件ずつ `CopyTo` すると、
  シェルの進捗ダイアログが件数ぶん開いて閉じる。
- **逆操作は履歴に積まない。** Redo は無いので、積むと `Ctrl+Z` の 2 回目が元の操作を
  やり直すことになる。
- **戻せなかった項目も履歴からは外す。** 対象が外から消された・名前を変えられた、
  という entry は、もう一度試したところで戻せるようにはならない。

### コピーと切り取りの合図

`Ctrl+C` と `Ctrl+X` は、ステータス行に**別々の言葉**を出す（`ui.copied_files` /
`ui.cut_files`）。クリップボードは入ったことを何も言わない ─ 一覧の見た目も変わらない
ので、**入ったのか、入ったとして「コピー」なのか「切り取り」なのかが画面のどこにも
出ていなかった。** 貼り付けて初めて分かる、では遅い。

- **同じ文言にまとめない。** 知りたいのは「今クリップボードが持っているのはどちら
  なのか」なので、両方が「コピーしました」では答えになっていない。
- **`Ctrl+Shift+C`（パスのコピー）は今までどおり `ui.copied`。** あちらが入れるのは
  テキストで、貼り付け先も貼り付け方も別のもの。
- **入れられなかったときは入れられなかったと言う**（`ui.clipboard_failed`）。
  `SetClipboardFiles` / `SetClipboardText` の戻り値を捨てて「コピーしました」と
  出していた頃は、他のアプリがクリップボードを掴んでいて失敗しても同じ表示だった
  ─ 何も言わないより悪い。

**切り取った項目はエクスプローラーと同じく薄く描く**（`App::IsCut` →
`AppUi::PaintList` の `ink`）。ステータス行の言葉は次の操作で消えるので、
**`Ctrl+X` を押したことが画面に残るのはここだけ。** 貼り付ける前に「何を切り取って
あるか」を確かめる手段が他に無い。

- **クリップボードは自分が何を持っているか教えてくれない**ので、`App` が
  `cutPaths_` に覚える。**`Ctrl+C` でも捨てる** ─ クリップボードが別のものに
  変わった以上、薄いままの行は嘘になる。捨てるのは他に、貼り付けた後（対象はもう
  動いている）と `Escape`（`Cmd::SelectNone`。エクスプローラーで切り取りを取り消す
  操作がこれ）。
- **ウィンドウがフォーカスを取り戻したら突き合わせる**（`App::SyncCutMarks`）。
  裏で別のアプリがコピーしていれば切り取りはもう無い。**読めなかったときも捨てる**
  ─ 一時的に掴まれているだけのことがあるが、薄さが消えるのと嘘が残るのとでは
  前者のほうが安い。
- **行ごと薄くする。行に色を敷かない。** 選択とカーソルがすでに同じ行について
  別のことを言っているので、3 つ目の塗りを足すと読み取れなくなる。名前だけでなく
  アイコンと右の列も同じ率で薄くする ─ 片方が明るいままだと、1 行が 2 つの答えを
  持っているように見える。
- **`IsCut()` は毎フレーム全行から呼ばれる。** `cutPaths_` を並べておいて二分探索
  するのはそのため（1 万件の切り取りは `Ctrl+X` 1 回で作れる）。

### 同じフォルダへの貼り付け

**コピー元と同じフォルダへ貼り付けたら複製を作る**（`App::DuplicateInPlace`）。名前は
`a.txt` → `a_copy.txt`、2 つ目から `a_copy2.txt`（`path::DuplicateName`）。
**これはシェルに任せられない衝突** ─ ぶつかっている相手が「いまコピーしている当の
ファイル」なので、どちらを残すかという問いが成立しない。`SHFileOperation` はバッチごと
断るので、**同じフォルダでコピーして貼り付けると何も起きなかった**（利用者から報告
された形がこれ）。

- **名前は Kite が決めてからシェルに渡す**（`IFileSystem::CopyAs` →
  `FOF_MULTIDESTFILES`）。`FOF_RENAMEONCOLLISION` でシェルに解決させると
  **出来上がった名前が誰にも分からない** ─ 取り消し履歴が触ってよいのは「操作の前に
  無く、後に在る」ものだけなので（上の「元に戻す」）、名前を知らないままでは
  `Ctrl+Z` がその複製を指せなくなる。ここは名前が先に決まっているぶん、履歴は
  今までどおり積める。
- **`CopyTo()` では書けない。** あちらが言えるのは行き先のフォルダだけで、置くときの
  名前は元のままになる ─ 同じフォルダへ向ければ必ずぶつかる。`CopyAs()` が別に在るのは
  そのためで、**1 回の `SHFileOperation` にまとめる**（1 件ずつ呼ぶと、複数を複製した
  ときに進捗ダイアログが件数ぶん開いて閉じる）。
- **印は `_copy` に固定。ASCII で、空白を入れない**（`path::kCopySuffix`）。
  **エクスプローラーの `- コピー` を真似していない唯一の場所**なので理由を残す ─
  (1) **表示言語で変えてはならない。** ディスクに残る名前なので、言語を切り替えたら
  同じ操作が別の名前を作ることになる（`ui.*` に置けない理由がこれで、だから i18n の
  表ではなく `path::` の定数に在る）。(2) **空白と多バイト文字を避ける。** この名前は
  コマンドラインやスクリプトへそのまま渡される先で、空白は引用符を、多バイトは
  文字コードの手間を毎回連れてくる。**片方だけ直さないこと** ─ 空白を抜いて
  `コピー` を残しても、言語で名前が変わる問題はそのまま残る。
- **フォルダは名前ぜんぶ。** `backup.2026` の `.2026` を拡張子として切り離さない
  （`Cmd::Rename` が語幹だけを選ぶときと同じ話）。**拡張子は書かれたまま**残す ─
  `path::Extension()` は小文字で答えるので、そこから名前を組み直すと `.TXT` が
  黙って書き換わる。
- **空いている名前はディスクに訊く。同じ回で先に決めた名前も避ける** ─ そちらはまだ
  ディスクに無いので、訊いても「空いている」と答える。**回数に上限を置く** ─ 答えの
  出どころがディスクなので、上限が無ければ UI スレッドで回り続ける。
- **切り取りを同じフォルダへ貼り付けたときは何もしない**（エクスプローラーと同じ）。
  **切り取りの印も残す** ─ 何も起きていないのだから、その印はまだ行き先を待っている。
- **クリップボードが両方を持っていたら 2 つに分ける。** 外から来たものは今までどおり
  `CopyTo`、ここに在るものは複製。**`Ctrl+Z` が 2 回になる**が、それは操作が 2 つ
  あるからで、片方だけを戻せないほうが困る。
- **1 件でも複製できたらステータス行で言う**（`ui.duplicated`）。行は一覧に現れるが、
  それがどこに並ぶかは並べ替えの答えで、しかも**名前を選んだのは Kite** ─ 選んだことを
  黙っていない。

### ステータス行

窓の下辺の 1 行（`AppUi::PaintStatusBar`）。**左は «ここはどういう場所か»**（件数・選択・
絞り込み・ドライブの使用量）、**右は «今何が起きたか»**（変換中の文字列・入力欄の見出し・
直前の操作の報告）。分けてあるのは寿命が違うからで、左は場所が変わるまで動かず、右は次の
操作で入れ替わる。

- **カーソル行の名前は出さない。** 右の最後の控えとして出していたが、やめた ─ 名前は
  行そのものに書いてあり、どの行の話かはカーソルの枠がすでに言っている。同じことを
  2 か所で言うために帯の半分を使っていたことになる。言うことが無ければ右は空でよい。
- **容量は «使用 / 総容量» で出す**（`ui.status_usage`）。空きだけでは多いのか少ないのかを
  言っていない ─ 「空き 40 GB」は 64 GB の USB では危険信号、8 TB の HDD では何でもない。
  総容量を覚えている人にしか読めない数を出すことになる。
- **使用量は引き算で出す**（`totalBytes - freeBytes`）。2 つの数と食い違いうる 3 つ目を
  持ち回らない。`freeBytes` は «この利用者がまだ書ける量»、`totalBytes` は «この利用者が
  書ける総量» で、**クォータを見る同じ組から取る** ─ 総量だけをボリューム全体の値にすると、
  引き算の答えがこの利用者の書いた量より大きくなる。
- **容量は列挙が持ち帰る**（`fs::ListResult` の 2 つ）。訊く先が OS で、冷えた共有では
  列挙と同じだけ待たされるので、**描くたびに訊ける値ではない** ─ 一覧を取りに行く
  ワーカーがすでに同じボリュームに触れているから、そこで 1 回訊いて結果に乗せる。
  一覧を取り直すたび（監視の通知でも `F5` でも）新しくなるので、コピーの前後で数字が動く。
- **総容量 `0` は「まだ訊いていない」なので何も出さない。** 仮想フォルダ・サーバーの
  共有一覧・列挙に失敗した場所は 0 のまま ─ そこに「0 B / 0 B」と出せば、容量が尽きたと
  読める。
- **相手はフォーカスされたタブのボリューム。** 分割していれば左右で別のドライブを
  見ているほうが普通なので、窓に 1 つの答えにはできない。
- **1 行に 2 つ置くので、後のものには前が使い残した幅しか渡さない**（重なった矩形は
  文字も重ねる ─ `F1` の見出し行と同じ話）。**席を先に取るのは右** ─ たった今起きたことの
  答えを切ってはならない。落とす順は容量 → 左ぜんぶで、「来た目的から遠いほう」から。

### 失敗の伝え方

**`SetStatus(err)` を直接呼ばない。** ファイル操作の失敗は `App::ReportFailure` を
通す ─ 「何が失敗したか」（`ui.rename_failed` など）を必ず言い、OS の文言が在れば
その後ろに括弧で足す。`ErrorText()` は**文言を用意していないコードに空文字列を返す**
ので、OS の文言だけを流していた頃は**何も出ないまま操作だけが失敗した**。名前も
一覧も変わらないので、利用者から見えるのは「キーが効かなかった」で、実際には
ロックされたファイルの名前を変えようとして断られている。

- **コピーと移動は別の言葉で言う**（`ui.copy_failed` / `ui.move_failed`）。切り取りの
  貼り付けは元を空にしうるので、どちらが失敗したのかが最初に要る情報になる。
- **元に戻せなかったときも「対象が変わっている」と混ぜない**（`ui.undo_failed`）。
  前者は「もう一度やっても駄目」、後者は「やることがもう無い」で、意味が違う。
- **利用者の取り消しは失敗ではない。** シェルのダイアログで「キャンセル」を押すと
  `SHFileOperation` は `DE_OPCANCELLED` を返す ─ `fAnyOperationsAborted` と同じ
  出来事の別の綴りなので、同じように黙って戻る。
- **`SHFileOperation` の戻り値を `ErrorText()` に渡さない**（`FileOperationError`）。
  0x71 以上はシェル独自の `DE_*` で、Win32 のコードと衝突する ─ `DE_ACCESSDENIEDSRC`
  は 120 で、FormatMessage は「この関数はサポートされていません」と答える。意味の
  同じ Win32 コードへ写してから訊くか、写せなければ番号で出す。

**半分の一覧を全部として出さない。** 列挙の途中で `FindNextFileW` が落ちたら
（USB を抜かれた、共有が切れた）**集めた分を捨てて**エラーにする ─ 途中までの一覧は
「残りは消えた」と読めてしまう。理由は `FindClose` の前に訊くこと（後では上書き
される）。`ListShares` が同じことをしているのと揃えてある。

**「空のフォルダ」と言うのは、本当に空だと分かったときだけ。** クラウドの
`ERROR_CLOUD_FILE_*` を既定の `Error` に落としていると、同期が止まっている
OneDrive のフォルダが「読めませんでした」ではなく空として出る。デバイス系
（`ERROR_DEVICE_REMOVED` など）も同じ理由で `Unavailable` に振ってある。

### 設定の置き場所

**`kite.exe` の隣に `config` フォルダが在ればそこ、無ければ `%APPDATA%\Kite`**
（`WinFileSystem` のコンストラクタ）。配布が zip 1 つで、しかも 2 つの exe が同居して
いないと動かない以上、設定も同じフォルダで完結できるほうが「展開したフォルダが
Kite の全部」と言い切れる。

**判定は起動時の 1 回だけ**で、実行中に決め直さない ─ 読んだ場所と書く場所が食い違えば、
終了時に別のフォルダへ書いて設定が消えたように見える。

**選ぶ規則そのものは `core/app/ConfigDir.h` の `config::Choose()` が持つ。** 候補の列と
「実在するか」だけを受け取る純関数で、列挙も存在確認も呼び出し側の仕事。置き場所の
規則を Windows 実装の中に埋めるとテストできなくなる（`tests/test_configdir.cpp`）。

**在るものを選ぶだけで、無いほうを作らない。** 先頭（exe の隣）を作りに行くと、ポータブル
運用を頼んでいない人の exe の隣にも `config` が生える。どれも無ければ末尾＝`%APPDATA%`
が「作る場所」になる。

**zip には `packaging/config/settings.ini` を同梱する**（`release.yml`）。フォルダが在る
ことが合図そのものなので、**同梱をやめると展開しただけでは `%APPDATA%` 方式に戻る。**
中身はコメントだけでよく、Kite が終了時に全項目を書き直す。

**書けない場所に置かれたら、書けないと言う。** `Program Files` や読み取り専用メディアでは
ini の書き込みが失敗する（x64 なので UAC のファイル仮想化も効かず、単に失敗する）。
設定フォルダへの書き込みは **`App::WriteConfigFile` を通す一本道**にしてあり、失敗は
そこでステータス行に出る ─ **設定フォルダに書くものを増やすときに `plat::WriteTextFile`
を直接呼ばないこと。** 黙って保存されない状態は、保存されないこと自体より質が悪い。
同じ理由で **「保存しました」と答えるのは書けたときだけ** ─ `Ctrl+S` は `SaveAll()` が
false なら `ui.saved` を出さず `WriteConfigFile` の失敗メッセージを残し、`Ctrl+Shift+,` の
確認（`ui.key_settings_saved`）も `keysChanged_` が書き込み成功時だけ立つ。ハイブリッド
にしてあるのも、**`config` を自分で置いた覚えの無い人をこの状態に落とさない**ため。

**見えるのは対話中の書き込みだけ**（初回起動の `keys.ini`、`Ctrl+Shift+,` での割り当て変更、
`Ctrl+S`）。**終了時の `SaveAll()` の失敗は誰にも見えない** ─ ステータス行はもう描かれず、
`IHost` にダイアログを出す手段も無い。それでも実用上気付けるのは、書けない場所に展開
すれば初回起動の `keys.ini` で必ず露見するから（ファイルが無いので必ず書きに行く）。
漏れるのは「一度書けた場所が後から書けなくなった」場合だけ。

**`%APPDATA%` からの移行は入れていない。** 0.1.x までの設定は拾いに行かないので、
`config` を同梱した版に上げると設定は初期状態から始まる。

**単一インスタンスの目印もここに掛かる。** 名前付きミューテックスを固定名にすると、
別フォルダに展開した 2 つのコピーが同じインスタンスとして振る舞い、一方の `config`
だけが使われる。**exe のパスから導いてある**（上の「単一インスタンス」）。

exe 自身の位置は `platform/win/WinPaths.h` に 1 か所だけ。`kite_shellhost.exe` の探索と
`config` の探索が同じ関数を見る ─ 「自分の隣」の数え方が 2 通りあってはならない。

### シェル拡張の隔離

サードパーティの DLL が動く経路は 2 つある ─ コンテキストメニュー
（`IContextMenu`）と、アイコンオーバーレイ（`IShellIconOverlayIdentifier`）。
**どちらも自プロセスで処理しない。**

```
kite.exe                                  kite_shellhost.exe（メニュー用）
  App::ShowContextMenuAt
  → WinShell::ShowContextMenu
    → ShellHostClient  ── 名前付きパイプ ──→  ReadPipeFrame
                                              ShowShellContextMenu
                                                QueryContextMenu
                                                TrackPopupMenu
                                                InvokeCommand
       応答（出せた／実行した／失敗） ←───────  WritePipeFrame

kite.exe                                  kite_shellhost.exe（列挙用）
  App::RequestLoad
  → DirectoryLoader のワーカー
    → WinFileSystem::ListVirtual
      → FolderHostClient  ── 名前付きパイプ ──→  ReadPipeFrame
                                                EnumerateShellFolder
                                                  SHParseDisplayName
                                                  IShellFolder::EnumObjects
       項目の名前・解析名・属性 ←───────────────  WritePipeFrame

kite.exe                                  kite_shellhost.exe（メニュー用）
  App::ActivateEntry
  → WinShell::Open
    → ShellHostClient::Extract ── パイプ ──→  ReadPipeFrame
                                              ExtractShellItem
                                                ResolveItemsInFolder
                                                IFileOperation::CopyItem
       取り出した実ファイルのパス ←───────────  WritePipeFrame
  → ShellExecute（もう普通のファイル）

kite.exe                                  kite_shellhost.exe（アイコン用）
  AppUi::PaintList
  → App::IconFor → IconCache（要求を貯める）
    → WinIconProvider のワーカースレッド
      → IconHostClient ── 名前付きパイプ ──→  ReadPipeFrame
                                              LoadShellIcon
                                                SHGetFileInfo
                                                  （SHGFI_ADDOVERLAYS）
       ID 列 + 新規ビットマップ ←────────────  WritePipeFrame
  → D2DRenderer::UploadIcon
```

送るのはパスと画面座標とテーマだけ、受け取るのは結果の種別と画素と項目の一覧、
そして取り出したファイルのパスだけ。
**PIDL もハンドルも渡らない** ─ ホストが入れ替わっても意味が変わらない文字列しか
持たせない、というのがこの書式の唯一の設計方針で、ごみ箱の項目を指すのに
「フォルダ + 解析名」という遠回りをしているのもそれを崩さないため（下記）。書式は
`platform/win/ShellHostProtocol.h`（Windows 非依存。`tests/test_hostproto.cpp` が
直接検証している）。**書式を変えたら `kMagic` も変えること** ─ 版の食い違うホストが
残っていたときに、誤って解釈するのではなく握手で失敗させるためにある。

**ホストは 3 つ起動する。** メニューのホストは表示中ずっと `TrackPopupMenu` の中に
いるので、接続を共有するとメニューを開いている間アイコンが 1 枚も更新されず、
仮想フォルダも開けない。起動と寿命管理は `ShellHostProcess` が 3 つとも提供する。
どれも最初の要求まで起動しないので、右クリックもアイコンも仮想フォルダも使わない
起動ではプロセスが 1 つも増えない。

知っておくべき点:

- **フォルダのメニューは 2 種類あり、取り違えると別の献立が出る。** 項目のメニュー
  （`GetUIObjectOf`）は、そのフォルダを**対象にする**動詞が並ぶ ─ 親フォルダの一覧で
  右クリックしたときのもので、開く・送る・コピー・削除・7-Zip・以前のバージョンの復元
  など。背景のメニュー（`IShellFolder::CreateViewObject`）はエクスプローラーが一覧の
  余白で出すもので、答えるのは**フォルダの中の空間**についてだけ ─ 新規作成と貼り付け、
  それに「ここで開く」系。要求の `background` がこれを切り替える。

  **`Cmd::FolderContextMenu` は項目側**。名前のとおり「このフォルダ自身」が対象で、
  背景側を出していた時期があるが、それは献立が半分無いメニューになる ─ 開く・送る・
  コピー・削除がどれも背景側には登録されていない。**背景側にするのは一覧の余白を
  右クリックしたときと、選択が空のとき**（`App::ShowBackgroundContextMenu`）。余白は
  どの項目でもないので、カーソル行に答えさせない。背景側では `InvokeCommand` に
  `lpDirectory` も渡す（「新規作成」の行き先になる）。
- **`CMF_EXTENDEDVERBS` を既定で付けない。** 拡張動詞はエクスプローラーが `Shift`
  押下時にだけ見せる集合で、拡張機能の側も「普段は出さないもの」を置く前提で登録して
  くる ─ TortoiseGit の「Git Clone...」はここにあり、working copy の中で「ここへ
  クローン」を勧められるのがこれ。「全部を 1 アクションで出せたほうが速い」と既定を
  拡張にしていた時期があり、その帰結がこの不具合だった。実測（同一フォルダ、
  TortoiseGit 導入環境）:

  | | 通常 | 拡張 |
  | --- | --- | --- |
  | 項目メニュー | Clone なし | Clone あり |
  | 背景メニュー | Clone なし | Clone あり |

  **つまり `background` の選択はこの件と無関係** ─ かつて `Cmd::FolderContextMenu` を
  背景側にして止めようとしたが効かなかった。Kite も `Shift` に従わせてある
  （`Menu` / `Ctrl+Menu` が通常、`Shift+F10` / `Ctrl+Shift+F10` が拡張）。
- **メニューが閉じるまで `ShowContextMenu` は戻らない。** ただし待っている間も
  `ShellPipe` が呼び出し側のメッセージを回すので、ウィンドウは再描画され続ける。
  クライアント領域への入力だけは捨てる（同一プロセスの `TrackPopupMenu` と同じ挙動）。
  `WM_APP` 以降は**あえて捨てずキューに残す** ─ 列挙完了通知をここで処理すると
  `App::ShowContextMenuAt` の内側から `App` に再入する。
- **ホストは最初のメニュー要求で起動する。** 起動パスに COM を置かない方針のため。
  一定時間使われなければ自分で終了し、拡張 DLL の分のメモリを返す。落ちた場合と
  区別せず、次の要求で黙って起動し直す。
- **ダイアログの親は Kite 本体のウィンドウ。** `TrackPopupMenu` はホストの隠し
  ウィンドウ（呼び出しスレッド所有が必須）だが、`InvokeCommand` に渡す `hwnd` は
  Kite 側にしている。隠しウィンドウを親にすると削除の確認や進捗が背後に回る。
- **メニューの明暗は要求ごとに送る。** OS の設定ではなく Kite 自身のテーマに従わせる
  （明るいデスクトップで Kite だけ暗くしている場合、メニューだけ白く光るのが一番
  目に付く）。ホストは常駐しテーマは実行中に変わるので、起動時ではなく毎回送る。
  実体は uxtheme.dll の非公開エクスポート（序数 104 / 133 / 135 / 136）で、
  `SetShellMenuDarkMode` が包んでいる。**序数の意味が変わった 1903 より前では
  何もしない** ─ 誤った関数を呼ぶくらいなら白いメニューのほうが安全。
- **ホストはジョブオブジェクトに入れる。** Kite が強制終了されても道連れにするため。
  ただし `SILENT_BREAKAWAY_OK` 付き ─ メニューが起動したプロセス（7-Zip の展開など）
  までジョブに入れてしまうと、Kite 終了時に一緒に殺してしまう。
- **アイコンの要求にだけ時間制限がある。** `SHGetFileInfo` は中断もタイムアウトも
  できないので、オーバーレイハンドラが返らなければそのスレッドは永久に止まる。
  自プロセスなら再起動以外に手が無いが、別プロセスなので読み取りを打ち切って
  ホストごと捨てられる。失うのはそのバッチ分のアイコンだけで、次の要求で
  黙って起動し直す。**これがアイコンを別プロセスに置いた最大の理由。**
  メニュー側に同じ制限を付けていないのは、開いたままのメニューを勝手に消す方が
  実害が大きいため。
- **アイコンはまとめて要求し、ビットマップは重複排除して返す。** オーバーレイの
  ぶんアイコンはパスごとに変わりうるので要求はパス単位だが、実際の絵は数種類しか
  無い。ホストが画素のハッシュで ID を振り、同じ絵は 2 度送らない。1 万件の
  フォルダでも転送されるのは数枚で済む。
- **その ID は 1 接続の中でしか意味を持たない。** ホストは接続ごとに 1 から振り直すので、
  アイドル終了（60 秒）や読み取り打ち切りでホストが入れ替わった瞬間、`kite.exe` が
  覚えている「パス → ID」はすべて別の絵を指す。`ShellHostProcess::generation()` が
  変わったら `IconCache::Forget()` とアップロード済みビットマップを捨てる
  （`WinIconProvider::Pump` の `stale`）。捨て忘れると **画面に出たままの行のアイコンが
  静かに別のファイルのものへ入れ替わる** ─ ドライブに PowerShell スクリプトのアイコンが
  付く、といった形で出る。60 秒放置すれば必ず起きるので、アイコン周りを触ったら
  「放置 → 別フォルダを開く → 元の行を見る」まで確認すること。
- **捨てるのは新しいホストの最初の応答と同時に。** そのときホストの重複排除表も空で、
  応答には配った絵がすべて入っているので、こちらを捨てて向こうを入れれば一致する。
  ずらして捨てると **ホストは「もう送った」と思っている絵を二度と送らない** ので、
  その ID の行は接続が切れるまで永久に空になる。同じ理由でその応答（`done`）は必ず
  取り込むこと ─ `Forget()` は取得中の項目に手を触れない（その結果はもう新しい
  ホストのものだから）。
- **`virtual:` の付いた文字列をプロセスの外へ出さない**（`win::ToShellPath`）。
  ホストもシェルもその前置を知らないので、受け取れば「そんなファイルは無い」と
  答える。kite.exe からシェルへ出ていく経路 ─ メニュー・アイコン・列挙 ─ は
  すべて `platform/win/VirtualNames.h` を通す。
- **オーバーレイは古くなる。** 絵はファイルの状態（コミット済み、同期済み、
  ロック中）を映すが、シェルからの通知は受けていない。利用者の操作で一覧を
  取り直すとき（`App::RefreshFocused`）に `IIconProvider::Invalidate()` で
  聞き直す。取り直しの間は直前の ID を返し続ける ─ 一旦 0 に戻すと、
  更新のたびに全行がベクタ描画へ点滅して戻る。

## 追加のしかた

**コマンドを足す** — `KITE_COMMAND_LIST`（`core/input/Commands.h`）に 1 行、
`App::Execute` に `case` を 1 つ、両言語テーブル（`core/i18n/Strings.cpp`）にラベル、
たいていは `core/input/KeyMap.cpp` に既定バインドも足す。ラベル漏れは
`test_strings.cpp` が、和音の重複は `test_keymap.cpp` が落として教えてくれる。

**既定のキー割り当てを変える** — 直すのは `core/input/KeyMap.cpp` の `kDefaults` **1 行だけ**。
`LoadDefaults()` も `KeyMap::DefaultChordsFor()` もそこを読むので、実行時の答えは自動的に
揃う。**和音を書き写した場所を増やさないこと:**

- **表示文字列に和音を直接書かない。** 案内が要る画面は `KeyMap::ChordText(Cmd)` に
  «今まさに割り当てられている和音» を訊き、`{0}` で差し込む（`F1` の見出し・設定画面の
  脚注・`keys.ini` の見出しがそうしている）。既定が動いた瞬間に嘘になるだけでなく、
  **利用者が割り当てを変えた環境では最初から嘘**になる ─ `Ctrl+F1` を書き写していた
  ころの `F1` の案内が、まさにその状態だった。割り当てが無いときは和音の入るはずの
  場所を空けず、`*_unbound` の別の文で言い切る。
- **画面が自前で見るキーは別**（`KeyEditor` の `Enter` / `Ctrl+Enter` / `Ctrl+R` など）。
  キーマップを通らず割り当ても変えられないので、書いてあるとおりに動く ─ こちらは
  直接書いてよい。
- **テストは `KeyMap::DefaultChordsFor(Cmd)` を通す。** 和音を直書きしたテストは、
  既定を動かした日に「キーが違う」で落ちる ─ 試したいのはたいてい振る舞いのほうで、
  どのキーに乗っているかではない。
- 検査は `test_keymap.cpp`（書き写しが復活していないか）と `test_app.cpp`（`keys.ini` の
  見出しが実際の割り当てを名乗っているか）。ドキュメントの和音は自動では追随しないので、
  既定を動かしたら `README.md`・`CLAUDE.md`・`docs/` も直すこと。

**ユーザーに見える文字列を足す** — `core/i18n/Strings.cpp` の `kEn` と `kJa` の両方に
追加する。それ以外の場所に表示文字列を直書きしない。

**設定画面に項目を足す** — `SettingId`（`core/app/SettingsEditor.h`）に 1 つ、
`SettingsEditor.cpp` の `kSettings` 表に 1 行（区画・ラベルキー・選択肢）、
`App::CollectSettings` と `App::ApplySetting` に 1 つずつ `case`、両言語テーブルに
ラベルと選択肢の文字列。**選択肢は必ず添字で表せる形にすること** ─ 数値の項目は
今のところ文字サイズだけで、`SettingOptionText()` が「数値ならパーセント」と
決め打ちしている。2 つ目の数値項目を足すなら、そこに単位を持たせてから。

**画面に出す設定と ini だけの設定を分ける基準** — 画面に出すのは「切り替えたことを
その場で目で確かめられるもの」だけ。`[theme.*]` の配色 40 項目やサイドバーの並びまで
並べれば、探すのに時間の掛かる一覧になる（それらは画面上の操作そのもので変えられる）。
**`settings.ini` は引き続き全項目の正**で、画面はその一部への近道でしかない。

**例外は「既定のファイルマネージャー」の 1 行だけ**（`SettingId::DefaultManager`）。
実体が `settings.ini` ではなく OS 側にあり、効果も Kite の外に出るので上の基準に
掛からないが、**それでも行にしてある** ─ これは動作ではなく状態で、「Kite は既定か」に
答えられるのは値を出せる行だけだから（詳細は「既定のファイルマネージャーとして登録」）。
**この例外を増やすなら、確認を挟むところまで込みで真似すること** ─ 変えたことが
画面から見えない行は、それが無いと «キーが効かなかった» と区別が付かない。

**テーマ色を足す** — `Theme` にフィールド、`Theme::Dark()` と `Theme::Light()` に既定値、
`Theme::ApplyIni` に `ReadColor` の行を追加。ダーク既定は**無彩色**で通している
（`test_theme.cpp` が RGB のばらつきを検査する）。色相を持つのは 3 つだけ:

| | なぜ色が要るか |
| --- | --- |
| `textError` | 明度では「これは失敗だ」と言えない |
| `accent` / `paneFocusBorder` / `cursorBorder` | 「キーボードがここにいる」。**Windows と同じ青**（下記） |
| `textSelection` | 入力欄の選択。OS の作法どおりの水色（下記） |

**フォーカス色は Windows のアクセント色に合わせる** ─ ダークは `#4CC2FF`（WinUI が
暗いテーマのフォーカス枠に使う明るいほう）、ライトは `#0078D4`（既定のアクセント色
そのもの）。「キーボードがここにいる」は OS 共通の語彙なので、ここだけ独自の青に
すると読み替えが要る。**彩度を落とした青を使っていた時期があるが、灰色の画面の中で
ただの灰色に見える**という当然の結果になった（`test_theme.cpp` は「無彩色でないこと」
を下限で検査する ─ 上限は純シアンを止めるためだけにある）。

**一覧の行の選択（`rowSelected`）は無彩色のまま。** 「選んである」と「キーボードが
ここにいる」は別の問いで、同じ色にすると区別が付かなくなる。**入力欄の選択だけが
別扱いなのは、比べる相手が違うから** ─ どの OS でもテキストの選択は水色で、しかも
入力欄の地は一覧の地より狭く短い。ここを灰色にすると「今この行は入力欄なのか」が
読み取れない。文字色は変えない（1 回の `DrawText` で描くため）ので、**通常の文字色が
乗って読める明度**にすること。これも `test_theme.cpp` が検査する。

色を足したくなったら、まず明度で表現できないか考えること。

**言語を足す** — 再ビルド不要。利用者が設定フォルダに `lang.<code>.ini` を置けばよい。
組み込みテーブルは `en` と `ja` のみ。

## 開発手順

コードを変更したら、次の 2 つは必ず満たしてから完了とすること。

### 1. ヘッダに Doxygen コメント（必須）

`src/**/*.h` に宣言を追加・変更したら、必ず Doxygen コメントを書く。記述は日本語。
対象はヘッダのみで、`.cpp` 内部の関数は対象外。

関数・メソッドには最低限これを書く:

- `@brief` … 何をするか 1 行。必須
- `@param[in]` / `@param[out]` / `@param[in,out]` … 全引数に必須。方向指定を省略しない
- `@return` … 戻り値がある場合は必須。「失敗時 nullptr」のような異常系まで書く

型・構造体には `@brief` を、public メンバには `///<` を付ける（private メンバは
`EXTRACT_PRIVATE = NO` のため任意。自明でないものだけでよい）。加えて必要に応じて:

- `@pre` … 事前条件（`Session::PaneInDirection()` が直前のレイアウトを要求する、など）
- `@note` … スレッド制約や呼び出し順の注意
- `@todo` … 既知の未対応・制限。憶測で書かず、実際に未実装な箇所だけに書く

基底クラスと同じ説明でよい override は `@copydoc` で参照する。書式の見本は
`core/fs/FileSystem.h` と `ui/Renderer.h`。

**落とし穴**: `///<` は複数宣言子の行では最後の 1 つにしか付かない。
`float l = 0, r = 0;  ///< 端` と書くと `l` が未文書化になる。1 行 1 宣言に分けること。

チェックはリポジトリのルートから:

```bash
doxygen docs/Doxyfile
```

出力は `build/doxygen/html/index.html`、`@todo` の一覧は同 `todo.html`。
`WARN_IF_UNDOCUMENTED` と `WARN_NO_PARAMDOC` を有効にしてあるので、`@brief` や
`@param` の書き漏れは警告として出る。**警告ゼロを維持すること。** ただし
`The selected output language "japanese" has not been updated` の 1 件だけは
Doxygen 側の翻訳が未更新なことによる無害な通知で、消せない（無視してよい）。

doxygen が無ければ `winget install DimitriVanHeesch.Doxygen`。クラス図は
Graphviz に依存するが、未インストール環境でも通るよう `HAVE_DOT = NO` にしてある。
図が欲しければ `winget install Graphviz.Graphviz` を入れて `YES` に変える。

### 2. core 層は単体テストを書く（必須）

`src/core/` に振る舞いを追加・変更したら、`tests/` にテストを足すこと。
`src/ui/` も抽象 `Renderer` の上にあるのでテスト可能で、判断ロジックを含むなら
同様に書く。`src/platform/` は OS に触れるため対象外。リンクするのは `kite_core`
だけ ─ 上の「アーキテクチャ」のとおり、この依存関係が分離の防波堤を兼ねている。

OS 境界のフェイクは `tests/Fakes.h` に揃えてある（`FakeFileSystem`、`FakeShell`、
`FakeHost`、`FakeWatcher`、`FakeIconProvider`）。`tests/FakePlatform.cpp` が `core/base/Platform.h` の
5 つの自由関数をメモリ上で実装するので、実ディスクにも実時計にも触れずに `App` を
端から端まで動かせる。非同期ローダーは実スレッドで動くため、ウィンドウと同じように
`test::PumpUntilSettled()` でポンプすること。

`FakeRenderer` も同じ場所にある。塗った矩形と色を覚えるので、**ウィンドウ無しで
1 フレーム描いて「どこに何色を塗ったか」を検査できる**（`tests/test_appui.cpp` が
ホバー表示をこれで見ている）。字形は捨てるが、**描いた文字とその外接矩形（`Text::ink`）は
残す** ─ どこに置いたかはレイアウトの結果であり、「2 つの文字列が重なっていないか」は
ここでしか検査できない。

- **`ink` は渡された矩形ではない。** 右揃え・中央揃えを解いた後の実際の墨の位置。
  右揃えの行は広い矩形の右端しか使わないので、矩形どうしの重なりを見ると嘘の警告に
  なる。
- **重なりは `TextsAfterFill(overlayScrim)` で、そのオーバーレイが描いたぶんだけ**
  取り出して見る。後ろの画面は同じ座標にすでに描かれている。
- `Overlaps()` に 0.01 の遊びがあるのは、行を足し算で積むと下端と次の上端が最下位
  ビットだけずれるため。
- **シェルアイコンは識別子と矩形だけ覚える**（`Icon` / `IconAt()`）。画素は
  プラットフォームの持ち物なので、検査できるのは「この行は実物を貰えたのか、
  ベクタ描画に落ちたのか」と「2 つの行が同じ絵なのか」の 2 つ。
- **`FakeIconProvider` は最初の問いで答える。** 本物は 0 を返してワーカーに積むが、
  描画の検査に 1 フレームぶんのポンプを毎回足す意味が無い。`SetIconProvider` は
  `Init()` の後でも構わない ─ `App::IconFor` が呼ばれるたびに見に行くので、
  「アイコンが無い 1 フレーム」と「ある 1 フレーム」を同じ器で並べられる。

スイートを足したら `CMakeLists.txt` の `KITE_TEST_SUITES` にも名前を追加する
（ctest に 1 スイート 1 エントリで登録される）。走らせ方は冒頭の「ビルドとテスト」。

判定に使うのは自作の 100 行のハーネス（`tests/TestFramework.h`）。
`KITE_EXPECT` / `KITE_EXPECT_EQ` / `KITE_EXPECT_NE` / `KITE_EXPECT_NEAR` /
`KITE_EXPECT_FALSE` / `KITE_FAIL` がある。

## CI

`.github/workflows/ci.yml` が main への push と PR で回る。手元で確認できることを
先回りするだけなので、落ちたら手元で同じことをすれば再現する。

| 見ているもの | 手元での再現 |
| --- | --- |
| ビルド＋全テスト（`-DKITE_WERROR=ON` で `/W4` の警告を `/WX` に格上げ） | `ctest --preset release` |
| `kite.exe` と `kite_shellhost.exe` が同じフォルダに出るか | `ls build/release/*.exe` |
| `core/` `ui/` `tests/` に Windows ヘッダが無いか | `git grep -nE '#include *[<"]windows' -- src/core src/ui tests` |
| `ShellMenu.cpp` / `ShellIcons.cpp` が `kite` ターゲットに入っていないか | `CMakeLists.txt` の `add_executable(kite WIN32 ...)` を見る |
| Doxygen の警告がゼロか | `doxygen docs/Doxyfile` |
| `*.ps1` が UTF-8 BOM 付きで、PowerShell 5.1 で構文解析できるか | ─ |

`KITE_WERROR` は CI 専用のスイッチで既定 OFF。手元では警告は警告のままにしておく
（作業中のビルドを警告 1 つで止めない）。

ローカルのビルドは Ninja プリセットだが、**CI は Visual Studio ジェネレータを使う**
（`cmake -S . -B build/ci -A x64`）。プリセットは vcvars を要求し、CI では第三者製
アクションへの依存が増えるため。バージョンは決め打ちにしないこと ─ `windows-latest`
が載せる VS は更新される。

Windows しかビルドしないので、**レイヤ分離はテストのビルドでは検査できない**
（`core/` に `<windows.h>` を入れても Windows 上では普通に通る）。上表の grep が
その代わりで、これを消すと分離を守っているものが無くなる。

## リリース

バージョンの正は `CMakeLists.txt` の `project(Kite VERSION x.y.z)` の 1 行だけ。
タグも Release もそこからの派生物なので、**タグは手で打たない。**

```powershell
.\release.ps1 0.2.0
```

やっているのは VERSION の書き換えとコミット・push だけで、あとは Actions が続ける:

```
release.ps1  CMakeLists.txt の VERSION を書き換えて push
  → tag.yml     main への push（CMakeLists.txt が変わったときだけ）を検知して
                v0.2.0 を作り、release.yml を workflow_call で呼ぶ
  → release.yml タグをチェックアウトしてビルド・テストし、
                kite-v0.2.0-win-x64.zip と .sha256 を Release に添付
```

- **`tag.yml` から release.yml へは `workflow_call` で入る。** タグ push の連鎖には
  できない ─ `GITHUB_TOKEN` で起こしたイベントは新しいワークフローを起動しない、
  という GitHub の再帰防止があり、タグだけできて Release が作られない状態になる。
- **手で打ったタグが残っていると自動化に乗らない。** `tag.yml` が「作成済み」と
  判断して何もしないため。`release.ps1` は push 前にこれを検査して止まる。
- **落とすものは zip 1 つ。** `kite.exe` と `kite_shellhost.exe` は同じフォルダに無いと
  コンテキストメニューが出ないので、別々に落とせる形にはしない。`config` フォルダも
  同梱する ─ 詳細は「設定の置き場所」。書庫名にも版番号を
  入れてあるが、展開したあとも両 exe のプロパティに版とコミットが出る
  （`Version.rc.in` から生成される VERSIONINFO）。
- **`.sha256` だけは別に添える。** 中身の一部ではなく書庫そのものについての情報なので、
  書庫の中に入れると自分自身のハッシュを名乗ることになる。**要るのは誤検知の報告のため** ─
  Defender の機械学習分類器は未署名で初見のバイナリを `Trojan:Win32/Bearfoos.B!ml` として
  誤検知することがあり（実際に踏んだ）、Microsoft への報告にこの値が要る。書式は
  `sha256sum` が読める形（ハッシュ・空白 2 つ・ファイル名、改行は LF）。
  **常に zip と 2 つ並べて置くこと** ─ 片方だけ差し替えると、検証した人が合わない
  ハッシュを見る。**署名の代わりにはならない**（有料なので採らない、という判断）。
- **`release.ps1` は UTF-8 BOM 付きで保存する。** `release.bat` が呼ぶ Windows
  PowerShell 5.1 は BOM の無い `.ps1` を CP932 として読むので、BOM を落とすと
  日本語コメントが化けて構文エラーになる（`ci.yml` が検査している）。
- リリースが失敗して作り直したいときは `gh workflow run release.yml -f tag=v0.2.0`。
  既にある Release には成果物を差し替えるだけなので、何度実行してもよい。

## 規約

- 文字列は**全面的に UTF-8**。UTF-16 は `platform/win/` の内部にのみ存在し、
  `WinUtf.h` が境界で変換する。
- コメントは *why* を書く。*what* は書かない。コードの逐語訳をしない。
- インデント 4 スペース、100 桁上限。`.clang-format` が正。
- `/W4 /permissive-` で警告ゼロを維持する。
- サードパーティ依存なし。テストハーネスが 100 行程度の自作なのもこの方針のため。

## すでに踏んだ罠

いずれも実際に時間を溶かしたので記録しておく。

- **`<windows.h>` のマクロ**。`CreateDirectory` と `CreateFile` はマクロなので、
  ファイルシステムのインターフェースは `MakeDirectory` / `MakeFile` という名前にしてある。
  `GetObject`、`SendMessage`、`min`/`max`（`NOMINMAX` 設定済み）も同様に注意。
- **`WIN32_LEAN_AND_MEAN`** は `ole2.h` を `windows.h` から外す。`IDataObject` や
  `IDropTarget` に触れるヘッダは自分で `<objidl.h>` を include すること。
- **`ID2D1HwndRenderTarget` は使えない**。少なくとも手元の Intel ドライバでは
  `EndDraw` が `S_OK` を返しながら画面に何も出ない。40 行の最小再現コードでも同じだった。
  レンダラは D3D11 デバイス → `ID2D1Device` → `ID2D1DeviceContext` → DXGI フリップ
  スワップチェーンという現代的な経路を使っている。「単純化」して戻さないこと。
- **D2D のターゲットはウィンドウを表示した後に作る**。そのため最初の `BeginFrame` で
  遅延生成している。
- **`ReadDirectoryChangesW` のハンドル寿命**。読み取りが未完了のままハンドルを閉じると、
  後から完了通知が届く。`WinDirectoryWatcher` は全ハンドルをワーカースレッドが所有し、
  キャンセル通知が届いてから初めてエントリを解放する。`Watch`/`Unwatch` はコマンドを
  投函するだけ。
- **クラウドのプレースホルダ**。列挙中にファイル内容を読まない。ファイルを直接開かず
  シェルに委ねる。`FILE_ATTRIBUTE_RECALL_ON_OPEN` と `RECALL_ON_DATA_ACCESS` が付いた
  ファイルはクラウドにしか実体がなく、触れるとダウンロードが走る。
- **シェルアイコンは UI スレッドから取らない**（詳細は「シェル拡張の隔離」）。届く
  までは `ui/Glyphs.cpp` のベクタ描画が同じ位置に出るので行はずれない。
  `[ui] shell_icons = false` でシェルを一切呼ばない運用にも戻せる。
- **`\\?\` を付けたパスには OS の正規化が掛からない**。`/` も `.` も `..` も解決されない
  まま渡るので、`C:/foo/../bar` は 200 文字なら開けて 300 文字で開けなくなる。
  `path::ToExtended()` は付ける前に `Normalize()` を通す。規則を core に置いてあるのは
  `config::Choose()` と同じ理由（テストできないため）で、UTF-16 へ変換するのは
  `win::ToExtendedPath()`。**閾値は UTF-16 の符号単位で測る** ─ MAX_PATH がその単位
  なので、バイト数で測ると日本語のフォルダ名を 3 倍に見積もることになる。
  なお `ToExtendedPath()` が `WinUtf.cpp` ではなく `WinPaths.cpp` に居るのは、
  前者が `kite_shellhost.exe` にもリンクされていて、そちらは `kite_core` を持たないため。
- **`small` は `<rpcndr.h>` のマクロ**（`char` に展開される）。`<shlobj.h>` 経由で
  入ってくるので、`bool small = ...` と書くと意味不明な構文エラーになる。
  `<windows.h>` のマクロ群と同じ扱いで避けること。
- **ワーカーの `Wake()` は再描画を意味しない**。`App::PumpLoader` は列挙結果が
  無ければ `Invalidate` せずに戻るため、アイコンだけが届いた場合に画面が更新
  されない。`WM_KITE_WAKE` の側で必ず `Invalidate()` する。
- **`App::SetStatus` は自分で再描画を要求する**。一覧を動かすコマンドは後続の
  `RefreshFocused` でどのみち描き直されるので長く気付かなかったが、画面を何も
  動かさずに答えるだけのもの ─ `Ctrl+C`、`Ctrl+X`、空の `Ctrl+Z` ─ は**次に
  無関係な打鍵が来るまでメッセージが出ない**（「コピーしたのに、カーソルを動かして
  初めて『コピーしました』が出る」という形で報告された）。表示期限のタイマーも
  同じ依存を持つ ─ `WinWindow::Paint` が描くときにしか張らないので、最初の 1 枚が
  来なければ消えるほうも動かない。**呼ぶ側に `Invalidate` を足して回らないこと**、
  それは 1 か所抜けたら同じ不具合が戻る形。
- **`OleInitialize` は 100 ms 以上かかる**。ドラッグ＆ドロップの登録は初回描画から
  200 ms 後のタイマーに逃がしてある。起動パスに戻さないこと。同じ理由で
  `kite_shellhost.exe` も最初のメニュー要求まで起動しない。
- **`wt.exe` は `lpDirectory` を見ない**。`ShellExecuteW` に作業フォルダを渡しても、
  Windows Terminal は各タブをプロファイルの `startingDirectory`（既定は `%USERPROFILE%`）で
  開くので、「ここでターミナルを開く」がホームフォルダで開く。フォルダは `-d` で渡すこと。
  引用は `QuoteArgument`（`WinPaths.cpp`）を通す ─ ドライブ直下のパスは `C:\` で終わり、
  そのまま `"` で閉じると引用符自身がエスケープされる（`Ctrl+N` のコマンドラインと同じ罠で、
  だから 2 か所から同じ関数を呼んでいる）。`cmd.exe` のほうは `lpDirectory` に従う。
- **`CMIC_MASK_UNICODE` は `<shellapi.h>` が必要**。実体は `SEE_MASK_UNICODE` で、
  `<shlobj.h>` だけでは「定義されていない識別子」になる。
- **`MsgWaitForMultipleObjects` に `MWMO_INPUTAVAILABLE` を足すと暴走しうる**。
  キューに残したままのメッセージがあると毎回即座に返るため、意図的に処理しない
  メッセージがある待機ループでは 100 % CPU になる。`ShellPipe.cpp` はこのフラグを
  使わず、代わりに 100 ms ごとに自力で起きる。
- **`WM_KEYDOWN` を消費しても `WM_CHAR` は止まらない**。`TranslateMessage` は
  `DispatchMessageW` の**前**に走るので、ハンドラが 0 を返すころには文字のメッセージは
  もうキューに入っている。ショートカット設定で「D」を割り当てると、取り込みが終わった
  直後に `d` が絞り込み欄へ落ちて一覧が飛ぶ、という形で出た。`KeyEditor` は取り込みを
  終えた打鍵に印を付け（`swallowNextChar_`）、続く 1 文字だけを捨てる。**印は次の
  `HandleKey` で必ず落とす** ─ 文字を伴わないキー（`Ctrl+Alt+Shift+F9` など）で
  割り当てた場合、印が残ったままだと次に打った 1 文字が消える。
- **開発者シェルの既定は x86**。`_purecall` が未解決だと言ってリンクが落ちるのはこれ
  （続けて `operator new` なども並ぶ）。**症状がコードのどこも指さない**のが厄介で、
  x64 のオブジェクトに対して x86 の `libvcruntime.lib` が見つかり、machine 不一致で
  黙って捨てられているだけ。**ビルドディレクトリは共有される**（`build/Release` は
  `build.ps1` と `--preset release` で同じ場所）ので、x64 で configure した木を x86 の
  シェルでビルドするとこの形になる。CMakeLists が `VSCMD_ARG_TGT_ARCH` を見て configure
  で止めるようにしたが、**止まる前に作られた `build/` が残っていれば消してからやり直す**こと。
- **テストマクロはオペランドをコピーする**。`KITE_EXPECT_EQ` は `auto&&` ではなく
  `const auto` を使う。生存期間延長はメンバ呼び出しを貫通しないため、
  `container().front()` を参照で束縛するとダングリングし、比較が静かに壊れる。

## 現時点の弱点

- **デスクトップとエクスプローラーの窓の中でフォルダを開いたときが未検証。**
  `Directory` だけを書いていた版では取れず（`Folder` に直した経緯は「既定のファイル
  マネージャーとして登録」）、`Folder\shell\open\command` を上書きした今の版は
  **実機で確かめていない。** 取れなかった場合、残る道は explorer.exe に読み込まれる
  DLL しかない ─ シェルのビューは自分のプロセスの中でフォルダを開くので、
  そのときはレジストリの動詞をそもそも通っていないことになる。**その道は採らない**
  （シェル拡張を別プロセスへ追い出したこの設計と正面からぶつかる）ので、そこが
  この機能の限界になる。`Win+E`・フォルダのショートカット・他アプリの
  「フォルダーを開く」は確認済み、あるいはこの経路の外。
- メニューを開いている間、Kite のタイトルバーは非アクティブ表示になる。前景を
  取るのはホストの隠しウィンドウなので避けられない。メニューが閉じれば戻る。
- ホストが応答しなくなった場合（拡張がフォールトではなくデッドロックした場合）、
  待機は終わらない。ウィンドウは再描画され続け、タイトルバーの × で閉じられるが、
  クライアント領域は反応しない。タイムアウトを入れていないのは、開いたままの
  メニューを勝手に消す方が実害が大きいと判断したため。
- **アイコンオーバーレイの枠は OS 全体で 15 個**しかなく、先に登録されたものから
  埋まる（キー名の先頭に空白を入れて順番を奪い合うのが慣例）。OneDrive が 7、
  Google Drive が 4 使う環境では TortoiseGit の 9 個はほぼ載らない。エクスプローラー
  でも同じで Kite 側では直せない ─
  `HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\ShellIconOverlayIdentifiers`
  を整理してもらうしかない。
- **「ネットワーク」は遅く、返ってくるものが少ない。** 実測で 15 秒待って SSDP で
  見つかったメディア機器が 2 つ、という環境がある（エクスプローラーで開いても同じ）。
  一覧はワーカーで走り「読み込み中」も出るので固まりはしないが、SMB サーバーを
  探しているなら `\\server` を直接打つほうが速い（ROADMAP P2-8b）。
- **終了時の保存失敗は伝えられない**（「設定の置き場所」の末尾を参照）。
- **2 枚目以降のウィンドウは状態を保存しない。** タブを並べ直しても、そのウィンドウを
  閉じれば消える。残したい配置は元のウィンドウで作ること。
- **書庫の中はシェルの列挙なので、実フォルダより遅く、監視も効かない。** 更新は `F5`
  （仮想フォルダ共通の制限）。フォルダとして開けるのは `.zip` と `.cab` だけ ─ `.7z` や
  `.rar` はシェル名前空間に現れないので、開く先が無い。
- **書庫の中では、右クリックのメニューの「開く」「プログラムから開く」「プロパティ」が
  効かない。** どれもシェルに素通しする経路で、中身の入っていないファイルが渡る。
  **Enter とダブルクリックは効く**（Kite が自分で取り出す）。直し方は「ZIP をフォルダ
  として開く」に。
- サムネイルと検索は未実装（ROADMAP フェーズ 2）。
- キーシーケンスは 1 打鍵のみ。`Chord` は 2 打鍵に拡張できる形にしてある。
